---
phase: 06-code-audit
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - schema/migrations/008_prospect_unique.sql
  - tools/intel/insert_prospects.py
  - tools/intel/qcc_client.py
  - tools/intel/db_query.py
  - tools/intel/report_writer.py
  - tools/intel/export_prospects.py
  - .claude/skills/batch-prospect/SKILL.md
  - .claude/skills/single-prospect/SKILL.md
  - .claude/skills/brand-research/SKILL.md
  - .claude/skills/industry-research/SKILL.md
  - requirements.txt
autonomous: true
requirements: [AUD-01]
findings: [INTEL-01, INTEL-02, INTEL-03, INTEL-04, INTEL-05, INTEL-06, INTEL-07, INTEL-08, INTEL-09, INTEL-10, INTEL-11, INTEL-14, INTEL-15, INTEL-16]
must_haves:
  truths:
    - "按 batch-prospect SKILL 逐步执行（占位符降级模式），第五步能导出非空 Excel——线索带 report_id 落库"
    - "重复执行同一展会的 batch-prospect 不产生重复线索行"
    - "企查查 Status=201 在输出中呈现为'无结果'而非错误；102 输出明确的停止批量信号"
    - "industry-research 传入 '机械和设备/机床' 能正确按 L1+L2 过滤查询"
  artifacts:
    - path: "schema/migrations/008_prospect_unique.sql"
      provides: "customer_prospect (brand_id, qcc_key_no) 部分唯一索引 + 存量去重"
    - path: "tools/intel/insert_prospects.py"
      provides: "--json 文件输入 + INSERT OR IGNORE 幂等插入"
    - path: "tools/intel/qcc_client.py"
      provides: "201/101/102 状态码语义化处理"
  key_links:
    - from: ".claude/skills/batch-prospect/SKILL.md"
      to: "tools/intel/insert_prospects.py"
      via: "先建报告取 report_id → insert_prospects --json 携带 report_id"
    - from: "tools/intel/export_prospects.py"
      to: "customer_prospect.intel_report_id"
      via: "WHERE intel_report_id = ? 命中由插入时直接带 report_id 保证"
---

<objective>
Wave 3 · 企查查上线前修复。打通 batch-prospect 的"搜索→落库→报告→Excel"后半程断链（雷区 4），加幂等保障与状态码语义，修齐 skill 参数解析与依赖声明，使企查查接入达到可上线状态。

Purpose: 审计结论"接入路径方向正确、落库链路必须修复后才可上线"。按 REVIEW-intel 既定调整清单执行，明确不做重试装饰器/缓存/限速器/调用记录表（投机性复杂度，违反 Simplicity First）。
Output: 端到端可走通且幂等的情报挖掘链路。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-audit/AUDIT.md
@.planning/phases/06-code-audit/REVIEW-intel.md
@CLAUDE.md

前置：06-02 已将 intel 迁移重编号为 006 并建立 init_db 全量迁移机制，本计划新增迁移从 008 起（007 为 provenance 唯一索引）。
</context>

<tasks>

<task type="auto">
  <name>Task 1: customer_prospect 唯一约束 + 写入连接开启外键 [INTEL-03][INTEL-07]</name>
  <files>schema/migrations/008_prospect_unique.sql, tools/intel/insert_prospects.py, tools/intel/report_writer.py</files>
  <action>
1. [INTEL-03] 新建 `schema/migrations/008_prospect_unique.sql`：先按 (brand_id, qcc_key_no) 去重存量（保留 MIN(rowid)，仅 qcc_key_no IS NOT NULL 范围），再 `CREATE UNIQUE INDEX IF NOT EXISTS idx_prospect_brand_qcc ON customer_prospect(brand_id, qcc_key_no) WHERE qcc_key_no IS NOT NULL`。对生产库应用（经 06-02 的 init_db 迁移路径）。
2. [INTEL-03] insert_prospects.py:53-71 裸 INSERT 改为 `INSERT OR IGNORE`，并统计 inserted/skipped 输出（用 cursor.rowcount 累计）。qcc_key_no 为 None 的记录无法靠索引去重——插入前按 (brand_id, company_name) 查重跳过。
3. [INTEL-07] report_writer.py:110 与 insert_prospects.py:44 的 sqlite3.connect 之后统一执行 `PRAGMA foreign_keys = ON`，使无效 brand_id 当场报 FK 错误而非静默写入孤儿引用。
  </action>
  <verify>
    <automated>python3 一次性脚本：临时库上对同一 prospects 列表插入两次，断言第二次 inserted=0 且总行数不变；插入 brand_id='NONEXIST' 的报告断言抛 IntegrityError</automated>
  </verify>
  <done>重复插入幂等；坏 brand_id 当场报错；生产库索引就位</done>
  <commit>fix(intel): unique prospect constraint with idempotent insert, enable foreign_keys on writes [INTEL-03][INTEL-07]</commit>
</task>

<task type="auto">
  <name>Task 2: insert_prospects 改为 --json 文件输入 [INTEL-02]</name>
  <files>tools/intel/insert_prospects.py</files>
  <action>
修复 insert_prospects.py:20-35 的"改源码再运行"模式：删除源文件内的 `prospects: list[dict] = []` 可编辑区，新增 `--json <path>` 必选参数，从 JSON 文件读取 prospects 列表（结构与现有 dict 字段一致：company_name 必填校验保留）。同时新增 `--report-id <int>` 参数：提供时覆盖/填充每条记录的 intel_report_id（配合 Task 3 的报告先行流程）。脚本本体恢复只读，工作区不再因执行而变脏。不写兼容旧模式的代码路径。
  </action>
  <verify>
    <automated>echo '[{"company_name":"测试公司","brand_id":"EXPO-0001"}]' > /tmp/p.json && python3 tools/intel/insert_prospects.py --json /tmp/p.json --report-id 1 --db <临时库> 正常插入且 intel_report_id=1；git status 显示 tools/intel/insert_prospects.py 执行后无未提交改动</automated>
  </verify>
  <done>数据走参数文件，源码只读；--report-id 直通落库</done>
  <commit>fix(intel): insert_prospects reads --json input file instead of source edits [INTEL-02]</commit>
</task>

<task type="auto">
  <name>Task 3: batch-prospect 流程重排（报告先行）+ 删除内嵌脚本副本 + 调用计数汇总 [INTEL-01][INTEL-11]</name>
  <files>.claude/skills/batch-prospect/SKILL.md</files>
  <action>
1. [INTEL-01] 重排 SKILL.md 步骤顺序：现第四步（report_writer 创建报告）提前到 prospects 插入之前——新顺序：① 取参展商名单 ② 企查查搜索 ③ **创建 intel_report 拿到 report_id**（用 --content-file，见 Task 5）④ 将搜索结果写 /tmp/prospects.json 并执行 `insert_prospects.py --json /tmp/prospects.json --report-id <report_id>` ⑤ export_prospects.py --report-id 导出 Excel。删除"intel_report_id: None # 下一步创建报告后更新"的断链模板。
2. [INTEL-11] 删除 SKILL.md:61-112 内嵌的完整脚本副本（含相对路径 DB_PATH bug 的那份），替换为对真实工具的调用指引（一行命令 + JSON 字段说明）。
3. [QCC 评估调整项·无编号] 批量搜索循环段末尾增加指引：输出"本批次企查查调用 N 次（成功 X / 无结果 Y / 失败 Z）"汇总行，供 BD 对账 API 消耗。仅 prompt 文案，不建新表。
  </action>
  <verify>
    <automated>grep -n "intel_report_id.*None" .claude/skills/batch-prospect/SKILL.md 输出为空；grep -n "DB_PATH" .claude/skills/batch-prospect/SKILL.md 输出为空；grep -c "report-id" .claude/skills/batch-prospect/SKILL.md ≥2（插入与导出两步均引用）</automated>
  </verify>
  <done>按文档逐步执行流程不再断链；skill 内无第二份脚本源；有调用次数汇总指引</done>
  <commit>fix(intel): reorder batch-prospect flow report-first, drop embedded script copy [INTEL-01][INTEL-11]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 4: qcc_client 状态码语义化 201/101/102 [INTEL-06]</name>
  <files>tools/intel/qcc_client.py</files>
  <behavior>
    - Status="201" → 返回 {"Status":"201","Message":"查询无结果（正常）","Result":[]}，不带"[企查查错误]"字样
    - Status="101"/"102" → Message 含明确"停止批量、检查 Key/余额"提示文案
    - Status="200" → 行为不变
  </behavior>
  <action>
修复 qcc_client.py:120-125,140：`Status != "200"` 的单一错误分支拆为三类——201 映射为正常空结果（Message 明示"无此企业"语义，LLM 不会写成查询失败）；101（Key 无效）/102（余额不足）返回的 Message 以 "STOP_BATCH:" 前缀开头并说明原因（按次计费 API 的资损防线，batch-prospect skill 看到该前缀即终止循环——在 SKILL.md 批量段补一句"Message 以 STOP_BATCH 开头时立即停止批量并报告"）；其余非 200 维持现有错误结构。保持 {"Status","Message","Result"} 收敛结构不变。
  </action>
  <verify>
    <automated>python3 一次性脚本 mock _request 返回 201/102/200 三种响应，断言上述 behavior 三条全部成立（正式 mock 测试在 06-05 落 tests/）</automated>
  </verify>
  <done>201 不再误判为错误；102 有可被 LLM 识别的停止信号；占位符降级模式不受影响</done>
  <commit>fix(intel): map qcc status 201 to empty result, emit STOP_BATCH on 101/102 [INTEL-06]</commit>
</task>

<task type="auto">
  <name>Task 5: db_query 四处修复（l1/l2 解析、多命中消歧、company_history 语义、L2 计数）[INTEL-04][INTEL-05][INTEL-09][INTEL-14]</name>
  <files>tools/intel/db_query.py, .claude/skills/industry-research/SKILL.md, .claude/skills/single-prospect/SKILL.md</files>
  <action>
1. [INTEL-04] db_query.py industry-research 入口：参数含 '/' 时在脚本内拆分为 (industry_l1, industry_l2) 再查询（落实 argument-hint 已承诺的语法）；single-prospect 的 SKILL.md:20 argument-hint 收紧为"仅公司名"，调查目的由 skill 正文引导用户另行说明，不再混入 LIKE。确认 --l2 参数与斜杠语法行为一致，消除死参数状态。
2. [INTEL-05] db_query.py:39-42 brand_research：fetchone 改为 fetchall + 确定性排序（精确 name_cn/brand_id 匹配优先，再 scale_score DESC）；多命中时输出首条结果并在输出顶部列出"另有 N 条匹配：<前 5 条 brand_id+name 清单>"提示。
3. [INTEL-09] db_query.py:188-199 company_history 输出首行注明"匹配字段：organizer/name_cn（主办/冠名维度，非参展记录）"；single-prospect/SKILL.md:77-91 报告模板的"二、展会参与轨迹/参展历史摘要/参展明细"标题统一改为"展会关联记录（主办/冠名维度）"。
4. [INTEL-14] db_query.py:153-156,169 的"总品牌数" COUNT 查询改用与列表查询完全相同的 WHERE 条件（含 l2 过滤）。
  </action>
  <verify>
    <automated>python3 tools/intel/db_query.py industry-research "机械和设备/机床" 返回 L2 过滤结果而非"该行业暂无数据"；python3 tools/intel/db_query.py brand-research "机床" 输出含"另有 N 条匹配"；python3 tools/intel/db_query.py company-history "<某主办方>" 首行含"主办/冠名维度"</automated>
  </verify>
  <done>l1/l2 组合可用；多命中可见可控；语义错位修正；L2 计数与列表口径一致</done>
  <commit>fix(intel): parse l1/l2 syntax, disambiguate brand match, relabel company_history, fix l2 count [INTEL-04][INTEL-05][INTEL-09][INTEL-14]</commit>
</task>

<task type="auto">
  <name>Task 6: skills 注入面收敛 + --content-file + allowed-tools/导出格式联动 + openpyxl [INTEL-10][INTEL-15][INTEL-16][INTEL-08]</name>
  <files>.claude/skills/batch-prospect/SKILL.md, .claude/skills/single-prospect/SKILL.md, .claude/skills/brand-research/SKILL.md, .claude/skills/industry-research/SKILL.md, tools/intel/export_prospects.py, requirements.txt</files>
  <action>
1. [INTEL-10] 四个 SKILL.md：所有把 `$ARGUMENTS` 直接内插进 `!`...`` / bash 命令的位置，改为 skill 正文先指示用 Write 工具把参数写入 /tmp 文件，命令从文件读参（db_query 等工具如需加 `--args-file` 之类的读取入口，仅做最小参数读取改动）。消除引号/`$()` 注入面。
2. [INTEL-15] 四个 skill 中所有 `report_writer.py --content "$(cat ...)"` 用法统一改为 `--content-file /tmp/xxx_report.md`（工具已支持，纯 skill 文案修正，规避 128KB 单参上限与孤儿文件路径）。
3. [INTEL-16a] export_prospects.py:175-179：按 --out 扩展名推断格式（.csv→csv，.xlsx→xlsx）；--out 扩展名与显式 --format 冲突时报错退出，不再把 xlsx 二进制写进 .csv。
4. [INTEL-16b] 四个 SKILL.md frontmatter 的 `allowed-tools` 从空格分隔改为逗号分隔（Claude Code 惯例），改完用一次 skill 调用实测白名单生效。
5. [INTEL-08] requirements.txt 增加 `openpyxl>=3.1.0`，并 pip install 验证。
  </action>
  <verify>
    <automated>grep -rn '\$ARGUMENTS"' .claude/skills/*/SKILL.md 中不再出现直接内插进 shell 命令的用法；grep -rn "content-file" .claude/skills/ 四个 skill 均命中；python3 tools/intel/export_prospects.py --out /tmp/x.csv --format xlsx 报错退出非 0；python3 -c "import openpyxl" 成功</automated>
  </verify>
  <done>注入面收敛、长报告可传、格式不再错写、依赖声明完整</done>
  <commit>fix(intel): file-based skill args, --content-file everywhere, format/out linkage, add openpyxl [INTEL-08][INTEL-10][INTEL-15][INTEL-16]</commit>
</task>

<task type="auto">
  <name>Task 7: 占位符降级模式端到端演练 + 测试套件</name>
  <files>(无修改)</files>
  <action>
REVIEW-intel 验收门槛：在无 QCC Key 的占位符降级模式下，按修复后的 batch-prospect SKILL 完整流程人机各半地走一遍（用临时拷贝库）：搜索（降级占位）→ 创建报告 → insert_prospects --json --report-id → export_prospects --report-id 导出 xlsx。断言导出文件存在且行数 = 插入数。然后运行 `pytest tests/ -x` 确认无回归（intel 专项 mock 测试在 06-05 补齐）。
  </action>
  <verify>
    <automated>导出的 /tmp/prospects_test.xlsx 存在且 openpyxl 读取行数与插入条数一致；pytest tests/ -x 退出码 0</automated>
  </verify>
  <done>端到端链路在降级模式下全程走通；既有测试全绿；企查查接入解除"不可上线"状态（SUMMARY 中声明）</done>
  <commit>test(intel): end-to-end placeholder-mode drill green</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部参展商名单 → $ARGUMENTS → shell | 不可信文本进入命令行（INTEL-10） |
| 企查查 API → qcc_client | 外部响应状态语义进入批量决策 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege | SKILL $ARGUMENTS shell 内插 | mitigate | 参数走 /tmp 文件（Task 6.1） |
| T-06-11 | Denial of Service (资损) | 102 余额耗尽仍批量循环 | mitigate | STOP_BATCH 信号（Task 4） |
| T-06-12 | Tampering | 重复运行产生重复线索 | mitigate | 唯一索引 + OR IGNORE（Task 1） |
</threat_model>

<verification>
- 降级模式端到端演练通过（Excel 非空导出）
- 双插幂等、201/102 语义断言通过
- l1/l2、多命中、--content-file、openpyxl 逐项 grep/运行验证
- pytest 全绿
</verification>

<success_criteria>
INTEL-01~11、INTEL-14~16 共 14 项关闭；企查查批量挖掘流程按文档执行可全程走通，解除上线封禁。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-code-audit/06-03-SUMMARY.md`，须包含端到端演练记录与上线解禁声明。
</output>
