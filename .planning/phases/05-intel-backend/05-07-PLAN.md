---
phase: 05-intel-backend
plan: "07"
type: execute
wave: 4
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - .claude/skills/single-prospect/SKILL.md
autonomous: true
requirements:
  - REQ-16
  - REQ-17
  - REQ-18
  - REQ-19
  - REQ-20
  - REQ-21
  - REQ-24

must_haves:
  truths:
    - ".claude/skills/single-prospect/SKILL.md 存在且有有效的 YAML frontmatter"
    - "frontmatter 包含 disable-model-invocation: true"
    - "Skill 包含 db_query.py company-history 调用步骤（跨展会参展记录）"
    - "Skill 包含 qcc_client.py 工商信息查询步骤"
    - "Skill 报告模板包含：公司画像、展会参与轨迹、潜在需求分析、风险标注（违规代理排查）"
    - "Skill 末尾包含 report_writer.py 调用（report_type=single_prospect）"
    - "Skill 说明 API Key 未配置时的调研降级路径"
  artifacts:
    - path: ".claude/skills/single-prospect/SKILL.md"
      provides: "单一客户深度挖掘 Claude Code Skill"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/skills/single-prospect/SKILL.md"
      to: "tools/intel/db_query.py"
      via: "!`python3 tools/intel/db_query.py company-history`"
      pattern: "db_query\\.py company-history"
    - from: ".claude/skills/single-prospect/SKILL.md"
      to: "tools/intel/report_writer.py"
      via: "python3 tools/intel/report_writer.py --type single_prospect"
      pattern: "single_prospect"
---

<objective>
创建单一客户深度挖掘 Claude Code Skill：对协会、关键企业或代理机构做全息画像调研，整合 DB 参展轨迹 + 企查查工商信息 + WebSearch，输出含风险标注的客户画像报告。

Purpose: BD 需要在接触重要目标客户前，了解其展会参与历史、企业背景和潜在风险（违规代理排查）。本 Skill 整合三个数据源，生成可直接用于 BD 谈判准备的客户画像（REQ-16/REQ-19）。

Output:
- .claude/skills/single-prospect/SKILL.md — 完整 Skill 文件
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-RESEARCH.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-02-SUMMARY.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-03-SUMMARY.md

<interfaces>
<!-- db_query.py company-history 输出格式（来自 05-02-PLAN.md） -->
```
### '[公司名]' 相关参展记录（共 N 条，最多显示30条）
- 2024 | 中国国际机床展览会 (EXPO-0001) | 行业=机械和设备 | 面积=120m² | 城市=上海
- 2023 | 华南机床展 (EXPO-0002) | ...
```

<!-- qcc_client.py 命令行格式 -->
```bash
python3 tools/intel/qcc_client.py "公司名称" --size 3
# 输出: [企查查] 找到 N 条结果: - 企业全称 | KeyNo=... | 代码=... | 法人=...
```

<!-- report_writer.py single_prospect 写入命令 -->
```bash
python3 tools/intel/report_writer.py \
  --type single_prospect \
  --target_company "公司名称" \
  --content "$(cat /tmp/single_report.md)"
```

<!-- D-22: 联系人信息复用 person + exhibition_contact 表（表存在但当前为空） -->
<!-- 单一客户画像报告入 intel_report，不另建 customer_profile 表 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 .claude/skills/single-prospect/SKILL.md</name>
  <files>.claude/skills/single-prospect/SKILL.md</files>

  <read_first>
    - .planning/phases/05-intel-backend/05-CONTEXT.md — D-18, D-19, D-22（单一挖掘输入/输出/联系人数据复用）
    - .planning/phases/05-intel-backend/05-RESEARCH.md — REQ-16~REQ-20 对应的研究说明，尤其 REQ-20 vs D-22 的解决方案
    - .planning/phases/05-intel-backend/05-02-SUMMARY.md — company-history 查询输出格式
  </read_first>

  <action>
创建 .claude/skills/single-prospect/ 目录并写入 SKILL.md：

```markdown
---
name: single-prospect
description: 单一目标客户深度调研。输入公司名称（协会/企业/代理机构），整合 mwlab.db 参展轨迹、企查查工商信息和 WebSearch，输出全息客户画像 Markdown 报告，含风险标注（违规代理排查）。
argument-hint: "[公司名称 + 可选: 调查目的，例: 上海精密机床 代理商资质排查]"
disable-model-invocation: true
allowed-tools: Bash WebSearch Read Write
---

## 单一客户深度调研任务

**目标**: $ARGUMENTS

---

## 第一步：从 DB 获取目标公司参展轨迹（禁止虚构）

> 以下数据自动注入，来自 mwlab.db。查询 organizer 字段和品牌名称进行模糊匹配。
> 若公司名称较通用（如"上海机床"），DB 可能返回多条相关展会，请全部纳入分析。

!`python3 tools/intel/db_query.py company-history "$ARGUMENTS"`

---

## 第二步：企查查工商信息查询

执行企查查模糊搜索获取工商信息：

```bash
python3 tools/intel/qcc_client.py "$ARGUMENTS" --size 3
```

**降级说明**：若 QCC_APP_KEY 未配置，输出"[企查查未配置]"，请继续执行，在报告的企业工商信息章节注明"企查查未配置，以下信息来自 WebSearch"。

---

## 第三步：WebSearch 补充信息

使用 WebSearch 搜索以下内容：

1. `"$ARGUMENTS" 公司 展览代理 资质`（代理商资质核查）
2. `"$ARGUMENTS" 公司 纠纷 违规 骗局`（风险信息）
3. `"$ARGUMENTS" 公司 近况 业务`（最新动态）
4. 若是协会：`"$ARGUMENTS" 协会 会员 主办展会`
5. 若是大型企业：`"$ARGUMENTS" 股权 法人变更`（来自企查查或天眼查报道）

---

## 第四步：分析与报告生成

基于以上三个数据源，在 /tmp/single_report.md 生成全息客户画像，格式如下：

```markdown
# 客户画像报告：[公司名称]

**调研日期**：[YYYY-MM-DD]  
**调查目的**：[从 $ARGUMENTS 中提取，或填"BD 客户评估"]  
**数据来源**：mwlab.db + 企查查 + WebSearch

---

## 一、企业基本信息

| 字段 | 值 | 来源 |
|------|-----|------|
| 企业全称 | [企查查 Name 或 WebSearch] | 企查查/WebSearch |
| 统一社会信用代码 | [企查查 CreditCode] | 企查查 |
| 企查查 KeyNo | [企查查 KeyNo] | 企查查 |
| 法定代表人 | [企查查 OperName] | 企查查 |
| 成立日期 | [企查查 StartDate] | 企查查 |
| 企业状态 | [企查查 Status] | 企查查 |
| 注册地址 | [企查查 Address] | 企查查 |

> 若企查查未配置：以上信息来自 WebSearch，可信度较低，建议后续通过企查查核验。

---

## 二、展会参与轨迹（来自 mwlab.db）

[基于第一步 DB 数据填写。若 DB 无该公司相关记录，说明"mwlab.db 中未找到相关参展记录"。]

### 2.1 参展历史摘要
- 涉及展会数量：[数字，来自 DB]
- 时间跨度：[最早年份] - [最新年份]
- 主要参展行业：[行业标签，来自 DB industry_l1]
- 主要参展城市：[城市，来自 DB]

### 2.2 参展明细
[逐条列出 DB 中的参展记录，格式：年份 | 展会名称 | 面积 | 城市]

### 2.3 参展规律分析
[分析参展频率、规模变化、行业偏好。数据依据说明来自 DB。若 DB 无数据，说明"无历史参展记录，无法分析参展规律"。]

---

## 三、潜在需求分析

[基于参展轨迹和 WebSearch，分析该公司的展会需求：]

1. **当前状态评估**：[是否仍在参展？规模是否增长？]
2. **痛点推测**：[展会密度不足？地域覆盖弱？行业细分需求？]
3. **MDS 契合度**：[与 MDS 的业务领域是否有交集？]
4. **接触切入点**：[建议从哪个角度、哪个时机接触？]

---

## 四、风险标注

### 4.1 违规代理排查
[重点检查项：]
- [ ] 是否存在投诉/纠纷记录（WebSearch 搜索结果）
- [ ] 企业状态是否正常（企查查 Status 字段）
- [ ] 法定代表人是否有异常变更（WebSearch）
- [ ] 是否涉及展会代理诈骗相关报道（WebSearch）

**风险等级**：[低/中/高]
**风险说明**：[具体风险描述，若无可疑信息则说明"未发现风险信号"]

### 4.2 数据局限性
- DB 匹配结果基于 organizer 字段模糊查询，可能有遗漏
- 企查查状态为查询时点数据，不代表实时状态
- WebSearch 结果为公开信息，可能不完整

---

## 五、BD 行动建议

[不超过 300 字，具体可执行的行动建议：]

1. **优先级**：[高/中/低，说明理由]
2. **接触方式**：[冷访/推荐介绍/展会现场等]
3. **时间窗口**：[何时接触最合适，结合参展规律]
4. **注意事项**：[谈判时需注意的风险点]
```

---

## 第五步：保存报告到 DB

```bash
python3 tools/intel/report_writer.py \
  --type single_prospect \
  --target_company "$ARGUMENTS" \
  --content "$(cat /tmp/single_report.md)"
```

> 注意：联系人信息（如找到具体联系人）可手工录入 person + exhibition_contact 表（D-22 约定），本 Skill 不自动写入（无法从 WebSearch 可靠提取结构化联系人数据）。

执行成功后输出 `report_id=<N>`。

---

## 注意事项

1. **DB 无匹配不等于无记录**：DB 基于 organizer 字段搜索，品牌下无主办方字段时可能漏查
2. **企查查精确匹配**：若搜索结果返回多家同名公司，选择注册地和业务范围最匹配的一家
3. **风险标注必做**：即使 WebSearch 无负面信息，也必须在报告中明确标注"未发现风险信号"
4. **人工触发**：本 Skill 为人工触发，不做批量自动化执行
5. **联系人数据**：若调研中发现具体联系人，在报告正文记录，不写入 DB（person 表需人工维护）
```

关键约束（per D-02, D-18, D-19, D-22, REQ-23）：
- `disable-model-invocation: true` 必须存在
- `!` 命令调用 company-history 查询跨展会参展记录（REQ-17）
- 风险标注章节为必填（REQ-19）
- report_type=single_prospect 写入 intel_report（而非单建 customer_profile 表，per D-22 说明）
- 联系人数据不自动写入（person 表需人工维护，D-22 复用现有表但当前为空）
  </action>

  <verify>
    <automated>grep -c "disable-model-invocation: true" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md" && grep -c "db_query.py company-history" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md" && grep -c "single_prospect" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"</automated>
  </verify>

  <acceptance_criteria>
    - 文件存在：.claude/skills/single-prospect/SKILL.md
    - grep -c "disable-model-invocation: true" SKILL.md 返回 1
    - grep -c "db_query.py company-history" SKILL.md 返回 1（参展轨迹查询）
    - grep -c "qcc_client.py" SKILL.md 返回 ≥ 1（工商信息查询）
    - grep -c "风险标注\|违规代理" SKILL.md 返回 ≥ 1（风险章节）
    - grep -c "single_prospect" SKILL.md 返回 ≥ 1（report_type 写入）
    - grep -c "report_writer.py" SKILL.md 返回 ≥ 1（结果沉淀）
    - grep -c "WebSearch" SKILL.md 返回 ≥ 2（多次 WebSearch 调用说明）
    - SKILL.md 文件大于 3KB（包含完整报告模板）
  </acceptance_criteria>

  <done>single-prospect Skill 完整，整合 DB + 企查查 + WebSearch 三数据源，含风险标注章节（违规代理排查），末尾写入 intel_report（report_type=single_prospect）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → shell 命令 | 用户输入的公司名被嵌入 !命令和 qcc_client 参数 |
| WebSearch 结果 → 报告内容 | 外部搜索结果影响调研报告结论 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Injection | !命令中的 $ARGUMENTS | accept | 本地单用户工具；db_query.py 内部参数化 SQL |
| T-05-19 | Information Disclosure | WebSearch 搜索含目标公司名 | accept | BD 调研目的明确，公司名为公开信息 |
| T-05-20 | Repudiation | 报告内容来源不明 | mitigate | 报告模板每个字段标注数据来源（DB/企查查/WebSearch），保留可追溯性 |
</threat_model>

<verification>
```bash
ls -la "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"
grep "disable-model-invocation" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"
grep "company-history" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"
grep "风险标注" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"
grep "single_prospect" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/single-prospect/SKILL.md"
```
</verification>

<success_criteria>
- SKILL.md 存在，frontmatter 格式正确
- 整合三个数据源：DB（company-history）+ 企查查 + WebSearch
- 报告模板含五个章节（企业基本信息/参展轨迹/潜在需求/风险标注/BD 建议）
- 风险标注章节为必填，含违规代理排查清单
- 末尾调用 report_writer.py 写入 intel_report（report_type=single_prospect）
- API Key 未配置时有降级路径（WebSearch 替代）
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-07-SUMMARY.md，记录：
- Skill 调用方式（含调查目的参数）
- 三数据源整合方式
- 风险标注章节结构
- 与 D-22（person 表复用）的关系说明
</output>
