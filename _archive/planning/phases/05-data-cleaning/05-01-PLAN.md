---
phase: 05-data-cleaning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/clean_brands.py
  - scripts/data/name_en_patterns.py
  - scripts/data/md_category_rules.py
  - tests/test_clean_brands.py
  - tests/conftest.py
autonomous: true
requirements:
  - CLEAN-NAME-EN
  - CLEAN-INDUSTRY
user_setup: []

must_haves:
  truths:
    - "exhibition_brand 中 name_en 不再包含中文字符"
    - "exhibition_brand 中 industry_l1 只包含 6 个 MD 类别值"
    - "所有清洗操作支持 --dry-run 预览，不写库"
    - "执行 UPDATE 前自动备份 exhibition_brand 表"
  artifacts:
    - path: "scripts/clean_brands.py"
      provides: "主入口脚本，4 个子命令 + 参数化查询"
      min_lines: 120
    - path: "scripts/data/name_en_patterns.py"
      provides: "英文名称提取与生成函数"
      exports: ["extract_embedded_en", "generate_name_en", "is_name_en_valid"]
    - path: "scripts/data/md_category_rules.py"
      provides: "MD 6 类别关键词映射规则"
      exports: ["MD_CATEGORY_RULES", "classify_industry_l1"]
    - path: "tests/test_clean_brands.py"
      provides: "覆盖 CLEAN-NAME-EN + CLEAN-INDUSTRY 的单元测试"
      contains: "def test_name_en"
    - path: "tests/conftest.py"
      provides: "in-memory exhibition_brand 测试 fixtures"
      contains: "def exhibition_brand_conn"
  key_links:
    - from: "scripts/clean_brands.py"
      to: "mwlab.db"
      via: "sqlite3.connect with parameterized queries"
---

<objective>
创建 clean_brands.py 脚本骨架，并实现 name-en 和 industry 两个子命令。

Purpose: 建立统一的清洗脚本框架（argparse、DB 备份、--dry-run），先完成 name_en 标准化和 industry_l1 类别映射两个独立模块。

Output:
- scripts/clean_brands.py — 主脚本（4 个子命令桩 + name-en + industry 完整实现）
- scripts/data/name_en_patterns.py — 英文名提取/生成函数
- scripts/data/md_category_rules.py — 行业分类规则
- tests/test_clean_brands.py — 单元测试
- tests/conftest.py — 追加 exhibition_brand fixture
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-data-cleaning/05-RESEARCH.md
@.planning/phases/05-data-cleaning/05-VALIDATION.md
@schema/init_db.sql
@tests/conftest.py
@merge_engine.py

<interfaces>
From schema/init_db.sql — exhibition_brand 表结构:
```sql
CREATE TABLE IF NOT EXISTS exhibition_brand (
    brand_id              TEXT    PRIMARY KEY,                       -- EXPO-XXXX
    name_cn               TEXT    NOT NULL,
    name_en               TEXT    NOT NULL DEFAULT '',
    first_year            INTEGER,
    organizer             TEXT    NOT NULL DEFAULT '',
    [...]
    industry_l1           TEXT    NOT NULL DEFAULT '',
    industry_l2           TEXT    NOT NULL DEFAULT '',
    mds_related           TEXT    NOT NULL DEFAULT '',               -- 无/MFC/Reha China 等
    [...]
);
```

From tests/conftest.py — existing fixture pattern (should be extended, not replaced):
```python
def make_test_db() -> tuple[sqlite3.Connection, sqlite3.Connection]:
    """Returns (src_conn, target_conn) with sample data."""
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试脚手架和数据模块</name>
  <files>
    scripts/data/name_en_patterns.py
    scripts/data/md_category_rules.py
    tests/conftest.py (append)
    tests/test_clean_brands.py
  </files>
  <read_first>
    tests/conftest.py — 现有 fixture 模式，追加新的 exhibition_brand fixture
    schema/init_db.sql — exhibition_brand 表结构
  </read_first>
  <action>
    1. 创建 `scripts/data/name_en_patterns.py`：
       - `extract_embedded_en(name_cn: str) -> str | None`：从中文名提取嵌入的英文
          * Pattern 1: 末尾 ASCII 序列 (re.search(r'[A-Z][A-Za-z\s&amp;-]{3,}$', name_cn))
          * Pattern 2: 括号内英文 (re.search(r'[（(]([A-Z][A-Za-z\s&amp;-]{2,})[）)]', name_cn))
          * Pattern 3: 独立英文缩写 (re.search(r'\b([A-Z]{2,10})\b', name_cn))
       - `generate_name_en(name_cn: str) -> str`：为无英文名的中文名生成标准名称
          * 去掉年份前缀（`r'^\d{4}'`）、届次前缀（`r'^(?:第\d+届)'`）
          * 规则1: 括号内已有英文缩写 → 直接使用
          * 规则2: 提取核心主题词 + "EXPO"（如 "上海国际机床展" → "Machine Tool EXPO"）
          * 规则3: 首位关键词 + "EXPO"（如 "礼品展" → "Gift EXPO"）
       - `is_name_en_valid(name_en: str) -> bool`：检查 name_en 是否不含中文
       - 所有函数无需外部依赖（仅用 re / string）
    2. 创建 `scripts/data/md_category_rules.py`：
       - 导出 `MD_CATEGORY_RULES: dict[str, list[str]]` 字典
       - 六个类别及关键词（从 RESEARCH.md `Pattern 2` 直接复制，确保完整覆盖）：
         * "机械和设备": ["机械","机床","设备","五金","工业","铸造","冶金","模具","橡塑","塑料","橡胶","包装","印刷","制冷","暖通","泵阀","紧固件","电缆","电力","新能源","能源","电动车","汽车","汽配","车展","摩托车","自行车","玻璃","复合材料","纺织工业","纺织印花","染料","非开挖","建材","建筑","分析测试仪器","仪器","安防","劳保"]
         * "休闲": ["休闲","旅游","体育","户外","房车","露营","游艇","船艇","钓鱼","玩具","宠物","花卉","园艺","景观","礼品","珠宝","文具","教育","乐器","文化"]
         * "生活方式": ["生活","消费","食品","糖酒","餐饮","烘焙","茶","咖啡","酒","饮料","服装","纺织","家纺","家居","家电","家具","孕婴童","个人护理","美容美发","化妆品","奢侈品","品牌授权","光学眼镜","成人用品","连锁加盟"]
         * "科技+": ["科技","电子","消费电子","半导体","显示","AI","人工智能","互联网","物联网","大数据","通信","5G","软件","信息","数字","无人驾驶","无人机","无人系统","灯光","照明","广告标识","直播电商"]
         * "医疗和健康": ["医疗","健康","医","药","制药","口腔","牙科","康复","养老","银发","保健","医疗器械","生物"]
         * "零售贸易和服务": ["零售","贸易","服务","物流","连锁","商业","加盟","消费品"]
       - `classify_industry_l1(messy_value: str) -> str`：遍历 MD_CATEGORY_RULES，逐关键词匹配，返回第一个匹配的类别名称；无匹配返回 ""
       - `list_nonempty_categories() -> list[str]`：返回所有非空类别名称列表
    3. 向 `tests/conftest.py` 追加：
       - `exhibition_brand_conn()` fixture（使用 `@pytest.fixture`）
       - 创建 in-memory SQLite，CREATE TABLE exhibition_brand（从 schema/init_db.sql 复制完整 DDL）
       - 插入 20 条样本数据：5 条带有效 name_en + non-empty industry_l1，5 条空 name_en + empty industry_l1，5 条 name_en 含中文，5 条带可提取的嵌入英文
       - 使用参数化 INSERT，返回 `sqlite3.Connection`
    4. 创建 `tests/test_clean_brands.py`：
       - 导入 `from scripts.data.name_en_patterns import extract_embedded_en, generate_name_en, is_name_en_valid`
       - `test_extract_embedded_en()`：测试 3 个 pattern 各 2 个用例 + 1 个无英文用例
       - `test_generate_name_en()`：测试年份前缀去除、届次前缀去除、括号缩写提取、核心词+EXPO、fallback
       - `test_is_name_en_valid()`：纯英文 → True，含中文 → False
       - `test_classify_industry()`：每个 MD 类别至少 2 个关键词匹配用例 + 1 个无匹配用例
       - 使用 `from scripts.data.md_category_rules import classify_industry`
       - 测试使用 pytest assert，无外部依赖
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 -m pytest tests/test_clean_brands.py::test_extract_embedded_en tests/test_clean_brands.py::test_generate_name_en tests/test_clean_brands.py::test_is_name_en_valid tests/test_clean_brands.py::test_classify_industry -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_clean_brands.py 文件存在且包含 test_extract_embedded_en, test_generate_name_en, test_is_name_en_valid, test_classify_industry
    - tests/conftest.py 包含 "def exhibition_brand_conn" 和 "CREATE TABLE exhibition_brand"
    - scripts/data/name_en_patterns.py 导出 extract_embedded_en, generate_name_en, is_name_en_valid
    - scripts/data/md_category_rules.py 导出 MD_CATEGORY_RULES, classify_industry_l1
    - pytest 测试全部通过（4 个测试函数至少 12 个用例）
  </acceptance_criteria>
  <done>
    测试框架和数据模块创建完毕，pytest 通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 clean_brands.py 骨架并实现 name-en 子命令</name>
  <files>
    scripts/clean_brands.py
  </files>
  <read_first>
    merge_engine.py — 现有脚本风格（argparse, sqlite3, logging, Path）
    scripts/data/name_en_patterns.py — 刚创建的模块，确认函数签名
  </read_first>
  <action>
    1. 创建 `scripts/clean_brands.py`：
       - 文件头和导入（遵循 merge_engine.py 风格：`from __future__ import annotations`）
       - `BASE_DIR = Path(__file__).parent.parent`
       - `DB_PATH = BASE_DIR / "mwlab.db"`
       - logging 配置（与 merge_engine.py 相同格式）
       - `backup_table(conn: sqlite3.Connection)` 函数：检查 `exhibition_brand_backup_YYYYMMDD` 是否存在，不存在则 CREATE TABLE AS SELECT * FROM exhibition_brand
       - argparse 设置：
         * `--db` 参数（默认 DB_PATH）
         * `--dry-run` 参数（action="store_true"）
         * subparsers：name-en, industry, mds, jufair-l2（industry 和 mds/jufair-l2 作为 stub，只打印 "Not yet implemented"）
       - `cmd_name_en(args)` 函数实现：
         * Step 1: `UPDATE exhibition_brand SET name_en = '' WHERE name_en GLOB '*[一-龥]*'` — 清除含中文的 name_en
         * Step 2: 遍历 name_en = '' 的行，调用 extract_embedded_en(name_cn)，非 None 则 UPDATE
         * Step 3: 对剩余 name_en = '' 的行，调用 generate_name_en(name_cn)，非空则 UPDATE
         * Step 4: 打印统计（总行数、已处理、剩余、跳过）
         * 所有 UPDATE 使用参数化查询（`conn.execute(sql, (value, brand_id))`）
         * dry-run 模式下不执行 UPDATE，只打印 preview
       - `main()`: 解析参数 → `conn = sqlite3.connect(args.db)` → backup_table → args.func(args) → conn.commit/rollback
       - `if __name__ == "__main__": main()`
    2. 确保脚本可执行：
       - `#!/usr/bin/env python3` shebang
       - `chmod +x scripts/clean_brands.py`
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 scripts/clean_brands.py --help 2>&1 | grep -q "name-en" && echo "OK" ; python3 scripts/clean_brands.py name-en --dry-run 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - scripts/clean_brands.py 存在且包含 "#!/usr/bin/env python3"
    - `python3 scripts/clean_brands.py --help` 输出包含 name-en, industry, mds, jufair-l2
    - `python3 scripts/clean_brands.py name-en --dry-run` 不报错退出
    - `grep -c "backup_table" scripts/clean_brands.py` 返回值 >= 1
    - `grep -c "execute.*\?" scripts/clean_brands.py` 返回值 >= 3（参数化查询）
  </acceptance_criteria>
  <done>
    clean_brands.py 骨架完成，name-en 子命令可运行
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现 industry 子命令</name>
  <files>
    scripts/clean_brands.py
  </files>
  <read_first>
    scripts/clean_brands.py — 现有骨架，添加 industry 子命令
    scripts/data/md_category_rules.py — classify_industry_l1 函数
  </read_first>
  <action>
    1. 将 `cmd_industry(args)` 从 stub 替换为完整实现：
       - 导入 `from scripts.data.md_category_rules import classify_industry_l1, list_nonempty_categories`
       - Step 1: 查询所有 `industry_l1 != ''` 的行（SELECT brand_id, industry_l1 FROM exhibition_brand WHERE industry_l1 != ''）
       - Step 2: 对每一行，调用 `classify_industry_l1(industry_l1)` 获取 target 类别
       - Step 3: 如果 target 非空且 `target != industry_l1`，执行 `UPDATE exhibition_brand SET industry_l1 = ? WHERE brand_id = ?`
       - Step 4: 打印映射统计（总处理行、映射成功/失败行数）
       - Step 5: 打印未匹配的 industry_l1 值列表（用于人工复核）
       - 使用参数化查询，支持 --dry-run
    2. 确保 `cmd_industry` 被注册到 subparser
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 scripts/clean_brands.py industry --dry-run 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cmd_industry" scripts/clean_brands.py` 返回值 >= 2（定义 + 注册）
    - `python3 scripts/clean_brands.py industry --dry-run` 输出包含 "Mapping" 和未匹配列表
    - `grep -c "classify_industry_l1" scripts/clean_brands.py` 返回值 >= 1
    - `grep -c "execute.*?" scripts/clean_brands.py` 在新加的代码中包含参数化 UPDATE
  </acceptance_criteria>
  <done>
    industry 子命令完整，--dry-run 预览正常
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/clean_brands.py → mwlab.db | 本地 SQLite 写操作，同一信任域 |
| scripts/clean_brands.py → Excel 文件 | 本计划暂不涉及 Excel 读取（Plan 2 处理） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | SQL UPDATE statements | mitigate | 所有 SQL 使用参数化查询（`conn.execute(sql, (param,))`），禁止字符串拼接 |
| T-05-04 | Repudiation | UPDATE before backup | mitigate | `backup_table()` 在任意 UPDATE 前创建 exhibition_brand_backup_YYYYMMDD 快照；--dry-run 模式不写库 |
</threat_model>

<verification>
1. `python3 scripts/clean_brands.py --help` 输出 4 个子命令
2. `python3 scripts/clean_brands.py name-en --dry-run` 预览英文名清洗
3. `python3 scripts/clean_brands.py industry --dry-run` 预览行业分类映射
4. `python3 -m pytest tests/test_clean_brands.py -x -q` 全部通过
</verification>

<success_criteria>
- [ ] 两个完整子命令（name-en, industry）可执行，支持 --dry-run
- [ ] 测试覆盖英文名提取/生成和行业分类逻辑
- [ ] backup_table 在所有 UPDATE 前执行
- [ ] 所有 SQL 使用参数化查询
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-cleaning/05-01-SUMMARY.md`
</output>
