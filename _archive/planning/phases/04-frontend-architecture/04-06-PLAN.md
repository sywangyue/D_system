---
phase: 04-frontend-architecture
plan: 06
type: execute
wave: 2
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - app/login/page.tsx
  - app/calendar/calendar-content.tsx
  - app/calendar/calendar-view.tsx
  - app/map/map-content.tsx
  - app/map/map-view.tsx
  - app/setting/setting-content.tsx
autonomous: true
requirements: [UI-POOL]
must_haves:
  truths:
    - "登录页使用 MD Logo + 品牌色，POST 到 FastAPI /api/auth/login 获取 JWT"
    - "登录成功后保存 JWT 到 localStorage + cookie，重定向到 /dashboard"
    - "日历页使用 MD 品牌色事件样式（橙色调替代绿色）"
    - "地图页城市标记点使用 MD Orange 色"
    - "设置页从 FastAPI/SQLite 获取用户列表和系统状态"
    - "所有页面未登录时被 middleware 重定向到 /login"
  artifacts:
    - path: "app/login/page.tsx"
      provides: "MD 品牌登录页"
      contains: "fetch to /api/auth/login, saveAuth() call"
    - path: "app/calendar/calendar-view.tsx"
      provides: "MD 品牌日历视图"
      contains: "react-big-calendar with MD orange event style"
    - path: "app/map/map-view.tsx"
      provides: "MD 品牌地图"
      contains: "CircleMarker with md-orange fillColor"
  key_links:
    - from: "login/page.tsx" to: "auth_api.py" via: "POST /api/auth/login"
    - from: "calendar-content.tsx" to: "app/api/calendar/events" via: "fetch()"
    - from: "map-content.tsx" to: "app/api/map/markers" via: "fetch()"
    - from: "setting-content.tsx" to: "app/api/users + api/setting/status" via: "fetch()"
---

<objective>
更新登录页、日历、地图和设置页面以使用 JWT 认证流程 + MD 品牌色。这些页面在 04-02/03 完成 API 迁移和 04-04 完成品牌色定义后，统一接入新认证体系和新颜色。

Purpose: 整合所有页面到新的 FastAPI JWT 认证 + MD 品牌体系（D-16, D-07, D-08）
Output: 6 个页面/组件文件更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (Auth Flow lines 370-391, MapView Leaflet 模式 lines 617-665, CalendarView 模式, Setting 页)
@./lib/auth.ts (saveAuth, getUserInfo, clearAuth)
@./app/login/page.tsx (当前使用 Supabase auth——改为 FastAPI JWT)
@./app/calendar/calendar-view.tsx (绿色事件样式——改为 MD Orange)
@./app/map/map-view.tsx (蓝色/橙色国内国际标记——统一 MD 橙)
@./app/setting/setting-content.tsx (Supabase admin API——改为 SQLite 查询)
</context>

<interfaces>
FastAPI auth_api.py 接口（已由 04-01 创建）：
```python
POST /api/auth/login -> { token: str, email: str, role: str, display_name: str }
```

lib/auth.ts 接口（已由 04-01 创建）：
```typescript
saveAuth(info: UserInfo, token: string): void  // 登录成功时调用
clearAuth(): void                                 // 退出时调用
getUserInfo(): UserInfo | null                    // 检查当前用户
```

地图 Marker MD 色（替换绿色）：
```typescript
const mdOrange = "#fe5c00"    // 国内展会标记
const mdLightOrange = "#ff8c00" // 国际展会标记
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写登录页——FastAPI JWT + MD 品牌（使用环境变量替代硬编码 URL）</name>
  <files>app/login/page.tsx</files>
  <read_first>
    @./app/login/page.tsx (当前 Supabase auth——完整重写)
    @./lib/auth.ts (saveAuth 函数)
    @./.env.local (FASTAPI_URL 环境变量)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md (Auth Flow 数据流 lines 370-391)
  </read_first>
  <action>
  完整重写 /Volumes/databoard/AI Project/D_dashboard/app/login/page.tsx：

  保留现有的 UI 结构和文案（竞争盘面看板、登录表单、邮箱/密码输入），但修改认证逻辑：

  1. **移除**所有 Supabase 相关导入：
  - `import { createClient } from "@/lib/supabase/client"`
  - `supabase.auth.signInWithPassword()` 调用

  2. **添加** JWT 登录逻辑，使用环境变量 FASTAPI_URL（W3 修复——不硬编码 localhost:8000）：
  ```typescript
  import { saveAuth } from "@/lib/auth"

  const FASTAPI_URL = process.env.NEXT_PUBLIC_FASTAPI_URL || 'http://localhost:8000'

  async function handleSubmit(e: FormEvent) {
    e.preventDefault()
    // ... 校验逻辑保持不变 ...

    setLoading(true)
    try {
      const res = await fetch(`${FASTAPI_URL}/api/auth/login`, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email: email.trim().toLowerCase(), password }),
      })

      if (!res.ok) {
        const err = await res.json().catch(() => ({}))
        if (res.status === 401) {
          setError("邮箱或密码错误，请重试")
        } else {
          setError("网络异常，请稍后重试")
        }
        setLoading(false)
        return
      }

      const data = await res.json()
      // data = { token, email, role, display_name }

      // 保存认证信息到 localStorage
      saveAuth(
        { email: data.email, role: data.role, display_name: data.display_name },
        data.token
      )

      // 设置 session cookie（让 middleware 能读取）
      document.cookie = `session=${data.token}; path=/; max-age=86400; SameSite=Lax`

      router.push("/dashboard")
      router.refresh()
    } catch {
      setError("网络异常，请稍后重试")
      setLoading(false)
    }
  }
  ```

  3. UI 修改（MD 品牌）：
  - 输入框 focus 颜色从 `focus:border-accent focus:ring-accent` 保持（accent 现在指向 MD Orange）
  - 登录按钮 background 从 `bg-accent` 保持（已指向 MD Orange）
  - hover 从 `hover:bg-accent-dark` 保持（已指向 MD Orange-dark）
  - 添加 MD Logo 区域（如果 logomd.png 存在，用 next/image 显示；否则保持文字 "MWLAB"）

  4. **保留**现有的：
  - 表单布局（全屏居中 + 白色卡片）
  - 文案："MWLAB" / "竞争盘面看板" / "请使用内部账号登录"
  - 邮箱/密码 label
  - 错误显示（AlertCircle + 红色文字）
  - 加载态（Loader2 动画）
  </action>
  <verify>
    <automated>
      grep -c "saveAuth\|FASTAPI_URL\|/api/auth/login" app/login/page.tsx && ! grep -q "supabase" app/login/page.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "supabase" app/login/page.tsx` == 0（无 Supabase）
    - `grep -c "saveAuth" app/login/page.tsx` >= 1
    - `grep -c "FASTAPI_URL" app/login/page.tsx` >= 1（使用环境变量而非硬编码）
    - `grep -c "/api/auth/login" app/login/page.tsx` >= 1（POST 到 FastAPI）
    - `grep -c "session.*token.*cookie\|session=" app/login/page.tsx` >= 1
    - 保留 "竞争盘面看板" 文案
    - 保留邮箱/密码输入框 + 登录按钮
  </acceptance_criteria>
  <done>
    登录页使用 FastAPI JWT 认证（通过环境变量 FASTAPI_URL），保存 token 到 localStorage+cookie，无 Supabase 引用
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新日历页——MD 品牌色事件样式</name>
  <files>app/calendar/calendar-content.tsx, app/calendar/calendar-view.tsx</files>
  <read_first>
    @./app/calendar/calendar-view.tsx (绿色事件样式——改为 MD Orange)
    @./app/calendar/calendar-content.tsx (数据获取——验证 API 端点)
  </read_first>
  <action>
  修改 2 个文件：

  **A) app/calendar/calendar-view.tsx** — 更新事件样式
  将 eventStyle 中的颜色替换：
  ```typescript
  const eventStyle = {
    "竞争对手": {
      backgroundColor: "#FEE2E2",
      borderLeft: "3px solid #EF4444",  // 保留红色（语义色）
      color: "#DC2626",
      borderRadius: "4px",
    },
    "潜在伙伴": {
      backgroundColor: "#fff3ec",        // MD Orange 10%
      borderLeft: "3px solid #fe5c00",  // MD Orange
      color: "#e55300",                 // MD Orange Dark
      borderRadius: "4px",
    },
    default: {
      backgroundColor: "#F3F4F6",
      borderLeft: "3px solid #9CA3AF",
      color: "#6B7280",
      borderRadius: "4px",
    },
  }
  ```

  **B) app/calendar/calendar-content.tsx** — 不修改 API 调用逻辑
  当前已使用 fetch("/api/calendar/events")，这个 API 已被 04-03 重写为 better-sqlite3。
  只需确保：
  - 无 Supabase 引用（当前已有，确认不引入）
  - loading / error / empty 状态逻辑保留
  - 重试按钮保持（background bg-accent 已指向 MD Orange）
  </action>
  <verify>
    <automated>
      grep -c "#fff3ec\|#fe5c00" app/calendar/calendar-view.tsx && ! grep -q "22C55E\|DCFCE7" app/calendar/calendar-view.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - calendar-view.tsx: `grep -c "#fff3ec\|#fe5c00"` >= 2（潜在伙伴背景 + 边框）
    - calendar-view.tsx: `grep -c "#22C55E\|#DCFCE7\|16A34A"` == 0（无绿色）
    - calendar-view.tsx: "竞争对手" 保留红色系（#FEE2E2/#EF4444）
    - calendar-content.tsx: `grep -c "supabase"` == 0
  </acceptance_criteria>
  <done>
    日历事件样式使用 MD Orange 替代绿色，竞争对手保留语义红色
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新地图页 + 设置页——MD 颜色 + JWT 认证</name>
  <files>app/map/map-content.tsx, app/map/map-view.tsx, app/setting/setting-content.tsx</files>
  <read_first>
    @./app/map/map-view.tsx (蓝色/橙色标记——改为 MD 品牌色)
    @./app/map/map-content.tsx (数据获取——验证 API 端点)
    @./app/setting/setting-content.tsx (Supabase admin API——改为 fetch from API)
  </read_first>
  <action>
  修改 3 个文件：

  **A) app/map/map-view.tsx** — 更新地图标记颜色
  在 chinaStyle 和 intlStyle 中：
  ```typescript
  function chinaStyle(count: number) {
    return {
      radius: getRadius(count),
      fillColor: "#fe5c00",        // MD Orange（国内展会）
      fillOpacity: 0.7,
      color: "#e55300",            // MD Orange Dark
      weight: 2,
    }
  }

  function intlStyle(count: number) {
    return {
      radius: getRadius(count),
      fillColor: "#ff8c00",        // MD Light Orange（国际展会）
      fillOpacity: 0.65,
      color: "#e55300",            // MD Orange Dark
      weight: 2,
    }
  }
  ```
  保留 Popup 内容和结构不变。图例颜色已在 map-content.tsx 中使用 Legend 组件。

  **B) app/map/map-content.tsx** — 不做结构变更
  确认：
  - 已使用 dynamic import MapView（ssr: false）— 保留
  - 已使用 fetch("/api/map/markers") — 保留（API 已由 04-03 重写）
  - loading / error / empty 状态保留
  - 无 Supabase 引用

  **C) app/setting/setting-content.tsx** — 移除 Supabase 依赖
  主要变更：
  - 移除 `import { createClient } from "@/lib/supabase/client"` 和 `supabase.auth.getUser()`
  - 移除 useEffect 中的 Supabase role 检查（不再需要——middleware 已检查 role=admin）
  - 使用 `import { getUserInfo } from "@/lib/auth"` 读取用户信息
  - 用户列表从 `/api/users` 获取（已由 04-03 重写为 SQLite 查询）
  - 系统状态从 `/api/setting/status` 获取（已由 04-03 重写）
  - 简化 roleChecked 状态——直接从 getUserInfo().role 判断
  - 更新 SystemInfoBlock 中的 "Supabase 项目" 改为 "数据库类型: SQLite"
  </action>
  <verify>
    <automated>
      grep -c "#fe5c00\|#e55300" app/map/map-view.tsx && ! grep -q "supabase" app/setting/setting-content.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - map-view.tsx: chinaStyle 使用 "#fe5c00"（MD Orange）替代蓝色
    - map-view.tsx: intlStyle 使用 "#ff8c00"（MD Light Orange）替代橙色
    - map-view.tsx: `grep -c "#3B82F6\|#2563EB"` == 0（无蓝色残留）
    - map-content.tsx: `grep -c "supabase"` == 0
    - setting-content.tsx: `grep -c "supabase"` == 0
    - setting-content.tsx: `grep -c "getUserInfo\|lib/auth"` >= 1
    - setting-content.tsx: system_info 显示 "数据库类型: SQLite" 而非 Supabase 项目 ref
  </acceptance_criteria>
  <done>
    地图标记使用 MD Orange 色系；设置页使用 JWT auth + SQLite API，无 Supabase
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 登录页 -> FastAPI auth_api | 用户密码明文传输（HTTPS 未部署前使用 localhost） |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | 登录页密码传输 | accept | 开发阶段 localhost 明文可接受；生产需 HTTPS |
</threat_model>

<verification>
1. FastAPI auth_api 运行中 → 登录页输入 admin@mwlab.internal / admin123 可登录
2. 登录成功后跳转到 /dashboard，cookie 设置正确
3. 日历页事件颜色使用 MD Orange（潜在伙伴为橙色边框）
4. 地图页标记点使用 MD Orange（国内）/ MD Light Orange（国际）
5. 设置页显示用户列表和系统状态（无 Supabase 引用）
</verification>

<success_criteria>
- 登录页可完成 JWT 认证流程（POST FastAPI → 保存 token → 重定向）
- 日历页事件使用 MD 品牌色（橙色调）
- 地图页标记点使用 MD Orange 色系
- 设置页从 SQLite API 获取用户和系统状态
- 所有页面无 Supabase 引用
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-06-SUMMARY.md
</output>
