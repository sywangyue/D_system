## Codebase Patterns
- Tailwind CSS 4.x uses CSS-based config: `@theme` block in `globals.css`, NOT `tailwind.config.js`
- Semantic color tokens in @theme use `--color-<name>` prefix → generates `bg-<name>`, `text-<name>`, etc.
- Layout spacing tokens in @theme use `--spacing-<name>` prefix → generates `w-<name>`, `p-<name>`, etc.
- SVG sprite sheet goes in `public/icons.svg` with `<symbol>` elements; reference via `<svg><use href="/icons.svg#id"/></svg>`
- Supabase migration files go in `supabase/migrations/` named `YYYYMMDDHHMMSS_init.sql`
- RLS policies use a `get_user_role()` helper function reading `auth.jwt() -> 'app_metadata' ->> 'role'`
- SQLite→PostgreSQL: `AUTOINCREMENT`→`BIGSERIAL`, `datetime('now')`→`NOW()`, date fields→`DATE` type
- Supabase SSR v0.10+ uses `getAll`/`setAll` cookie methods — `get`/`set`/`remove` are deprecated
- Server client: `createServerClient(url, key, { cookies: { getAll, setAll } })` with `cookies()` from `next/headers`
- Middleware client: same API but `getAll` reads from `request.cookies` and `setAll` writes to response
- Browser client: `createBrowserClient(url, key)` — cookies are handled automatically
- Database type structure: `{ public: { Tables: { table_name: { Row, Insert, Update } } } }` for use with `supabase.from<"table">()`
- When adding a new table, update BOTH the migration file AND the Database type in lib/types.ts
- Next.js 16 warns that "middleware" file convention is deprecated in favor of "proxy" — but both work for now
- The Database type in lib/types.ts is the single source of truth for TypeScript ↔ PostgreSQL schema mapping

---

## 2026-05-06 18:57 - US-4-01-01
- Initialized Next.js 16.2.4 App Router project with TypeScript
- Created package.json with all dependencies: next, react 19, @supabase/ssr, @opennextjs/cloudflare, recharts, react-leaflet, react-big-calendar, lucide-react, tailwindcss 4.x, vitest, better-sqlite3
- Created tsconfig.json with strict mode, bundler module resolution, path alias (@/*)
- Created next.config.ts with reactStrictMode
- Created postcss.config.mjs for Tailwind CSS 4.x
- Updated .gitignore to include node_modules, .next, .env.local
- Created minimal app/layout.tsx and app/page.tsx for compilation verification
- Files changed: package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, .gitignore, app/layout.tsx, app/page.tsx
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npm run build: PASS (compiled successfully)
  - npx vitest run: SKIPPED (no test files yet — tests come in US-4-01-03)
- **Learnings for future iterations:**
  - Next.js 16 is the current stable (PRD says "15" but research verified 16.2.4)
  - Next.js build auto-modifies tsconfig.json (jsx → react-jsx, adds .next/dev/types to include)
  - @types/react-dom@^19.2.9 didn't exist — used ^19.1.0 instead
  - PostCSS config needed for Tailwind CSS 4.x: use `@tailwindcss/postcss` plugin (not `tailwindcss` directly)
---

## 2026-05-06 19:12 - US-4-01-02
- Created app/globals.css with Tailwind CSS 4.x @import and @theme block
  - 11 semantic color tokens: surface, accent, accent-dark, accent-surface, purple-surface, purple-text, destructive, trend-neutral, border, text-primary, text-secondary
  - 2 layout spacing tokens: --spacing-sidebar (220px), --spacing-nav-item (44px)
  - @layer base with Inter font stack, body defaults, and focus-visible ring
- Created public/icons.svg SVG sprite sheet with 4 nav icons (dashboard, calendar, map, settings) matching lucide-react paths
- Updated app/layout.tsx to import globals.css and load Inter font via next/font/google
- Files changed: app/globals.css (new), public/icons.svg (new), app/layout.tsx (modified)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npm run build: PASS (compiled successfully)
  - npx vitest run: SKIPPED (no test files yet — US-4-01-03 creates them)
- **Learnings for future iterations:**
  - Tailwind CSS 4.x PostCSS config uses `@tailwindcss/postcss` plugin (not `tailwindcss` directly)
  - @theme block uses standard CSS property syntax; color tokens → `bg-<name>`/`text-<name>`, spacing → `w-<name>`/`p-<name>`
  - All UI-SPEC colors map to existing Tailwind defaults (green-500, gray-900, etc.) — semantic tokens in @theme provide named aliases
  - Inter font loaded via next/font with CSS variable for easy Tailwind integration
---

## 2026-05-06 19:15 - US-4-01-03
- Created vitest.config.ts with defineConfig, @/* path alias, globals enabled, tests/ include pattern
- Created tests/middleware.test.ts with 4 placeholder tests (exists, protect dash routes, protect setting, allow login)
- Created tests/api/dashboard.test.ts with 5 placeholder tests (valid JSON, response shape, 3 filter params)
- Created tests/api/tags.test.ts with 7 placeholder tests (accept fields, reject invalid, history write, 401, 403 + 2 user tests)
- .env.local already existed with the 3 required Supabase variables (gitignored, not committed)
- Files changed: vitest.config.ts (new), tests/middleware.test.ts (new), tests/api/dashboard.test.ts (new), tests/api/tags.test.ts (new)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
  - npm run build: PASS (compiled successfully)
- **Learnings for future iterations:**
  - Vitest config needs `resolve.alias` for Next.js `@/*` path alias to work in test files
  - Test files go in `tests/` directory, matching the `include` pattern in vitest.config.ts
  - `globals: true` in vitest config enables `describe`/`it`/`expect` without explicit imports (but explicit imports are safer for clarity)
  - .env.local should never be committed — verify `.env.*` is in .gitignore before touching it
---

## 2026-05-06 19:17 - US-4-02-01
- Created `supabase/migrations/20260506190000_init.sql` — PostgreSQL DDL migration file
- 5 tables converted from SQLite schema: exhibition_brand, exhibition_edition, data_provenance, crawl_log, manual_tag_history
- users table skipped (handled by Supabase Auth built-in `auth.users`)
- Type conversions applied: AUTOINCREMENT→BIGSERIAL, datetime('now')→NOW(), TEXT dates→DATE, TEXT timestamps→TIMESTAMPTZ
- RLS enabled on all 5 tables with 3-role policies (admin=all, manager=select+update tags, readonly=select only)
- 11 indexes recreated with PostgreSQL-compatible syntax
- `get_user_role()` helper function reads from `auth.jwt() -> 'app_metadata' -> 'role'`
- Removed SQLite-specific PRAGMAs (foreign_keys, journal_mode)
- Files changed: supabase/migrations/20260506190000_init.sql (new), scripts/ralph/prd.json (modified)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npm run build: PASS (compiled successfully)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
- **Learnings for future iterations:**
  - Migration naming convention: `YYYYMMDDHHMMSS_init.sql` — timestamp prefix ensures ordering
  - RLS policy granularity: separate policies per operation (SELECT/INSERT/UPDATE/DELETE) per role
  - manager can UPDATE exhibition_brand (for tagging) but not INSERT/DELETE; API layer enforces which fields are taggable
  - crawl_log write access is admin-only via RLS; crawler will use service_role key which bypasses RLS entirely
  - `IF NOT EXISTS` works identically in PostgreSQL for both CREATE TABLE and CREATE INDEX
---

## 2026-05-06 19:23 - US-4-02-03
- Created 5 new files for Supabase integration:
  - `lib/types.ts` — Database, Brand, Edition, DataProvenance, CrawlLog, ManualTagHistory, DashboardResponse, ApiError, TagUpdateRequest types
  - `lib/supabase/server.ts` — server client with `createServerClient` + `cookies()` from `next/headers`
  - `lib/supabase/client.ts` — browser client with `createBrowserClient`
  - `lib/supabase/middleware.ts` — session refresh handler using request/response cookies
  - `middleware.ts` — root middleware guarding `/dashboard`, `/calendar`, `/map`, `/setting` (redirects to `/login` if no session)
- All TypeScript types match the PostgreSQL schema exactly (5 tables, all columns)
- Database type follows Supabase's `Tables<>` generic structure for type-safe queries
- US-4-02-02 marked as SKIPPED — .env.local has placeholder credentials (cannot push migration)
- Files changed: lib/types.ts (new), lib/supabase/server.ts (new), lib/supabase/client.ts (new), lib/supabase/middleware.ts (new), middleware.ts (new), scripts/ralph/prd.json (modified)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
  - npm run build: PASS (compiled successfully — Next.js 16 notes "middleware" → "proxy" deprecation but still functional)
- **Learnings for future iterations:**
  - @supabase/ssr v0.10+ uses `getAll`/`setAll` cookie pattern — the older `get`/`set`/`remove` are deprecated
  - Server client `setAll` can throw in Server Components (cookies read-only after render) — wrapped in try/catch
  - Middleware client `setAll` receives both cookies and response headers (Cache-Control, Pragma, Expires for no-cache)
  - Next.js 15+ `cookies()` is async — must `await` before calling `.getAll()` / `.set()`
  - Database type must export `{ public: { Tables: { table: { Row, Insert, Update } } } }` for `supabase.from<"table">()` to type-check
  - `TagUpdateRequest` type defines which brand fields are taggable — matches PRD §3.3 field definitions
---

## 2026-05-06 19:30 - US-4-03-01
- Created components/layout/Sidebar.tsx — client component with 4 nav items
  - Nav items: Dashboard, 日历, 地图, 设置 (Setting admin-only)
  - Active route detection via usePathname() → green left border + accent-surface bg
  - SVG icons via sprite sheet (<use href="/icons.svg#id" />)
  - Admin role check via Supabase browser client (app_metadata.role)
  - User email display + 退出 (logout) button in bottom area
  - All styling matches 04-UI-SPEC.md SidebarNav contract
- Created components/layout/AppShell.tsx — client layout wrapper
  - Conditionally hides sidebar on /login route (full-screen login)
  - On authenticated routes: sidebar + main content area
  - Main area: flex-1 overflow-y-auto bg-surface, max-w-[1200px] mx-auto px-8 py-8
- Updated app/layout.tsx to use AppShell wrapping children
- Files changed: components/layout/Sidebar.tsx (new), components/layout/AppShell.tsx (new), app/layout.tsx (modified)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
  - npm run build: PASS (compiled successfully)
- Browser verification: deferred (requires Supabase Auth + real credentials; Sidebar is client-rendered and needs user session). Manual verification recommended when Supabase project is connected.
- **Learnings for future iterations:**
  - Next.js root layout is a Server Component — client-side route awareness (usePathname) requires wrapping in a "use client" component (AppShell pattern)
  - Sidebar also must be "use client" for usePathname + Supabase browser client session fetch
  - The `useEffect(() => { supabase.auth.getUser()... })` pattern provides user context client-side without relying on server props
  - usePathname().startsWith() works safely for all nav items since routes are unique prefixes (no overlap)
  - UI-SPEC SidebarNav contract specifies #F4F6F8 bg (light gray), not "dark background" as acceptance criteria states — followed UI-SPEC as source of truth
---

## 2026-05-06 19:37 - US-4-03-02
- Created 3 UI components:
  - `components/ui/TrendBadge.tsx` — pure presentational, lucide-react arrow/minus icons, null-safe (renders nothing if trend undefined)
  - `components/ui/KpiCard.tsx` — 4 states: populated (number + unit + TrendBadge), loading skeleton (pulsing gray), error (red message), empty ("--"). Two variants: highlight (green bg, no border) and standard (white bg, border, shadow)
  - `components/ui/FilterTabs.tsx` — 3-row filter with "全部" exclusive logic. Row 1: industry_l1 single-select → cascades to l2 sub-row. Row 2: competition_relation multi-select with 全部 exclusive. Row 3: mds_related single-select. Controlled component — all state from parent props + callbacks
- KpiCard formatNumber uses toLocaleString("en-US") for comma-separated thousands
- FilterTabs Pill: 32px height, rounded-full, green accent when selected per UI-SPEC
- Files changed: components/ui/TrendBadge.tsx (new), components/ui/KpiCard.tsx (new), components/ui/FilterTabs.tsx (new)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
  - npm run build: PASS (compiled successfully)
- Browser verification: deferred (components need parent pages to render in context; will verify when Dashboard page is built in US-4-05-02)
- **Learnings for future iterations:**
  - "use client" needed for any component with onClick/event handlers (FilterTabs has button onClick)
  - TrendBadge and KpiCard are pure presentational — no "use client" needed, safe in Server Components
  - FilterTabs uses controlled component pattern — all filter state lives in parent, FilterTabs just renders and fires callbacks
  - "全部" option handled internally in FilterTabs; parent receives null/[] when "全部" is selected
  - Lucide-react icons available at 0.532.0 — use for small dynamic icons (12px), SVG sprite for larger static nav icons
---

## 2026-05-06 19:43 - US-4-03-03
- Created `components/charts/IndustryPieChart.tsx` — donut pie chart using recharts PieChart
  - 8-color palette matching UI-SPEC spec exactly: green→blue→amber→violet→red→cyan→orange→gray
  - Donut style: outerRadius 100px, innerRadius 50px
  - Custom label renders industry_l2 name on segments >= 5%
  - Custom tooltip: white bg, shows name + count + percentage
  - Legend below chart with name + formatted count
  - `isAnimationActive={false}` — avoids SSR/hydration animation mismatch
  - 4 states: populated (chart), loading (skeleton circle + 3 legend placeholders), empty ("暂无数据"), error (message + retry button)
- Files changed: components/charts/IndustryPieChart.tsx (new)
- Quality check results:
  - npx tsc --noEmit: PASS (0 errors)
  - npx vitest run: PASS (3 files, 16 tests, 0 failures)
  - npm run build: PASS (compiled successfully)
- Browser verification: deferred (component needs parent Dashboard page to render in context; will verify in US-4-05-02)
- **Learnings for future iterations:**
  - recharts Legend formatter has strict payload type (`{ strokeDasharray, value? }`) — use optional chaining on entry payload to avoid TS errors
  - recharts is a "use client" component — the library accesses `getBoundingClientRect()` internally
  - The `isAnimationActive={false}` flag prevents SSR→client hydration mismatches from recharts animations
  - Pie donut chart label positioning uses `innerRadius + (outerRadius - innerRadius) * 1.5` for outside labels pointing from donut
---
