---
phase: 05-intel-backend
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - schema/migrations/005_intel_tables.sql
  - tools/intel/.gitkeep
  - reports/industry/.gitkeep
  - reports/brand/.gitkeep
  - reports/customer/.gitkeep
autonomous: true
requirements:
  - REQ-05
  - REQ-10
  - REQ-15
  - REQ-20
  - REQ-23

must_haves:
  truths:
    - "执行 schema/migrations/005_intel_tables.sql 后 mwlab.db 中存在 intel_report 表"
    - "执行 schema/migrations/005_intel_tables.sql 后 mwlab.db 中存在 customer_prospect 表"
    - "intel_report 表有 report_type CHECK 约束，只允许 'industry_research'、'brand_research'、'batch_prospect'、'single_prospect'"
    - "customer_prospect 表有 brand_id 外键关联 exhibition_brand"
    - "reports/ 目录结构已创建（industry/brand/customer 三个子目录）"
    - "tools/intel/ 目录存在（供后续 Wave 2 脚本使用）"
  artifacts:
    - path: "schema/migrations/005_intel_tables.sql"
      provides: "intel_report + customer_prospect 完整 DDL + 索引"
      contains: "CREATE TABLE IF NOT EXISTS intel_report"
    - path: "tools/intel/.gitkeep"
      provides: "工具目录占位符"
    - path: "reports/industry/.gitkeep"
      provides: "报告目录占位符"
  key_links:
    - from: "schema/migrations/005_intel_tables.sql"
      to: "mwlab.db"
      via: "sqlite3 mwlab.db < schema/migrations/005_intel_tables.sql"
      pattern: "intel_report|customer_prospect"
    - from: "customer_prospect"
      to: "exhibition_brand"
      via: "FOREIGN KEY (brand_id) REFERENCES exhibition_brand(brand_id)"
      pattern: "REFERENCES exhibition_brand"
---

<objective>
建立 Phase 5 情报后端的 DB 基础层：新增两张情报专用表并完成项目目录结构初始化。

Purpose: 所有后续 Wave（情报脚本、Skill 文件）都依赖这两张表持久化调研结果。没有这一层，Wave 2-4 的写入操作无法执行。

Output:
- schema/migrations/005_intel_tables.sql — intel_report + customer_prospect 完整 DDL
- mwlab.db 中新增两张表（执行迁移后）
- tools/intel/ 目录（Wave 2 脚本的存放位置）
- reports/{industry,brand,customer}/ 目录（Markdown 报告输出位置）
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/ROADMAP.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/schema/init_db.sql

<interfaces>
<!-- 现有 DB 表结构（从 schema/init_db.sql 提取） -->

```sql
-- exhibition_brand 主键
brand_id TEXT PRIMARY KEY  -- 格式: EXPO-XXXX

-- exhibition_edition 外键
brand_id TEXT NOT NULL REFERENCES exhibition_brand(brand_id) ON DELETE CASCADE
```

<!-- 新表的外键约束必须指向 exhibition_brand.brand_id -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 DB 迁移文件（intel_report + customer_prospect DDL）</name>
  <files>schema/migrations/005_intel_tables.sql</files>

  <read_first>
    - schema/init_db.sql — 确认现有表名、外键约束风格、CHECK 约束语法
  </read_first>

  <action>
在 schema/migrations/ 目录下（如不存在则创建）新建 005_intel_tables.sql，内容如下（严格复制，不修改字段名或约束）：

```sql
-- Phase 5: 情报后端 · 新增表迁移
-- 执行方式: sqlite3 /path/to/mwlab.db < schema/migrations/005_intel_tables.sql
-- 依赖: exhibition_brand 表必须已存在

PRAGMA foreign_keys = ON;

-- ─── 表1: intel_report（统一调研报告存储）─────────────────────────
CREATE TABLE IF NOT EXISTS intel_report (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    report_type     TEXT    NOT NULL
                        CHECK (report_type IN (
                            'industry_research',
                            'brand_research',
                            'batch_prospect',
                            'single_prospect'
                        )),
    -- 输入参数（根据 report_type 填写对应字段）
    brand_id        TEXT    REFERENCES exhibition_brand(brand_id) ON DELETE SET NULL,
    industry_l1     TEXT,
    industry_l2     TEXT,
    target_company  TEXT,   -- single_prospect 时的目标公司名
    params_json     TEXT    NOT NULL DEFAULT '{}',  -- 完整输入参数 JSON
    -- 输出内容
    report_md       TEXT    NOT NULL DEFAULT '',    -- Markdown 报告正文
    report_file     TEXT    NOT NULL DEFAULT '',    -- 相对项目根的文件路径
    -- 元数据
    status          TEXT    NOT NULL DEFAULT 'draft'
                        CHECK (status IN ('draft', 'published', 'archived')),
    created_by      TEXT    NOT NULL DEFAULT 'claude-code',
    created_at      TEXT    NOT NULL DEFAULT (datetime('now', 'localtime')),
    updated_at      TEXT    NOT NULL DEFAULT (datetime('now', 'localtime'))
);

CREATE INDEX IF NOT EXISTS idx_intel_report_type
    ON intel_report(report_type, created_at DESC);
CREATE INDEX IF NOT EXISTS idx_intel_report_brand
    ON intel_report(brand_id) WHERE brand_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_intel_report_industry
    ON intel_report(industry_l1, industry_l2) WHERE industry_l1 IS NOT NULL;

-- ─── 表2: customer_prospect（批量客户挖掘结果）────────────────────
CREATE TABLE IF NOT EXISTS customer_prospect (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    -- 关联
    intel_report_id     INTEGER REFERENCES intel_report(id) ON DELETE SET NULL,
    brand_id            TEXT    REFERENCES exhibition_brand(brand_id) ON DELETE SET NULL,
    -- 数据来源
    source_type         TEXT    NOT NULL
                            CHECK (source_type IN ('qcc_search', 'manual', 'db_match')),
    -- 企查查字段（API 返回原始字段名，snake_case 映射）
    qcc_key_no          TEXT,           -- 企查查内部唯一 ID (KeyNo)
    company_name        TEXT    NOT NULL,
    credit_code         TEXT,           -- 统一社会信用代码 (CreditCode)
    oper_name           TEXT,           -- 法定代表人 (OperName)
    start_date          TEXT,           -- 成立日期 YYYY-MM-DD (StartDate)
    company_status      TEXT,           -- 企业状态 (Status)
    reg_no              TEXT,           -- 注册号 (No)
    address             TEXT,           -- 注册地址 (Address)
    -- BD 评估字段
    prospect_score      INTEGER CHECK (prospect_score IS NULL OR prospect_score BETWEEN 1 AND 5),
    contact_status      TEXT    NOT NULL DEFAULT ''
                            CHECK (contact_status IN ('未接触', '已接触', '谈判中', '合作中', '放弃', '')),
    notes               TEXT    NOT NULL DEFAULT '',
    -- 元数据
    created_at          TEXT    NOT NULL DEFAULT (datetime('now', 'localtime')),
    updated_at          TEXT    NOT NULL DEFAULT (datetime('now', 'localtime'))
);

CREATE INDEX IF NOT EXISTS idx_prospect_brand
    ON customer_prospect(brand_id) WHERE brand_id IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_prospect_company
    ON customer_prospect(company_name);
CREATE INDEX IF NOT EXISTS idx_prospect_qcc
    ON customer_prospect(qcc_key_no) WHERE qcc_key_no IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_prospect_report
    ON customer_prospect(intel_report_id) WHERE intel_report_id IS NOT NULL;
```

注意事项（per D-06, D-20, D-21, REQ-23）：
- 使用 `CREATE TABLE IF NOT EXISTS` — 幂等，可安全重复执行
- 所有字段名 snake_case（per CLAUDE.md）
- `intel_report` 用 `report_type` 字段区分四种报告类型，满足 REQ-05/REQ-10（用 report_type='industry_research'/'brand_research' 替代分开建表）
- `customer_prospect` 的 `source_type` 包含 'manual'，支持用户手工提供参展商名单（解决 DB 无参展商明细的限制）
  </action>

  <verify>
    <automated>sqlite3 "/Volumes/databoard/AI Project/D_dashboard/mwlab.db" ".read /Volumes/databoard/AI Project/D_dashboard/schema/migrations/005_intel_tables.sql" && sqlite3 "/Volumes/databoard/AI Project/D_dashboard/mwlab.db" ".tables" | grep -E "intel_report|customer_prospect"</automated>
  </verify>

  <acceptance_criteria>
    - sqlite3 mwlab.db ".tables" 输出包含 "intel_report" 和 "customer_prospect"
    - sqlite3 mwlab.db "PRAGMA table_info(intel_report);" 返回至少 12 行（所有字段）
    - sqlite3 mwlab.db "PRAGMA table_info(customer_prospect);" 返回至少 15 行
    - sqlite3 mwlab.db "INSERT INTO intel_report (report_type, params_json) VALUES ('industry_research', '{}');" 执行成功（返回 0）
    - sqlite3 mwlab.db "INSERT INTO intel_report (report_type, params_json) VALUES ('invalid_type', '{}');" 返回 CHECK constraint failed 错误
    - 文件 schema/migrations/005_intel_tables.sql 包含字符串 "CREATE TABLE IF NOT EXISTS intel_report"
    - 文件 schema/migrations/005_intel_tables.sql 包含字符串 "CREATE TABLE IF NOT EXISTS customer_prospect"
  </acceptance_criteria>

  <done>两张情报表已在 mwlab.db 中创建，迁移文件幂等可重复执行，CHECK 约束正确拒绝非法 report_type 值</done>
</task>

<task type="auto">
  <name>Task 2: 初始化项目目录结构</name>
  <files>
    tools/intel/.gitkeep
    reports/industry/.gitkeep
    reports/brand/.gitkeep
    reports/customer/.gitkeep
  </files>

  <read_first>
    - 无（纯目录创建，不依赖现有代码）
  </read_first>

  <action>
创建以下目录和 .gitkeep 占位文件（per RESEARCH.md 推荐项目结构）：

1. tools/intel/.gitkeep — 情报脚本目录（Wave 2 将在此创建 db_query.py、qcc_client.py 等）
2. reports/industry/.gitkeep — 行业调研报告输出目录
3. reports/brand/.gitkeep — 品牌调研报告输出目录
4. reports/customer/.gitkeep — 客户画像报告输出目录

每个 .gitkeep 文件内容为空（零字节文件），目的只是让 git 追踪目录。

同时在 tools/intel/ 下创建 __init__.py（空文件），使其成为 Python 包，支持 `python3 tools/intel/db_query.py` 的模块导入风格。
  </action>

  <verify>
    <automated>ls "/Volumes/databoard/AI Project/D_dashboard/tools/intel/.gitkeep" "/Volumes/databoard/AI Project/D_dashboard/reports/industry/.gitkeep" "/Volumes/databoard/AI Project/D_dashboard/reports/brand/.gitkeep" "/Volumes/databoard/AI Project/D_dashboard/reports/customer/.gitkeep"</automated>
  </verify>

  <acceptance_criteria>
    - ls tools/intel/ 显示 .gitkeep 和 __init__.py
    - ls reports/ 显示 industry/ brand/ customer/ 三个子目录
    - ls reports/industry/ 显示 .gitkeep（目录非空，git 可追踪）
    - git status 显示 tools/intel/ 和 reports/ 目录为新增文件（untracked 或 staged）
  </acceptance_criteria>

  <done>项目目录结构初始化完成，tools/intel/ 和 reports/ 三个子目录就位，Wave 2 脚本可直接写入</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL 文件 → SQLite | migration SQL 在本地执行，无网络边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | 005_intel_tables.sql | accept | 本地单用户工具，仅 BD 团队手动执行，无远程访问风险 |
| T-05-02 | Information Disclosure | mwlab.db 文件权限 | accept | 本地文件，按现有项目惯例管理文件权限 |
| T-05-03 | Denial of Service | 重复执行迁移覆盖数据 | mitigate | 使用 CREATE TABLE IF NOT EXISTS，幂等执行不破坏已有数据 |
</threat_model>

<verification>
执行以下命令验证 Wave 1 完成：

```bash
# 1. 确认两张表存在
sqlite3 "/Volumes/databoard/AI Project/D_dashboard/mwlab.db" ".tables"
# 预期输出包含: customer_prospect  intel_report

# 2. 确认目录结构
ls "/Volumes/databoard/AI Project/D_dashboard/tools/intel/"
ls "/Volumes/databoard/AI Project/D_dashboard/reports/"

# 3. 确认 intel_report CHECK 约束正常工作
sqlite3 "/Volumes/databoard/AI Project/D_dashboard/mwlab.db" \
  "INSERT INTO intel_report (report_type, params_json) VALUES ('industry_research', '{}'); SELECT id FROM intel_report ORDER BY id DESC LIMIT 1;"
# 预期: 返回插入行的 id（整数）
```
</verification>

<success_criteria>
- mwlab.db 新增 intel_report 表，含 report_type CHECK 约束（4 种类型）
- mwlab.db 新增 customer_prospect 表，含外键关联 exhibition_brand
- schema/migrations/005_intel_tables.sql 可幂等重复执行
- tools/intel/ 目录就位（Python 包结构）
- reports/{industry,brand,customer}/ 目录就位
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-01-SUMMARY.md，记录：
- 两张新表的完整字段列表
- 迁移执行方式
- 目录结构清单
</output>
