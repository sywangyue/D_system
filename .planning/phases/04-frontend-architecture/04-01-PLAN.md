---
phase: 04-frontend-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - globals.css
  - .env.local
  - vitest.config.ts
  - tests/middleware.test.ts
  - tests/api/dashboard.test.ts
  - tests/api/tags.test.ts
autonomous: false
requirements: ["UI-POOL"]
user_setup:
  - service: "Cloudflare"
    why: "域名 + Workers 开通"
    dashboard_config:
      - task: "确认 Cloudflare 账号已开通 Workers（免费层即可）"
        location: "dashboard.cloudflare.com -> Workers & Pages"
  - service: "Supabase"
    why: "PG 数据库 + Auth"
    dashboard_config:
      - task: "在 supabase.com 创建免费 PostgreSQL 项目，记录 URL、anon key、service_role_key"
        location: "supabase.com -> New project"

must_haves:
  truths:
    - "Next.js 15 项目可用 `npm run dev` 启动并访问 localhost"
    - "所有依赖包已安装，TypeScript 编译无错误"
    - "Tailwind CSS 4.x 配置了UI-SPEC中定义的颜色Token和间距Token"
    - "测试框架可执行 `npx vitest run` 并报告测试结果"
    - "测试文件（middleware/dashboard/tags）的骨架存在，包含占位测试"
  artifacts:
    - path: "package.json"
      provides: "项目依赖声明"
      contains: "next, @supabase/ssr, @opennextjs/cloudflare, react-leaflet, recharts"
    - path: "globals.css"
      provides: "Tailwind CSS 4.x @theme 配置，含所有颜色/间距/字体Token"
      min_lines: 30
    - path: ".env.local"
      provides: "环境变量模板（SUPABASE_URL, ANON_KEY, SERVICE_ROLE_KEY）"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: "vitest.config.ts"
      provides: "Vitest 测试框架配置"
      contains: "defineConfig"
  key_links:
    - from: "package.json"
      to: "next.config.ts"
      via: "next 包名"
      pattern: "next"
    - from: "globals.css"
      to: "tailwindcss"
      via: "@import 'tailwindcss'"
      pattern: "tailwindcss"

---

<objective>
Next.js 15 项目脚手架 + 全部依赖安装 + 开发工具配置

Purpose: 创建 Phase 4 前端项目的完整基础设施。项目以 Cloudflare Workers 为部署目标（通过 @opennextjs/cloudflare 适配器），使用 Next.js 15 App Router + TypeScript + Tailwind CSS 4.x。同时安装所有 UI 和数据库依赖，配置 Vitest 测试框架并创建测试文件骨架。
Output: 可执行的 Next.js 项目，包含所有依赖、配置、环境变量模板和测试骨架。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-architecture/04-CONTEXT.md
@.planning/phases/04-frontend-architecture/04-RESEARCH.md
@.planning/phases/04-frontend-architecture/04-UI-SPEC.md
@.planning/phases/04-frontend-architecture/04-VALIDATION.md

<interfaces>
N/A — 本 Plan 创建基础设施，不定义业务接口。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Next.js 项目 + 安装全部依赖</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/package.json
    /Volumes/databoard/AI Project/D_dashboard/tsconfig.json
    /Volumes/databoard/AI Project/D_dashboard/next.config.ts
    /Volumes/databoard/AI Project/D_dashboard/.gitignore
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-RESEARCH.md (Standard Stack 章节，第110-160行)
    .planning/phases/04-frontend-architecture/04-RESEARCH.md (Architecture Patterns 章节，第166-238行)
    .planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 5: OpenNext 配置，第377-406行)
  </read_first>

  <action>
    （A）在项目根目录 /Volumes/databoard/AI Project/D_dashboard/ 中创建 Next.js 15 项目。使用手动创建而非 npx create-next-app（避免覆盖现有文件）：

    创建 package.json，包含以下依赖（版本号来自 RESEARCH.md Standard Stack 章节）：
    ```json
    {
      "name": "mwlab-dashboard",
      "version": "0.1.0",
      "private": true,
      "scripts": {
        "dev": "next dev",
        "build": "next build",
        "start": "next start",
        "preview": "opennextjs-cloudflare build && opennextjs-cloudflare preview",
        "deploy": "opennextjs-cloudflare build && opennextjs-cloudflare deploy",
        "test": "vitest run",
        "test:watch": "vitest",
        "lint": "next lint"
      },
      "dependencies": {
        "next": "^16.2.4",
        "react": "^19.2.5",
        "react-dom": "^19.2.5",
        "@supabase/supabase-js": "^2.105.3",
        "@supabase/ssr": "^0.10.2",
        "@opennextjs/cloudflare": "^1.19.6",
        "react-leaflet": "^5.0.0",
        "leaflet": "^1.9.4",
        "react-big-calendar": "^1.19.4",
        "recharts": "^2.x",
        "lucide-react": "^0.x",
        "date-fns": "^3.x"
      },
      "devDependencies": {
        "typescript": "^6.0.3",
        "@types/react": "^19.2.14",
        "@types/react-dom": "^19.x",
        "@types/leaflet": "^1.x",
        "tailwindcss": "^4.2.4",
        "wrangler": "^4.88.0",
        "vitest": "^3.x",
        "@testing-library/react": "^16.x",
        "@vitejs/plugin-react": "^4.x",
        "@types/node": "^22.x"
      }
    }
    ```

    （B）创建 tsconfig.json，标准 Next.js 配置：
    ```json
    {
      "compilerOptions": {
        "target": "ES2017",
        "lib": ["dom", "dom.iterable", "esnext"],
        "allowJs": true,
        "skipLibCheck": true,
        "strict": true,
        "noEmit": true,
        "esModuleInterop": true,
        "module": "esnext",
        "moduleResolution": "bundler",
        "resolveJsonModule": true,
        "isolatedModules": true,
        "jsx": "preserve",
        "incremental": true,
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./*"] }
      },
      "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }
    ```

    （C）创建 next.config.ts（标准 OpenNext Cloudflare 配置）：
    ```typescript
    import type { NextConfig } from 'next'
    const nextConfig: NextConfig = {}
    export default nextConfig
    ```
    注意：**不**使用 `export const runtime = 'edge'` — 见 RESEARCH.md Pitfall 1 和 Anti-Patterns。

    （D）创建 .gitignore（Next.js 标准 + wrangler 产物）：
    ```
    node_modules/
    .next/
    out/
    .open-next/
    *.local
    ```

    （E）安装依赖：
    ```bash
    cd /Volumes/databoard/AI Project/D_dashboard
    npm install
    ```

    验证 package.json 中的依赖版本符合 RESEARCH.md Standard Stack 章节。
  </action>

  <acceptance_criteria>
    1. `package.json` 包含 next ^16.2.4、@supabase/ssr ^0.10.2、@opennextjs/cloudflare ^1.19.6、react-leaflet ^5.0.0、recharts、lucide-react、date-fns
    2. `package.json scripts` 包含 dev、build、preview、deploy、test 命令
    3. `tsconfig.json` 包含 paths: { "@/*": ["./*"] }
    4. `next.config.ts` 文件存在，不含 "export const runtime = 'edge'"
    5. `npm install` 执行成功，node_modules 目录存在
    6. `tsc --noEmit` 执行无错误（或仅 missing module 错误，因为源码尚未创建）
  </acceptance_criteria>

  <verify>
    <automated>ls package.json tsconfig.json next.config.ts && tsc --noEmit --pretty 2>/dev/null; echo "Exit: $?"</automated>
  </verify>

  <done>
    package.json 所有依赖声明完成，npm install 成功，TypeScript 编译通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 配置 Tailwind CSS 4.x + 全局样式 + SVG sprite</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/globals.css
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-UI-SPEC.md (Design System 第22-31行, Color 第73-99行, Spacing Scale 第36-55行, Typography 第60-66行)
  </read_first>

  <action>
    创建 globals.css，使用 Tailwind CSS 4.x 的 CSS-based 配置方式（@theme 指令，非 tailwind.config.js）：

    ```css
    @import "tailwindcss";

    @theme {
      /* ─── Color System ─── */
      --color-surface: #F4F6F8;
      --color-accent: #22C55E;
      --color-accent-dark: #16A34A;
      --color-accent-surface: #DCFCE7;
      --color-purple-surface: #EDE9FE;
      --color-purple-text: #7C3AED;
      --color-destructive: #EF4444;
      --color-trend-neutral: #9CA3AF;
      --color-border: #E5E7EB;
      --color-text-primary: #111827;
      --color-text-secondary: #6B7280;

      /* ─── Spacing (multiples of 4) ─── */
      --spacing-xs: 4px;
      --spacing-sm: 8px;
      --spacing-md: 16px;
      --spacing-lg: 24px;
      --spacing-xl: 32px;
      --spacing-2xl: 48px;
      --spacing-3xl: 64px;
      --spacing-sidebar-width: 220px;
      --spacing-pill-height: 32px;

      /* ─── Typography ─── */
      --font-family-sans: Inter, ui-sans-serif, system-ui, -apple-system, sans-serif;
      --font-size-body: 14px;
      --font-size-label: 12px;
      --font-size-heading: 18px;
      --font-size-display: 36px;
    }

    body {
      font-family: var(--font-family-sans);
      color: var(--color-text-primary);
      background-color: var(--color-surface);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    /* react-big-calendar overrides to match design system */
    .rbc-calendar {
      font-family: var(--font-family-sans);
    }
    .rbc-toolbar button.rbc-active {
      background-color: var(--color-accent-surface) !important;
      border-color: var(--color-accent) !important;
      color: var(--color-accent-dark) !important;
    }
    .rbc-event {
      background-color: var(--color-accent-surface) !important;
      border-left: 3px solid var(--color-accent) !important;
      color: var(--color-accent-dark) !important;
      border-radius: 4px !important;
      font-size: var(--font-size-label) !important;
    }

    /* Leaflet marker cluster fix for Next.js dynamic import */
    .leaflet-container {
      height: 100%;
      width: 100%;
      border-radius: 12px;
    }

    /* Selection colors */
    ::selection {
      background-color: var(--color-accent-surface);
      color: var(--color-accent-dark);
    }
    ```

    所有颜色值和间距值精确来源于 04-UI-SPEC.md 的 Color 和 Spacing Scale 章节。
  </action>

  <acceptance_criteria>
    1. `globals.css` 文件存在于项目根目录
    2. 文件包含 `@import "tailwindcss"` 头部
    3. 文件包含 `@theme` 块，声明了所有 --color-* 和 --spacing-* 自定义属性
    4. --color-accent 值为 #22C55E
    5. --color-surface 值为 #F4F6F8
    6. --font-size-display 值为 36px
    7. --spacing-sidebar-width 值为 220px
    8. 文件包含 .rbc-event 和 .leaflet-container 覆盖样式
  </acceptance_criteria>

  <verify>
    <automated>grep -c '@import "tailwindcss"' /Volumes/databoard/AI\ Project/D_dashboard/globals.css && grep -c '\-\-color-accent: #22C55E' /Volumes/databoard/AI\ Project/D_dashboard/globals.css</automated>
  </verify>

  <done>
    Tailwind CSS 4.x 配置完成，所有 UI-SPEC 中的颜色和间距 Token 可在 Tailwind 类中直接使用。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 .env.local 模板 + Vitest 配置 + 测试文件骨架</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/.env.local
    /Volumes/databoard/AI Project/D_dashboard/vitest.config.ts
    /Volumes/databoard/AI Project/D_dashboard/tests/middleware.test.ts
    /Volumes/databoard/AI Project/D_dashboard/tests/api/dashboard.test.ts
    /Volumes/databoard/AI Project/D_dashboard/tests/api/tags.test.ts
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 1: middleware.ts, 第240-294行)
    .planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 2: server client, 第298-321行)
    .planning/phases/04-frontend-architecture/04-VALIDATION.md (Wave 0 Requirements, 第50-57行)
    .planning/phases/04-frontend-architecture/04-PATTERNS.md (Test patterns, 第252-298行)
  </read_first>

  <action>
    （A）创建 .env.local（模板，用户填入实际值）：
    ```
    # Supabase 项目凭据（从 supabase.com 项目设置中获取）
    NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
    SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

    # Admin 密码（seed-users.ts 使用）
    ADMIN_PASSWORD=your-admin-password-here
    ```

    （B）创建 vitest.config.ts：
    ```typescript
    import { defineConfig } from 'vitest/config'
    import react from '@vitejs/plugin-react'
    import path from 'path'

    export default defineConfig({
      plugins: [react()],
      test: {
        environment: 'node',
        globals: true,
        include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
        exclude: ['node_modules', '.next'],
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        },
      },
    })
    ```

    （C）创建 tests/middleware.test.ts（路由守卫测试骨架，含以下待填充的测试用例）：
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('middleware / route guard', () => {
      it('未登录访问 /dashboard 重定向到 /login', () => {
        // TODO: 在 Plan 04-02 实现 middleware.ts 后填充
        expect(true).toBe(true)
      })

      it('admin 访问 /setting 成功，manager 重定向到 /dashboard', () => {
        // TODO: 在 Plan 04-02 实现 middleware.ts 后填充
        expect(true).toBe(true)
      })
    })
    ```

    （D）创建 tests/api/dashboard.test.ts（Dashboard 聚合查询 API 测试骨架）：
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('GET /api/dashboard', () => {
      it('返回正确聚合数据（industry_l2, relation, mds 三参数过滤）', () => {
        // TODO: 在 Plan 04-04 实现 API Route 后填充
        expect(true).toBe(true)
      })
    })
    ```

    （E）创建 tests/api/tags.test.ts（打标 API 测试骨架）：
    ```typescript
    import { describe, it, expect } from 'vitest'

    describe('PATCH /api/brands/[id]/tags', () => {
      it('200 + 正确响应（合法字段和值）', () => {
        // TODO: 在 Plan 04-04 实现 API Route 后填充
        expect(true).toBe(true)
      })

      it('422 — 非法字段名（不在 TAGGABLE_FIELDS 中）', () => {
        expect(true).toBe(true)
      })

      it('422 — 枚举值越界', () => {
        expect(true).toBe(true)
      })

      it('404 — 品牌不存在', () => {
        expect(true).toBe(true)
      })

      it('写入 manual_tag_history', () => {
        expect(true).toBe(true)
      })
    })
    ```

    注意：所有测试骨架使用 `expect(true).toBe(true)` 占位，确保 `npx vitest run` 通过。实现在后续计划中完成。
  </action>

  <acceptance_criteria>
    1. `.env.local` 包含 NEXT_PUBLIC_SUPABASE_URL、NEXT_PUBLIC_SUPABASE_ANON_KEY、SUPABASE_SERVICE_ROLE_KEY、ADMIN_PASSWORD
    2. `vitest.config.ts` 包含 plugins: [react()]、environment: 'node'、@ 别名
    3. `tests/middleware.test.ts` 包含 2 个 describe 块（未登录守卫、角色守卫）
    4. `tests/api/dashboard.test.ts` 包含 1 个 describe 块
    5. `tests/api/tags.test.ts` 包含 5 个 it 块
    6. `npx vitest run` 执行成功并通过所有占位测试
  </acceptance_criteria>

  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && npx vitest run --reporter=dot 2>&1 | tail -5</automated>
  </verify>

  <done>
    环境变量模板就绪，Vitest 测试框架配置完成，三个测试文件骨架创建并通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.local → git | SERVICE_ROLE_KEY 在 .env.local 中，未提交到 git |
| 本地开发服务器 → 外部 | 开发环境仅 localhost 访问 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Information Disclosure | .env.local | mitigate | .env.local 列在 .gitignore 中（已在 Task 1 配置），防止 SERVICE_ROLE_KEY 提交到仓库。Task 3 在 .env.local 文件头部有警告注释。 |
</threat_model>

<verification>
1. `ls package.json tsconfig.json next.config.ts globals.css .env.local vitest.config.ts` — 6 个文件全部存在
2. `npx vitest run` — 全部 8 个占位测试通过
3. `npm ls next` — 输出 next@16.x.x 表示安装成功
4. `.gitignore` 包含 .env.local 条目（需确认）
</verification>

<success_criteria>
1. Next.js 15 项目结构完整，package.json 声明所有依赖
2. Tailwind CSS 4.x 配置了 UI-SPEC 中定义的所有颜色 Token（12 个）和间距 Token（8 个）
3. Vitest 测试框架可执行，三个测试文件（共 8 个占位用例）全部通过
4. .env.local 模板包含 4 个环境变量占位符
5. 项目可通过 `npm run dev` 启动（不含页面时显示 404 属正常）
</success_criteria>

<output>
完成 Plan 04-01 后，创建 .planning/phases/04-frontend-architecture/04-01-SUMMARY.md
</output>
