---
phase: 04-frontend-architecture
plan: 05
type: execute
wave: 2
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - components/dashboard/LayerTabs.tsx
  - components/dashboard/SubTabs.tsx
  - components/dashboard/KpiCardRow.tsx
  - components/dashboard/TrendChart.tsx
  - components/dashboard/BrandTable.tsx
  - app/dashboard/page.tsx
  - app/dashboard/dashboard-content.tsx
autonomous: true
requirements: [UI-POOL]
must_haves:
  truths:
    - "Dashboard 有 4 个主 Layer 标签可切换：概览 / 分析 / 地理 / 明细"
    - "概览层内部有 4 个子 Tab：总览 / 趋势 / 集团 / 快照"
    - "概览层-总览 Tab 显示 4 个 KPI 卡片（展览面积/展商/观众/集团）+ 年比趋势"
    - "概览层-趋势 Tab 显示 TrendChart（从 API yearTrend 数据源获取）"
    - "分析层内部有 5 个子 Tab：行业分布 / 竞争关系 / MDS 相关 / 热力矩阵 / 标签摘要"
    - "地理层内部有 5 个子 Tab：城市分布 / 场馆列表 / 国内外对比 / 城市排名 / 场馆排名"
    - "明细层内部有 4 个子 Tab：品牌列表 / 届次列表 / 搜索 / 导出"
    - "FilterTabs 过滤仍在 Dashboard 顶部，过滤联动所有 Layer"
  artifacts:
    - path: "components/dashboard/LayerTabs.tsx"
      provides: "4 层主 Tab 切换器组件"
      exports: "LayerTabs"
    - path: "components/dashboard/SubTabs.tsx"
      provides: "每层内部的 4-6 个子 Tab 切换器"
      exports: "SubTabs"
  key_links:
    - from: "dashboard-content.tsx" to: "LayerTabs.tsx" via: "activeLayer state"
    - from: "dashboard-content.tsx" to: "app/api/dashboard" via: "fetch() with filter params"
    - from: "dashboard-content.tsx" to: "TrendChart.tsx" via: "data?.yearTrend prop (B1 修复)"
---

<objective>
将当前单页面/三过滤器的 Dashboard 重构为 4 层架构（D-05），每层 4-6 个子 Tab。保留顶部 FilterTabs 作为全局过滤，新增 LayerTabs 和 SubTabs 导航。同时确保 TrendChart 从 API 获取真实的年比趋势数据（B1 修复）。

Purpose: 实现 D-05（4 层 Dashboard × 4-6 Tab 每层）和 D-06（导航结构支持快速跳转）
Output: 5 个新组件 + 2 个页面文件修改
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (4-Layer Dashboard 组件树 lines 330-366, SQL 查询模式 lines 447-482, 数据流 lines 304-327)
@./app/dashboard/dashboard-content.tsx (当前单页——需要重构为 4 层架构)
@./app/dashboard/page.tsx (Suspense wrapper——保留)
@./components/ui/FilterTabs.tsx (顶部过滤——保留但不移动)
@./components/ui/KpiCard.tsx (已更新 MD 品牌色)
</context>

<interfaces>
从 RESEARCH.md 提取的 4-Layer 组件树：

概览层 (Layer 1) SubTabs: 总览 / 趋势 / 集团 / 快照
分析层 (Layer 2) SubTabs: 行业分布 / 竞争关系 / MDS 相关 / 热力矩阵 / 标签摘要
地理层 (Layer 3) SubTabs: 城市分布 / 场馆列表 / 国内外对比 / 城市排名 / 场馆排名
明细层 (Layer 4) SubTabs: 品牌列表 / 届次列表 / 搜索 / 导出

Dashboard 数据流（B1 修复：API 响应新增 yearTrend）：
```
用户操作 FilterTabs → 更新 URL searchParams → useEffect refetch
  → GET /api/dashboard?industry_l2=X&competition_relation=Y&mds_related=Z
  → { kpis, brands, industryDistribution, yearTrend }
  → KpiCardRow ← data.kpis
  → TrendChart   ← data.yearTrend (B1: 来自 API 的真实年比数据)
  → BrandTable   ← data.brands
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 LayerTabs 和 SubTabs 组件</name>
  <files>components/dashboard/LayerTabs.tsx, components/dashboard/SubTabs.tsx</files>
  <read_first>
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md lines 330-366 (4-Layer 组件树定义、每层 SubTab 名称)
  </read_first>
  <action>
  创建 2 个新组件文件：

  **A) components/dashboard/LayerTabs.tsx** — 4 层主 Tab 切换器
  ```typescript
  "use client"

  import { Layers, BarChart3, MapPin, Table2 } from "lucide-react"

  export type LayerId = "overview" | "analysis" | "geo" | "detail"

  const LAYERS: { id: LayerId; label: string; icon: typeof Layers }[] = [
    { id: "overview", label: "概览", icon: Layers },
    { id: "analysis", label: "分析", icon: BarChart3 },
    { id: "geo", label: "地理", icon: MapPin },
    { id: "detail", label: "明细", icon: Table2 },
  ]

  interface LayerTabsProps {
    activeLayer: LayerId
    onChange: (layer: LayerId) => void
  }

  export default function LayerTabs({ activeLayer, onChange }: LayerTabsProps) {
    return (
      <div className="flex gap-1 bg-surface rounded-lg p-1">
        {LAYERS.map((layer) => {
          const isActive = activeLayer === layer.id
          const Icon = layer.icon
          return (
            <button
              key={layer.id}
              onClick={() => onChange(layer.id)}
              className={`flex items-center gap-2 px-4 py-2 rounded-md text-sm font-medium transition-all duration-150
                ${isActive
                  ? "bg-white text-accent shadow-sm"
                  : "text-text-secondary hover:text-text-primary hover:bg-white/50"
                }`}
            >
              <Icon size={16} />
              {layer.label}
            </button>
          )
        })}
      </div>
    )
  }
  ```

  **B) components/dashboard/SubTabs.tsx** — 每层内部的子 Tab 切换器
  接收 subTabs: {id: string, label: string}[] 数组和 activeSub / onChange 回调
  Pill 风格（圆角胶囊），选中态使用 MD Orange（bg-accent-surface + text-accent-dark）
  4 个 Layer 的 SubTab 列表由父组件 DashboardContent 传入：

  ```typescript
  "use client"

  interface SubTab { id: string; label: string }

  interface SubTabsProps {
    tabs: SubTab[]
    activeTab: string
    onChange: (tabId: string) => void
  }

  export default function SubTabs({ tabs, activeTab, onChange }: SubTabsProps) {
    return (
      <div className="flex flex-wrap gap-2">
        {tabs.map((tab) => (
          <button
            key={tab.id}
            onClick={() => onChange(tab.id)}
            className={`px-3 py-1.5 rounded-full text-sm transition-all duration-100
              ${activeTab === tab.id
                ? "bg-accent-surface text-accent-dark font-semibold border border-accent/30"
                : "bg-white text-text-secondary hover:bg-surface border border-border"
              }`}
          >
            {tab.label}
          </button>
        ))}
      </div>
    )
  }
  ```

  为每个 Layer 定义 subTab 常量：
  ```typescript
  export const OVERVIEW_SUBTABS: SubTab[] = [
    { id: "summary", label: "总览" },
    { id: "trend", label: "趋势" },
    { id: "organizer", label: "集团" },
    { id: "snapshot", label: "快照" },
  ]

  export const ANALYSIS_SUBTABS: SubTab[] = [
    { id: "industry", label: "行业分布" },
    { id: "relation", label: "竞争关系" },
    { id: "mds", label: "MDS 相关" },
    { id: "heat", label: "热力矩阵" },
    { id: "tags", label: "标签摘要" },
  ]

  export const GEO_SUBTABS: SubTab[] = [
    { id: "cities", label: "城市分布" },
    { id: "venues", label: "场馆列表" },
    { id: "compare", label: "国内外对比" },
    { id: "city-rank", label: "城市排名" },
    { id: "venue-rank", label: "场馆排名" },
  ]

  export const DETAIL_SUBTABS: SubTab[] = [
    { id: "brands", label: "品牌列表" },
    { id: "editions", label: "届次列表" },
    { id: "search", label: "搜索" },
    { id: "export", label: "导出" },
  ]
  ```
  </action>
  <verify>
    <automated>
      grep -c "LayerId\|overview\|analysis\|geo\|detail" components/dashboard/LayerTabs.tsx && grep -c "OVERVIEW_SUBTABS\|ANALYSIS_SUBTABS" components/dashboard/SubTabs.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - LayerTabs.tsx 存在且导出 LayerTabs 组件 + LayerId 类型
    - LayerTabs.tsx 包含 4 个 Layer: 概览/分析/地理/明细
    - LayerTabs.tsx 选中态使用 "text-accent"（MD Orange）
    - SubTabs.tsx 存在且导出 SubTabs 组件
    - SubTabs.tsx 导出 OVERVIEW_SUBTABS（4 个 Tab）
    - SubTabs.tsx 导出 ANALYSIS_SUBTABS（5 个 Tab）
    - SubTabs.tsx 导出 GEO_SUBTABS（5 个 Tab）
    - SubTabs.tsx 导出 DETAIL_SUBTABS（4 个 Tab）
    - SubTabs 选中态使用 bg-accent-surface/text-accent-dark（MD Orange）
  </acceptance_criteria>
  <done>
    LayerTabs 和 SubTabs 组件创建完成，支持 4 层 × 4-6 Tab 导航
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 KpiCardRow、TrendChart 和 BrandTable 子组件（TrendChart 从 API yearTrend 数据源获取）</name>
  <files>components/dashboard/KpiCardRow.tsx, components/dashboard/TrendChart.tsx, components/dashboard/BrandTable.tsx</files>
  <read_first>
    @./components/ui/KpiCard.tsx (现有 KPI 卡片——KpiCardRow 将其封装为行)
    @./lib/types.ts (KpiData, Brand 类型)
  </read_first>
  <action>
  创建 3 个组件用于 Dashboard 各 Layer 的内容区域：

  **A) components/dashboard/KpiCardRow.tsx** — 4 张 KPI 卡片行
  ```typescript
  import KpiCard from "@/components/ui/KpiCard"
  import type { KpiData } from "@/lib/types"

  interface KpiCardRowProps {
    data: KpiData | null
    isLoading: boolean
  }

  export default function KpiCardRow({ data, isLoading }: KpiCardRowProps) {
    return (
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <KpiCard label="展览面积" value={data?.total_area ?? null} unit="㎡" variant="highlight" isLoading={isLoading} />
        <KpiCard label="展商数量" value={data?.total_exhibitors ?? null} isLoading={isLoading} />
        <KpiCard label="观众数量" value={data?.total_visitors ?? null} isLoading={isLoading} />
        <KpiCard label="展览集团" value={data?.total_organizers ?? null} isLoading={isLoading} />
      </div>
    )
  }
  ```

  **B) components/dashboard/TrendChart.tsx** — 年比趋势图（B1 修复——接收真实 yearTrend 数据）
  使用 recharts 的 BarChart 展示近 5 年展览面积趋势。
  Props 接口：
  ```typescript
  interface TrendChartProps {
    data: { year: number; area_sqm: number }[]
    isLoading: boolean
  }
  ```
  - **数据来源**: dashboard API 响应中的 yearTrend 字段（04-03 Task 1 新增）
  - isLoading=true 时显示骨架屏（h-[300px] bg-gray-100 rounded-xl animate-pulse）
  - data 为空数组时显示居中文本 "暂无趋势数据"
  - data 有数据时：
    - 使用 recharts BarChart 组件
    - xAxis 展示 year，yAxis 展示 area_sqm（格式化为 "㎡"）
    - Bar 组件 fill="#fe5c00" (MD Orange)
    - 添加响应式容器 ResponsiveContainer
    - 简洁的 tooltip（显示年份和面积数值）
    - 禁用 legend（单系列）

  **C) components/dashboard/BrandTable.tsx** — 品牌列表表格
  接收 brands: Brand[]，展示以下列：
  - 品牌中文名 (name_cn)
  - 所属行业 (industry_l2)
  - 主办方 (organizer)
  - 竞争关系 (competition_relation) — 用 MD 色系胶囊显示
  - MDS 相关性 (mds_related)
  - 操作列（预留，Phase 4 不做打标操作）
  - 行 hover 高亮
  - 如果 brands 为空，显示空状态消息 "当前筛选条件下无展会数据"
  使用 Tailwind 表格样式，无依赖第三方表格库。
  </action>
  <verify>
    <automated>
      test -f components/dashboard/KpiCardRow.tsx && test -f components/dashboard/TrendChart.tsx && test -f components/dashboard/BrandTable.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - KpiCardRow.tsx 存在，导出默认组件，渲染 4 个 KpiCard
    - TrendChart.tsx 存在，导出默认组件，使用 recharts BarChart
    - TrendChart.tsx 填充色使用 "#fe5c00"（MD Orange）
    - TrendChart.tsx 接收 data: {year: number, area_sqm: number}[] 和 isLoading props
    - TrendChart.tsx 包含空状态（"暂无趋势数据"）和加载态（骨架屏）处理
    - BrandTable.tsx 存在，导出默认组件，包含 name_cn 列
    - BrandTable.tsx 包含 competition_relation 胶囊显示
  </acceptance_criteria>
  <done>
    KpiCardRow、TrendChart（含 yearTrend 数据源连接）、BrandTable 3 个 Dashboard 子组件创建完成
  </done>
</task>

<task type="auto">
  <name>Task 3: 重构 dashboard-content.tsx + dashboard/page.tsx——4 层架构（含 yearTrend 数据管道）</name>
  <files>app/dashboard/dashboard-content.tsx, app/dashboard/page.tsx</files>
  <read_first>
    @./app/dashboard/dashboard-content.tsx (当前单页——需要重构)
    @./app/dashboard/page.tsx (Suspense wrapper——保留或小幅修改)
    @./components/dashboard/LayerTabs.tsx (Task 1 创建)
    @./components/dashboard/SubTabs.tsx (Task 1 创建——含 4 组 SubTab 常量)
    @./components/dashboard/KpiCardRow.tsx (Task 2 创建)
    @./components/dashboard/TrendChart.tsx (Task 2 创建——接收 yearTrend data)
    @./components/dashboard/BrandTable.tsx (Task 2 创建)
    @./components/ui/FilterTabs.tsx (顶部过滤保留)
  </read_first>
  <action>
  重构 /Volumes/databoard/AI Project/D_dashboard/app/dashboard/dashboard-content.tsx：

  该文件是 Dashboard 的核心控制器。当前实现是单页式（FilterTabs + KPI cards + PieChart）。重构后：

  **状态管理 (useState)**：
  - activeLayer: LayerId = "overview"（主 Tab 状态）
  - activeSub: string = 当前 Layer 的第一个 SubTab（层内子 Tab）
  - filter state: selectedL1, selectedL2, selectedRelations, selectedMds（保留现有）
  - data: DashboardResponse | null（保留现有——从 /api/dashboard 获取）
  - isLoading / error（保留现有）

  **主要变更**：
  1. 添加 LayerTabs 组件在顶部（在 FilterTabs 之上或之间）
  2. 添加 SubTabs 组件在每个 Layer 内容之上
  3. FilterTabs 保持全局（在所有 Layer 之上共享）
  4. 数据获取逻辑保留（当前 useEffect 模式——API 已由 04-03 重写为 better-sqlite3）
  5. **B1 修复：从 API 响应中提取 yearTrend 并传递给 TrendChart**：
     ```typescript
     // 数据获取后：
     const { kpis, brands, industryDistribution, yearTrend } = data
     // 使用方式：
     // <TrendChart data={data?.yearTrend ?? []} isLoading={isLoading} />
     ```
  6. 根据 activeLayer + activeSub 条件渲染不同内容区域

  **Layer 内容映射**：
  ```
  overview/summary → KpiCardRow(data?.kpis) + IndustryPieChart(data?.industryDistribution)
  overview/trend → TrendChart(data={data?.yearTrend ?? []}, isLoading={isLoading})  // B1: yearTrend from API
  overview/organizer → OrganizerTable（先简单显示，搜索 scope 见 deferred）
  overview/snapshot → 文本摘要快照

  analysis/industry → IndustryPieChart（保留，色板已更新）
  analysis/relation → RelationPieChart（简单复用 IndustryPieChart，数据不同）
  analysis/mds → MDS 品牌网格（简单 List）
  analysis/heat → 空状态（"开发中" 文本）
  analysis/tags → 空状态（"开发中" 文本）

  geo/cities → MapView（已存在，dynamic import）
  geo/venues → 空状态
  geo/compare → 空状态
  geo/city-rank → 空状态
  geo/venue-rank → 空状态

  detail/brands → BrandTable(data?.brands)
  detail/editions → 空状态
  detail/search → 空状态
  detail/export → 空状态
  ```

  **app/dashboard/page.tsx**：保持不变（Suspense wrapper）
  </action>
  <verify>
    <automated>
      grep -c "LayerTabs\|activeLayer\|overview\|analysis\|geo\|detail" app/dashboard/dashboard-content.tsx && grep -c "yearTrend" app/dashboard/dashboard-content.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - dashboard-content.tsx: `grep -c "activeLayer"` >= 1（Layer 切换状态）
    - dashboard-content.tsx: `grep -c "activeSub"` >= 1（SubTab 切换状态）
    - dashboard-content.tsx: `grep -c "LayerTabs"` >= 1
    - dashboard-content.tsx: `grep -c "SubTabs"` >= 1
    - dashboard-content.tsx: `grep -c "overview\|analysis\|geo\|detail"` >= 4
    - dashboard-content.tsx: `grep -c "summary\|trend\|organizer\|snapshot"` >= 4（概览层子 Tab 内容）
    - **dashboard-content.tsx: `grep -c "yearTrend"` >= 1**（B1 修复：年比趋势数据管道）
    - dashboard-content.tsx: `grep -c "brands\|editions\|search\|export"` >= 4（明细层子 Tab 内容）
    - dashboard-content.tsx: `grep -c "supabase"` == 0
    - dashboard/page.tsx: 保留 Suspense wrapper 结构
  </acceptance_criteria>
  <done>
    dashboard-content.tsx 重构为 4 层 × 每个 Layer 的子 Tab 内容区域架构；
    TrendChart 接入 API yearTrend 数据源（B1 修复），不再显示空状态
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
无新信任边界（所有数据通过已建立的 BFF API 获取）
## STRIDE Threat Register
无新增（所有安全控制已在 middleware 和 API 层实现）
</threat_model>

<verification>
1. `npm run dev` 启动后，访问 /dashboard 看到顶部 4 个 Layer Tab
2. 点击 "概览" / "分析" / "地理" / "明细" 切换 Layer
3. 每个 Layer 内部子 Tab 可切换
4. FilterTabs 过滤影响所有 Layer 数据
5. 概览层-总览 Tab 显示 4 个 KPI 卡片 + 行业分布图
6. **概览层-趋势 Tab 显示 TrendChart 柱状图（数据来自 API yearTrend，非空状态）**
</verification>

<success_criteria>
- LayerTabs 4 层导航（概览/分析/地理/明细）正常工作
- 每层 4-6 个子 Tab 切换器正常工作
- FilterTabs 全局过滤联动所有 Layer
- 概览层-总览显示 4 张 KPI 卡片（MD Orange 高亮）+ 行业分布图（MD 色板）
- **概览层-趋势显示 TrendChart（数据来自 API yearTrend 响应）**
- 明细层-品牌列表显示品牌表格
- 所有空状态显示友好的占位提示
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-05-SUMMARY.md
</output>
