---
phase: 05-intel-backend
plan: "06"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - .claude/skills/batch-prospect/SKILL.md
  - tools/intel/insert_prospects.py
autonomous: true
requirements:
  - REQ-11
  - REQ-12
  - REQ-13
  - REQ-14
  - REQ-15
  - REQ-21
  - REQ-22
  - REQ-24
  - REQ-25

must_haves:
  truths:
    - ".claude/skills/batch-prospect/SKILL.md 存在且有有效的 YAML frontmatter"
    - "frontmatter 包含 disable-model-invocation: true"
    - "Skill 明确说明输入模式：用户提供参展商关键词列表（因 DB 无参展商明细）"
    - "Skill 包含 qcc_client.py fuzzy_search 调用步骤"
    - "Skill 包含 customer_prospect 表写入步骤（INSERT SQL 或 Python 脚本调用）"
    - "Skill 包含 export_prospects.py 调用步骤（Excel 导出）"
    - "Skill 说明 API Key 未配置时的降级行为"
  artifacts:
    - path: ".claude/skills/batch-prospect/SKILL.md"
      provides: "批量客户挖掘 Claude Code Skill"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/skills/batch-prospect/SKILL.md"
      to: "tools/intel/qcc_client.py"
      via: "python3 tools/intel/qcc_client.py"
      pattern: "qcc_client\\.py"
    - from: ".claude/skills/batch-prospect/SKILL.md"
      to: "customer_prospect 表"
      via: "INSERT INTO customer_prospect"
      pattern: "customer_prospect"
---

<objective>
创建批量客户挖掘 Claude Code Skill：接受用户提供的参展商关键词列表，调用企查查模糊搜索批量获取企业工商信息，将结果写入 customer_prospect 表并导出 Excel 供 BD 使用。

Purpose: 因 DB 无参展商明细数据（exhibition_edition 只有聚合数字），本 Skill 采用「用户提供关键词 + 企查查搜索」模式（Pitfall 5 解决方案），BD 可直接使用 Excel 结果做客户开发。

Output:
- .claude/skills/batch-prospect/SKILL.md — 完整 Skill 文件
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-RESEARCH.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-02-SUMMARY.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-03-SUMMARY.md

<interfaces>
<!-- qcc_client.py 命令行用法（来自 05-02-PLAN.md） -->
```bash
python3 tools/intel/qcc_client.py "上海机床厂" --page 1 --size 10
# 未配置 API Key 时输出: "[企查查未配置] QCC_APP_KEY / QCC_SECRET_KEY 未配置..."
# 已配置时输出: "[企查查] 找到 N 条结果: ..."
```

<!-- export_prospects.py 用法 -->
```bash
python3 tools/intel/export_prospects.py --report_id <N> -o exports/batch_<date>.xlsx
```

<!-- customer_prospect 写入（需通过 Python 脚本批量插入） -->
```python
# 在 Skill 中通过 Bash 执行 Python 脚本批量写入
python3 - <<'PYEOF'
import sqlite3, json
from pathlib import Path
DB_PATH = Path("mwlab.db")
# INSERT INTO customer_prospect (intel_report_id, brand_id, source_type, company_name, ...)
PYEOF
```

<!-- export_for_tagging.py 导出参考 -->
<!-- 所有 openpyxl + sqlite3 模式见 tools/export_for_tagging.py -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 .claude/skills/batch-prospect/SKILL.md</name>
  <files>.claude/skills/batch-prospect/SKILL.md, tools/intel/insert_prospects.py</files>

  <read_first>
    - .planning/phases/05-intel-backend/05-RESEARCH.md — "Common Pitfalls" Pitfall 5（批量挖掘无参展商明细）、"企查查 API 详情"章节
    - .planning/phases/05-intel-backend/05-CONTEXT.md — D-16, D-17（批量挖掘输入/输出）
    - .planning/phases/05-intel-backend/05-02-SUMMARY.md — qcc_client.py 命令格式
    - .planning/phases/05-intel-backend/05-03-SUMMARY.md — export_prospects.py 用法
  </read_first>

  <action>
创建 .claude/skills/batch-prospect/ 目录并写入 SKILL.md：

```markdown
---
name: batch-prospect
description: 批量客户挖掘。用户提供目标竞品展会的参展商关键词列表（因 DB 无参展商明细，需手工提供），调用企查查 API 进行企业模糊搜索，批量获取工商信息，结果写入 customer_prospect 表并导出 Excel。
argument-hint: "[brand_id（竞品展会）+ 换行分隔的参展商关键词列表]"
disable-model-invocation: true
allowed-tools: Bash Read Write
---

## 批量客户挖掘任务

**输入参数**: $ARGUMENTS

---

## 重要说明：数据来源限制

> mwlab.db 的 exhibition_edition 表只存储参展商**聚合数字**（exhibitors_count），**没有**参展企业名单明细。
> 因此本 Skill 使用「用户提供关键词 + 企查查搜索」模式，而非从 DB 直接获取参展商列表。

---

## 第一步：确认输入参数

请用户按以下格式提供输入（若 $ARGUMENTS 已包含则跳过）：

```
目标竞品展会 brand_id: EXPO-XXXX（可选，用于关联 customer_prospect 记录）
搜索关键词列表（每行一个，来自目标展会参展商名单或行业关键词）：
上海精密机床有限公司
广州机床工具集团
苏州数控设备
...
```

将关键词保存到 /tmp/prospect_keywords.txt（每行一个关键词）。

---

## 第二步：企查查批量搜索

对 /tmp/prospect_keywords.txt 中的每个关键词执行企查查搜索：

```bash
while IFS= read -r keyword; do
  echo "=== 搜索: $keyword ==="
  python3 tools/intel/qcc_client.py "$keyword" --size 5
  echo ""
done < /tmp/prospect_keywords.txt > /tmp/qcc_results.txt
cat /tmp/qcc_results.txt
```

**降级说明**：若 QCC_APP_KEY 未配置，上述命令输出 "[企查查未配置]" 字样。
此时仍可继续执行，customer_prospect 表使用 source_type='manual'，company_name 填入关键词本身，企查查字段留空。

---

## 第三步：将结果写入 customer_prospect 表

调用固定脚本 `tools/intel/insert_prospects.py` 批量写入 DB（在 Skill 执行前需已根据搜索结果填充 prospects 列表）：

> 注意：tools/intel/insert_prospects.py 是固定工具脚本（随代码库提交），在此处根据实际搜索结果修改其 prospects 列表后执行。

```python
#!/usr/bin/env python3
"""批量写入 customer_prospect，由 batch-prospect skill 生成"""
import sqlite3
from datetime import datetime
from pathlib import Path

DB_PATH = Path("mwlab.db")

# ── 从以上搜索结果提取结构化数据（替换以下列表）──
prospects = [
    # {
    #     "brand_id": "EXPO-XXXX",        # 关联竞品展会（可为 None）
    #     "intel_report_id": None,         # 先写 None，下一步创建报告后更新
    #     "source_type": "qcc_search",     # 或 "manual"（API 未配置时）
    #     "company_name": "企业全称",
    #     "qcc_key_no": "企查查KeyNo",      # API 未配置时为 None
    #     "credit_code": "统一社会信用代码",
    #     "oper_name": "法定代表人",
    #     "start_date": "YYYY-MM-DD",
    #     "company_status": "存续",
    #     "reg_no": "注册号",
    #     "address": "注册地址",
    # },
]

conn = sqlite3.connect(str(DB_PATH))
inserted = 0
for p in prospects:
    conn.execute(
        "INSERT INTO customer_prospect "
        "(intel_report_id, brand_id, source_type, company_name, qcc_key_no, "
        " credit_code, oper_name, start_date, company_status, reg_no, address) "
        "VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
        (
            p.get("intel_report_id"),
            p.get("brand_id"),
            p.get("source_type", "qcc_search"),
            p["company_name"],
            p.get("qcc_key_no"),
            p.get("credit_code"),
            p.get("oper_name"),
            p.get("start_date"),
            p.get("company_status"),
            p.get("reg_no"),
            p.get("address"),
        )
    )
    inserted += 1
conn.commit()
print(f"已写入 {inserted} 条 prospect 记录")
conn.close()
```

执行：
```bash
python3 tools/intel/insert_prospects.py
```

---

## 第四步：创建调研报告（记录本次挖掘元数据）

```bash
python3 tools/intel/report_writer.py \
  --type batch_prospect \
  --brand_id "EXPO-XXXX" \
  --content "# 批量客户挖掘报告

**挖掘日期**: $(date +%Y-%m-%d)
**目标展会**: [竞品展会名称 + brand_id]
**搜索关键词数**: [关键词总数]
**企查查命中数**: [实际找到企业数]
**数据来源**: $([ -z \"$QCC_APP_KEY\" ] && echo '手工录入（企查查未配置）' || echo '企查查 API')

## 挖掘摘要

[简述本次挖掘的目标、方法和主要发现]

## 关键词列表

$(cat /tmp/prospect_keywords.txt | head -20)

## 后续行动建议

- BD 团队请下载 Excel 文件进行人工筛选
- 优先跟进企业规模较大、成立时间较长的目标
- 建议通过 /single-prospect 对高价值目标做深度调研
"
```

记录返回的 report_id 值，用于下一步 Excel 导出。

---

## 第五步：导出 Excel 供 BD 使用

```bash
# 使用上一步的 report_id 导出（替换 <report_id> 为实际值）
python3 tools/intel/export_prospects.py \
  --report_id <report_id> \
  -o "exports/batch_prospect_$(date +%Y%m%d).xlsx"
```

文件路径显示在输出中，告知用户文件位置。

---

## 第六步：汇总输出

向用户展示：
1. 本次挖掘的关键词数量和企查查命中数
2. 写入 customer_prospect 的记录数
3. Excel 文件路径
4. intel_report 的 report_id（供历史追踪）
5. 建议后续使用 `/single-prospect` 对高价值目标深度调研

---

## 注意事项

1. **企查查未配置**：source_type 使用 'manual'，company_name 填入原始关键词，工商字段留空
2. **关键词数量**：建议每次不超过 50 个关键词（控制 API 费用和执行时间）
3. **数据质量**：企查查模糊搜索可能返回同名公司，BD 需人工核验最终名单
4. **禁止自动化**：本 Skill 为人工触发，不做定时批量执行
```

关键约束（per D-02, D-09, D-16, D-17, Pitfall 5, REQ-23）：
- 明确说明 DB 无参展商明细，采用关键词输入模式（解决 Pitfall 5）
- API Key 未配置时降级为 source_type='manual'，不阻塞整个流程
- INSERT INTO customer_prospect 使用参数化查询
- 建议每次关键词 ≤ 50 个（防止 API 费用过高）
  </action>

  <verify>
    <automated>grep -c "disable-model-invocation: true" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md" && grep -c "qcc_client.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md" && grep -c "customer_prospect" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md"</automated>
  </verify>

  <acceptance_criteria>
    - 文件存在：.claude/skills/batch-prospect/SKILL.md
    - grep -c "disable-model-invocation: true" SKILL.md 返回 1
    - grep -c "qcc_client.py" SKILL.md 返回 ≥ 1（企查查搜索命令）
    - grep -c "customer_prospect" SKILL.md 返回 ≥ 2（INSERT + export）
    - grep -c "export_prospects.py" SKILL.md 返回 ≥ 1（Excel 导出命令）
    - grep -c "report_writer.py" SKILL.md 返回 ≥ 1（报告沉淀命令）
    - grep -c "exhibitors_count\|无参展商明细\|聚合数字" SKILL.md 返回 ≥ 1（说明 DB 限制）
    - grep -c "manual\|未配置" SKILL.md 返回 ≥ 1（降级模式说明）
  </acceptance_criteria>

  <done>batch-prospect Skill 完整，明确说明 DB 限制和关键词输入模式，企查查未配置时有降级路径，结果写入 customer_prospect 并导出 Excel</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户关键词 → 企查查 API | 用户提供的公司名称发送至外部 API |
| 企查查响应 → SQLite 写入 | API 返回数据写入 customer_prospect 表 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Injection | customer_prospect INSERT | mitigate | insert_prospects.py 使用参数化查询，不拼接 SQL |
| T-05-16 | Information Disclosure | 企查查搜索关键词包含公司名 | accept | 公司名称为公开信息，模糊搜索非敏感操作 |
| T-05-17 | Elevation of Privilege | 企查查 API 费用消耗 | mitigate | Skill 建议每次关键词 ≤ 50 个，BD 人工控制执行频率 |
</threat_model>

<verification>
```bash
ls -la "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md"
grep "disable-model-invocation" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md"
grep "qcc_client.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md"
grep "export_prospects.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/batch-prospect/SKILL.md"
```
</verification>

<success_criteria>
- SKILL.md 存在，frontmatter 格式正确
- 明确说明 DB 无参展商明细，采用关键词输入模式
- 企查查未配置时有降级路径（source_type='manual'）
- 结果写入 customer_prospect 表并导出 Excel
- 末尾调用 report_writer.py 记录调研元数据
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-06-SUMMARY.md，记录：
- Skill 使用方式（输入格式示例）
- 企查查降级模式说明
- customer_prospect 写入的字段映射
</output>
