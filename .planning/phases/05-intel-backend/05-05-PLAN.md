---
phase: 05-intel-backend
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - .claude/skills/brand-research/SKILL.md
autonomous: true
requirements:
  - REQ-07
  - REQ-09
  - REQ-21
  - REQ-22

must_haves:
  truths:
    - ".claude/skills/brand-research/SKILL.md 存在且有有效的 YAML frontmatter"
    - "frontmatter 包含 disable-model-invocation: true"
    - "Skill 包含 !`python3 tools/intel/db_query.py brand-research` 命令"
    - "Skill 报告模板包含历史届次趋势、竞争关系（含 fallback）、MA 价值评估、战略建议四个章节"
    - "Skill 明确说明 exhibition_relation 为空时使用同行业列表作为竞争参考"
    - "Skill 末尾包含 report_writer.py 调用命令"
  artifacts:
    - path: ".claude/skills/brand-research/SKILL.md"
      provides: "品牌展会调研 Claude Code Skill"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/skills/brand-research/SKILL.md"
      to: "tools/intel/db_query.py"
      via: "!`python3 tools/intel/db_query.py brand-research`"
      pattern: "db_query\\.py brand-research"
    - from: ".claude/skills/brand-research/SKILL.md"
      to: "tools/intel/report_writer.py"
      via: "python3 tools/intel/report_writer.py --type brand_research"
      pattern: "report_writer\\.py"
---

<objective>
创建品牌展会调研 Claude Code Skill：深度分析单一展会品牌的历史届次趋势、竞争网络（含空表 fallback）、MA 价值评估和战略建议，输出 BD 谈判支撑报告。

Purpose: BD 团队通过 `/brand-research EXPO-0001` 或 `/brand-research 中国国际机床展览会` 触发完整品牌调研，Skill 优先使用 DB 数据（REQ-06），竞争分析具备双轨 fallback（Pitfall 1）。

Output:
- .claude/skills/brand-research/SKILL.md — 完整 Skill 文件
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-RESEARCH.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-02-SUMMARY.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-03-SUMMARY.md

<interfaces>
<!-- db_query.py brand-research 输出格式（来自 05-02-PLAN.md 实现） -->
```
### 品牌基本信息
- brand_id: EXPO-XXXX
- 中文名: ...
- MA潜力评分: X（满分5）
- 战略相关性: X（满分5）
- 规模评分: X（满分10）

### 历史届次数据（最近10届）
- 2024: 面积=XXm², 展商=XX, 观众=XX, 城市=XX, 趋势=上升

### 同行业展会（exhibition_relation 表当前无数据，fallback 到同行业聚合）
- [展会名] (EXPO-XXXX) 规模=X MA潜力=X
```

<!-- report_writer.py 品牌报告写入命令 -->
```bash
python3 tools/intel/report_writer.py \
  --type brand_research \
  --brand_id "$ARGUMENTS" \
  --content "$(cat /tmp/brand_report.md)"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 .claude/skills/brand-research/SKILL.md</name>
  <files>.claude/skills/brand-research/SKILL.md</files>

  <read_first>
    - .planning/phases/05-intel-backend/05-RESEARCH.md — "Pattern 1" 章节中的 brand-research SKILL.md 示例
    - .planning/phases/05-intel-backend/05-RESEARCH.md — "Common Pitfalls" 章节 Pitfall 1（exhibition_relation 为空的 fallback）
    - .planning/phases/05-intel-backend/05-CONTEXT.md — D-14, D-15（品牌调研输入/输出规范）
    - .planning/phases/05-intel-backend/05-02-SUMMARY.md — brand-research 查询输出格式
  </read_first>

  <action>
创建 .claude/skills/brand-research/ 目录并写入 SKILL.md：

```markdown
---
name: brand-research
description: 对目标展会品牌进行完整调研分析。输入 brand_id（如 EXPO-0001）或品牌中文名，从 mwlab.db 获取完整历史届次和竞争关系数据，结合 WebSearch 调研主办方背景，输出历史趋势分析、竞争格局、MA 价值评估和 BD 战略建议。
argument-hint: "[brand_id 或品牌中文名，例: EXPO-0001 或 中国国际机床展览会]"
disable-model-invocation: true
allowed-tools: Bash WebSearch Read Write
---

## 品牌调研任务

**目标品牌**: $ARGUMENTS

---

## 第一步：从 DB 注入品牌完整数据（禁止跳过，禁止虚构）

> 以下由脚本自动执行，输出内容直接来自 mwlab.db。
> **规则：报告中所有展会历史数据（面积、展商数、观众数、届次）必须来自以下 DB 输出，禁止 LLM 凭记忆填写数字。**

!`python3 tools/intel/db_query.py brand-research "$ARGUMENTS"`

---

## 第二步：WebSearch 补充主办方背景

基于第一步获取的主办方名称，使用 WebSearch 搜索：

1. `"[主办方名称]" 公司背景 股权结构 注册地`
2. `"[主办方名称]" 展览业务 历史`
3. `"[品牌名称]" 展会 主办方 合作 收购`

> 注意：若第一步 DB 数据中主办方字段为空或"未知"，则跳过 WebSearch。

---

## 第三步：分析与报告生成

基于以上 DB 数据和 WebSearch 结果，在 /tmp/brand_report.md 生成完整 Markdown 报告，格式如下：

```markdown
# 品牌调研报告：[展会中文名] ([brand_id])

**调研日期**：[YYYY-MM-DD]  
**数据来源**：mwlab.db（[届次总数]届历史数据）+ WebSearch  
**调研目的**：BD 机会评估

---

## 一、基本信息

| 字段 | 值 |
|------|-----|
| brand_id | [来自 DB] |
| 中文名 | [来自 DB] |
| 英文名 | [来自 DB] |
| 主办方 | [来自 DB] |
| 行业 | [L1 / L2，来自 DB] |
| 举办城市 | [来自 DB] |
| UFI 认证 | [是/否，来自 DB] |
| 国际化 | [是/否，来自 DB] |

---

## 二、历史届次趋势分析

### 2.1 规模趋势（数据来自 DB exhibition_edition 表）

[用文字描述近10届的面积、展商数、观众数变化趋势。如有下降或异常，明确指出。]

| 年份 | 面积(m²) | 展商数 | 观众数 | 城市 | 趋势 |
|------|---------|-------|-------|------|------|
[逐行填入，数据来自 DB，不虚构。如某字段为 N/A，保留 N/A]

### 2.2 增长评估
[基于表格数据，计算或估算 CAGR（若数据不足，说明无法计算），判断展会处于增长/平稳/下降阶段]

---

## 三、竞争关系分析

[重要提示：根据第一步 DB 输出判断数据来源]

**情形 A（DB 输出含"竞争关系网络（来自 exhibition_relation 表）"时）**：
列出竞争/合作/收购目标等关系，逐条分析每个关系的含义。

**情形 B（DB 输出含"fallback 到同行业聚合"时）**：
> 注：exhibition_relation 表当前无数据，以下竞争参考来自同行业展会列表（DB industry_l1 聚合）

[列出同行业展会，按规模评分排序，标注 MA 潜力和战略相关性]

**竞争态势总结**：[2-3句话，描述目标品牌在同行业的竞争位置]

---

## 四、主办方背景

[基于 WebSearch 结果填写。如无 WebSearch 数据，说明"暂未获取主办方背景信息"。]

- 公司全称：
- 注册地：
- 成立时间：
- 主要业务：
- 其他展会资产：
- 重要股东/关联方：

---

## 五、MA 价值评估

### 5.1 DB 评分数据
- MA 潜力评分：[来自 DB，满分5，若 N/A 则说明未评分]
- 战略相关性：[来自 DB，满分5]
- 规模评分：[来自 DB，满分10]
- MDS 关联：[来自 DB mds_related 字段]

### 5.2 综合评估
[基于以上数据，从以下三个维度评估 MA 价值：]
1. **战略契合度**：是否与 MDS 现有业务互补
2. **规模价值**：展会体量是否达到收购门槛
3. **可行性风险**：主办方背景、竞争法规等风险

---

## 六、战略建议

[不超过 500 字。给出具体的 BD 行动建议：是主动接触合作、评估收购、持续观察还是排除。
每条建议说明依据（引用具体数字或 DB 字段）。]

1. **短期行动（0-3个月）**：
2. **中期策略（3-12个月）**：
3. **风险提示**：

---

## 七、数据局限性

- 历史届次数据覆盖范围：[最早年份] - [最新年份]
- 竞争关系表状态：exhibition_relation [有数据/当前为空，使用同行业列表代替]
- 主办方信息来源：WebSearch（可能不完整）
```

---

## 第四步：保存报告到 DB

报告生成后，执行以下命令将报告持久化（D-03 结果沉淀原则）：

```bash
python3 tools/intel/report_writer.py \
  --type brand_research \
  --brand_id "$ARGUMENTS" \
  --content "$(cat /tmp/brand_report.md)"
```

> 注意：若 $ARGUMENTS 是品牌名称而非 brand_id，在第一步 DB 输出中确认实际 brand_id 后，将 --brand_id 替换为实际 brand_id 值。

执行成功后输出 `report_id=<N>`。

---

## 注意事项

1. **禁止虚构历史数据**：届次表格必须逐行来自 DB，不得推测或填充空缺年份
2. **竞争分析双轨**：根据 DB 输出自动判断使用 exhibition_relation 还是同行业 fallback
3. **N/A 保留**：DB 字段缺失时在报告中保留 N/A，不猜测数据
4. **WebSearch 可选**：主办方背景若 WebSearch 无结果，报告中说明"暂无信息"
```

关键约束（per D-07, D-14, D-15, Pitfall 1, REQ-22, REQ-23）：
- `disable-model-invocation: true` 必须存在
- `!` 命令用 `$ARGUMENTS` 传入 brand_id（REQ-22 展会为核心键位）
- 竞争分析必须说明"情形A/情形B"，与 db_query.py 的实际输出匹配
- MA 评估章节直接引用 DB 字段（ma_potential, strategic_relevance）
  </action>

  <verify>
    <automated>grep -c "disable-model-invocation: true" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md" && grep -c "db_query.py brand-research" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md" && grep -c "report_writer.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md"</automated>
  </verify>

  <acceptance_criteria>
    - 文件存在：.claude/skills/brand-research/SKILL.md
    - grep -c "disable-model-invocation: true" SKILL.md 返回 1
    - grep -c "db_query.py brand-research" SKILL.md 返回 1
    - grep -c "report_writer.py" SKILL.md 返回 1
    - grep -c "fallback" SKILL.md 返回 ≥ 1（竞争分析 fallback 说明）
    - grep -c "MA 价值评估\|MA潜力" SKILL.md 返回 ≥ 1
    - grep -c "战略建议" SKILL.md 返回 ≥ 1
    - grep -c "禁止虚构" SKILL.md 返回 ≥ 1
    - SKILL.md 文件大于 3KB（包含完整报告模板）
  </acceptance_criteria>

  <done>brand-research Skill 完整，DB 注入在分析前执行，竞争分析双轨（exhibition_relation 有数据/空表），MA 价值评估引用 DB 字段，末尾持久化入库</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → shell 命令 | 用户输入的 brand_id 或品牌名被嵌入 !命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Injection | SKILL.md !命令中的 $ARGUMENTS | accept | 本地单用户工具，BD 团队输入；db_query.py 内部使用参数化 SQL |
| T-05-14 | Information Disclosure | WebSearch 查询包含主办方公司名 | accept | 主办方背景调研为公开信息需求，不含敏感数据 |
</threat_model>

<verification>
```bash
# 验证 Skill 文件完整性
ls -la "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md"
grep "disable-model-invocation" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md"
grep "db_query.py brand-research" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md"
grep "fallback" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/brand-research/SKILL.md"
```
</verification>

<success_criteria>
- SKILL.md 存在，frontmatter 格式正确
- 报告模板覆盖七个章节（基本信息/届次趋势/竞争分析/主办方/MA评估/战略建议/局限性）
- 竞争分析明确区分"情形A（relation表有数据）"和"情形B（fallback）"
- MA 价值评估章节引用 ma_potential + strategic_relevance 字段
- 末尾调用 report_writer.py 写入 intel_report 表
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-05-SUMMARY.md，记录：
- Skill 调用方式
- 报告模板的7个章节
- 竞争分析双轨逻辑说明
</output>
