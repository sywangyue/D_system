---
phase: 06-code-audit
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - merge_engine.py
  - schema/db.py
  - schema/migrations/006_intel_tables.sql
  - schema/migrations/007_provenance_unique.sql
  - tools/fix_audit_data.py
  - mwlab.db (一次性数据修复，先备份)
autonomous: false
requirements: [AUD-02]
findings: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08, CORE-10, INTEL-12, D-06]
must_haves:
  truths:
    - "库中存在十六进制 brand_id 时 next_brand_id 仍返回数字序列下一个值，不回退 EXPO-0001"
    - "对同一目标库连续运行两次 run_merge，data_source 与 data_provenance 行数不再膨胀（幂等）"
    - "normalize_city('呼和浩特') == '呼和浩特'，四字真城市名不再被截断"
    - "init_db() 新建的库含 002~006 全部迁移列（display_ready / country_cn / city_en / is_international_source / intel 两表）"
    - "生产库 schema_version 登记与真实已应用迁移完全一致（对账回填后 001~006 全登记）"
    - "生产库 data_source 无 'jufair/jufair' 类重复串，无 '浩特'/'木齐' 类截断城市"
  artifacts:
    - path: "merge_engine.py"
      provides: "CORE-01/02/03/05/06/07/08/10 八处修复"
    - path: "schema/migrations/006_intel_tables.sql"
      provides: "intel 迁移重编号（原 005_intel_tables.sql）"
    - path: "schema/migrations/007_provenance_unique.sql"
      provides: "data_provenance 去重 + (brand_id, source_url) 唯一索引"
    - path: "tools/fix_audit_data.py"
      provides: "一次性生产数据清洗（data_source 去重 / 城市修复），支持 --dry-run"
  key_links:
    - from: "schema/db.py init_db()"
      to: "schema/migrations/*.sql"
      via: "按编号顺序全量应用 + schema_version（生产既有 INTEGER 主键形状）防重"
      pattern: "migrations"
    - from: "merge_engine.py insert_provenance"
      to: "data_provenance 唯一索引"
      via: "INSERT OR IGNORE 按 (brand_id, source_url)"
---

<objective>
Wave 2 · 数据完整性。修复合并引擎三个 BLOCKER（雷区 3）及其连带的非幂等问题，让 merge_engine 可以安全地再次运行；统一迁移链（重编号 + init_db 全量应用 + 生产库 schema_version 对账回填）；备份后清洗生产库存量脏数据。

Purpose: Phase 1b（Jufair 全集采集）解封后必须跑全量合并——当前代码再跑一次会把所有新建品牌坍缩覆盖到 EXPO-0001。本计划是 Phase 1b 的硬前置。
Output: 幂等且与生产库数据状态兼容的 merge_engine + 完整迁移链 + 清洗后的生产库。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-audit/AUDIT.md
@.planning/phases/06-code-audit/REVIEW-core.md
@CLAUDE.md

硬约束：
- **本计划全程禁止对生产库 mwlab.db 运行 merge_engine.py**（CORE-01/02/03 修复验证一律用临时库/内存库）。
- 生产库数据修复（Task 8-10）必须先备份、先 dry-run、再经 checkpoint 确认；**007 迁移含破坏性 DELETE，对生产库的应用同样必须在 checkpoint（Task 9）之后（Task 10）执行**。
- depends_on 06-01：mwlab.db 必须已脱离 git 跟踪后才做库内修改与备份，避免备份/修改进入版本库。

已核实的生产库迁移状态（Task 7 的前提事实，不得假设其它状态）：
- schema_version 表形状为 `(version INTEGER PRIMARY KEY, description TEXT NOT NULL, applied_at TEXT)`，仅登记 1、4、5 三行；
- 002/003/005_intel 实际已应用但未登记（002 的 4 列已存在于生产库，count=4；intel 两表 Phase 5 已建）；
- 002/003 为 `ALTER TABLE ADD COLUMN`（SQLite 无 IF NOT EXISTS for columns，非幂等），对生产库重放会抛 `duplicate column name`——任何"未登记即重放"的简单守卫都会在此爆炸。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 生产库备份</name>
  <files>backups/ (新建目录，已被 *.db 规则忽略)</files>
  <action>
创建 `backups/` 目录（确认被 .gitignore 覆盖：`*.db` 规则已忽略库文件，另在 .gitignore 加 `backups/` 一行兜底）。执行 `sqlite3 mwlab.db ".backup backups/mwlab_pre_wave2_$(date +%Y%m%d_%H%M%S).db"`（用 .backup 而非 cp，避免写入中拷贝损坏）。记录备份文件路径与 size 到任务输出。本任务是后续一切库内修改（Task 7 对账回填与迁移登记、Task 10 清洗）的前置。
  </action>
  <verify>
    <automated>ls backups/mwlab_pre_wave2_*.db && sqlite3 backups/mwlab_pre_wave2_*.db "PRAGMA integrity_check" 输出 ok</automated>
  </verify>
  <done>备份存在且 integrity_check 通过；备份不在 git 跟踪中</done>
  <commit>chore(db): add backups/ ignore rule (production backup created)</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: next_brand_id 兼容十六进制 ID [CORE-01]</name>
  <files>merge_engine.py</files>
  <behavior>
    - 库含 EXPO-D92BC0D6 与 EXPO-5935 时，next_brand_id 返回 EXPO-5936
    - 空库返回 EXPO-0001
    - 仅有十六进制 ID 的库返回 EXPO-0001（数字子集为空）
  </behavior>
  <action>
修复 merge_engine.py:150-159：弃用"ORDER BY brand_id DESC LIMIT 1 + 正则"方案，改为 `SELECT MAX(CAST(SUBSTR(brand_id,6) AS INTEGER)) FROM exhibition_brand WHERE brand_id GLOB 'EXPO-[0-9]*'`——仅在纯数字 ID 子集内求最大值，十六进制 ID 不参与也不干扰。注意 GLOB 'EXPO-[0-9]*' 仍会匹配 EXPO-1A2B（首字符为数字），需加 `AND brand_id NOT GLOB 'EXPO-*[^0-9]*'` 排除含非数字字符的 ID。保持现有 EXPO-%04d 格式输出。单进程串行运行场景下不引入额外锁机制（最小代码）。
  </action>
  <verify>
    <automated>python3 -c "
import sqlite3, merge_engine
c = sqlite3.connect(':memory:')
c.executescript(open('schema/init_db.sql').read())
c.execute(\"INSERT INTO exhibition_brand(brand_id,name_cn) VALUES('EXPO-D92BC0D6','x'),('EXPO-5935','y')\")
assert merge_engine.next_brand_id(c) == 'EXPO-5936', merge_engine.next_brand_id(c)
print('ok')"（按 next_brand_id 实际签名与 NOT NULL 列调整 INSERT；期望输出 ok）</automated>
  </verify>
  <done>三个 behavior 用例全部通过；现有 pytest test_next_brand_id_* 仍绿</done>
  <commit>fix(merge): next_brand_id ignores hex brand_ids via numeric-subset MAX [CORE-01]</commit>
</task>

<task type="auto">
  <name>Task 3: data_source 幂等覆盖 + 溯源唯一键与跨源 payload 修复 [CORE-02][CORE-05]</name>
  <files>merge_engine.py, schema/migrations/007_provenance_unique.sql</files>
  <action>
1. [CORE-02] merge_engine.py:341：`ON CONFLICT(edition_id) DO UPDATE SET data_source = excluded.data_source || '/' || data_source` 改为在 Python 侧合并——读取现有 data_source，与新来源做集合并集（保持出现顺序，'/'.join(dict.fromkeys(...))），UPDATE 写入去重后的值。重复运行不再追加。
2. [CORE-05a] 新建 `schema/migrations/007_provenance_unique.sql`：先 DELETE 重复行（按 (brand_id, source_url) 分组保留 MIN(rowid)），再 `CREATE UNIQUE INDEX IF NOT EXISTS idx_provenance_brand_url ON data_provenance(brand_id, source_url)`。新库执行时 DELETE 为无害空操作。**注意：本任务只创建迁移文件；007 对生产库的应用必须等到 Task 9 checkpoint 批准后在 Task 10 执行（含破坏性 DELETE）。**
3. [CORE-05a] merge_engine.py:355-382 insert_provenance：改为 `INSERT OR IGNORE`，幂等性由唯一索引保证（uuid 主键保留但不再是幂等依据）。
4. [CORE-05b] merge_engine.py:481-488 双源合并分支：按 url 判定来源后，jufair url 传 jufair 原始行、cnexpo url 传 cnexpo 原始行作为 raw_payload，不再恒传 jufair 行。
5. research.py:166-167 的 dict.fromkeys 去重补丁是掩盖 CORE-02 的死代码——按"外科手术"规则仅在 SUMMARY 中提及，不动 research.py（其去重在修复后变为无害幂等操作）。
  </action>
  <verify>
    <automated>python3 一次性脚本：内存目标库上对同一源数据连续 run_merge 两次，断言第二次后 SELECT COUNT(*) FROM data_provenance 与第一次相同，且所有 data_source 值不含重复段（对每行 assert len(parts)==len(set(parts))）</automated>
  </verify>
  <done>重复运行 merge 不增加 provenance 行数、不膨胀 data_source；cnexpo 溯源行的 raw_payload 来自 cnexpo 原始数据</done>
  <commit>fix(merge): idempotent data_source and provenance, correct cross-source raw payload [CORE-02][CORE-05]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 4: normalize_city 四字城市修复 + parse_date_pair 跨年/同月区间 [CORE-03][CORE-08]</name>
  <files>merge_engine.py</files>
  <behavior>
    - normalize_city: 呼和浩特→呼和浩特、乌鲁木齐→乌鲁木齐、齐齐哈尔→齐齐哈尔、西双版纳→西双版纳（不截断）
    - normalize_city: 湖北武汉→武汉、上海上海→上海（真"省+市"仍正确剥离）
    - parse_date_pair('2026.12.30 - 01.02') → ('2026-12-30', '2027-01-02')（结束月小于开始月时年份+1）
    - parse_date_pair('2026.05.01-05') → ('2026-05-01', '2026-05-05')（同月仅日范围）
  </behavior>
  <action>
1. [CORE-03] merge_engine.py:131-146：删除 `if len(s)==4: return s[2:]` 盲截断，改为仅当前两字命中 `geo_dict.CN_PROVINCES` 的省份简称白名单时才剥离前缀（import tools/geo_dict 或复制省份简称集合——优先 import，避免双源）。
2. [CORE-08] merge_engine.py:99-110：区间分支当 `int(em) < int(sm)` 时结束年份用 `y+1`；新增"同月仅日范围"格式分支（`YYYY.MM.DD-DD`）解析为同年月的起止日。
  </action>
  <verify>
    <automated>python3 -c "
from merge_engine import normalize_city, parse_date_pair
assert normalize_city('呼和浩特')=='呼和浩特'
assert normalize_city('乌鲁木齐')=='乌鲁木齐'
assert normalize_city('湖北武汉')=='武汉'
assert parse_date_pair('2026.12.30 - 01.02')==('2026-12-30','2027-01-02')
assert parse_date_pair('2026.05.01-05')==('2026-05-01','2026-05-05')
print('ok')"（按函数真实空格/格式微调入参；期望 ok）</automated>
  </verify>
  <done>全部 behavior 通过；现有 TestNormalizeCity / 日期测试仍绿</done>
  <commit>fix(merge): stop truncating 4-char city names, handle cross-year and same-month date ranges [CORE-03][CORE-08]</commit>
</task>

<task type="auto">
  <name>Task 5: match_brand 歧义保护 + edition_id 缺年回退 [CORE-06][CORE-07]</name>
  <files>merge_engine.py</files>
  <action>
1. [CORE-06] merge_engine.py:165-189：模糊匹配分支加双重门限——记录 best 与 second_best ratio，仅当 `best >= 0.90 且 (best - second_best) >= 0.05`（或仅一个候选时 best >= 0.90）才返回匹配；否则按新品牌处理，并把低置信候选（0.85≤ratio<0.90 或差距不足）print 一行 `[LOW-CONF] name | best_id | ratio` 供人工复核。MATCH_THRESHOLD 常量同步调整，不引入行业/城市联合判别（最小改动，审计给出的备选方案）。
2. [CORE-07] merge_engine.py:321-322：year 缺失时不再用 0——`suffix = year if year else (norm.get('date_start') or 'undated')`，edition_id = f"{brand_id}-{suffix}"，使同品牌不同 date_start 的无年届次不再坍缩；date_start 也缺失时保留 'undated'（与现状等价但显式可见）。
  </action>
  <verify>
    <automated>python3 一次性脚本：构造两条 name 相近（ratio≈0.86）的品牌断言不合并；构造同 brand 两条 year=None 但 date_start 不同的届次，断言入库后 edition 行数为 2</automated>
  </verify>
  <done>0.85-0.90 区间的模糊匹配不再自动合并且有 LOW-CONF 日志；缺年届次按 date_start 区分不坍缩</done>
  <commit>fix(merge): add ambiguity guard to fuzzy match, fallback edition_id to date_start when year missing [CORE-06][CORE-07]</commit>
</task>

<task type="auto">
  <name>Task 6: dry-run 连接真实目标库（只读不提交）[CORE-10]</name>
  <files>merge_engine.py</files>
  <action>
修复 merge_engine.py:420：dry_run 分支改为连接真实 target_db 并在结束时 `conn.rollback()` 而非 commit（全程同一连接、不落盘）。确保 dry-run 路径下任何 commit 调用被跳过（grep merge 流程中的 conn.commit() 调用点，统一经 `if not dry_run: conn.commit()` 守卫）。这样 match_brand/next_brand_id 基于真实数据统计，预演结果可信。
  </action>
  <verify>
    <automated>对临时拷贝库执行 python3 merge_engine.py --dry-run ...，断言：① 统计输出中"匹配既有品牌"数 > 0（不再全部判新建）；② 运行前后该库文件 md5 不变</automated>
  </verify>
  <done>dry-run 统计反映真实目标库匹配情况且零写入</done>
  <commit>fix(merge): dry-run reads real target db with rollback, restoring preview value [CORE-10]</commit>
</task>

<task type="auto">
  <name>Task 7: migration 重编号 005_intel→006 + init_db 全量应用迁移 + 生产库 schema_version 对账回填 [INTEL-12][CORE-04][D-06]</name>
  <files>schema/migrations/006_intel_tables.sql, schema/db.py, mwlab.db (schema_version 对账回填 + 未应用迁移登记)</files>
  <action>
1. [INTEL-12][D-06] `git mv schema/migrations/005_intel_tables.sql schema/migrations/006_intel_tables.sql`，消除与 005_people.sql 的编号冲突。grep 全仓库 "005_intel" 引用并同步修改。
2. [CORE-04] schema/db.py:33-35 init_db() 重写为：执行 init_db.sql 后，`sorted(glob('schema/migrations/*.sql'))` 按文件名顺序遍历，借助 schema_version 表跳过已登记项，逐个执行并登记。**schema_version 表结构必须沿用生产库既有形状 `(version INTEGER PRIMARY KEY, description TEXT NOT NULL, applied_at TEXT)`——禁止引入新形状**（生产库已有该表，CREATE TABLE IF NOT EXISTS 无法调和形状差异，且 INSERT 缺 description 会违反 NOT NULL）。迁移文件名与整数 version 的映射固定为文件名前缀数字：001_initial→1、002_display_ready→2、003_user_prefs→3、004_*→4、005_people→5、006_intel_tables→6、007_provenance_unique→7。登记行写 `(version=前缀整数, description=迁移文件名, applied_at=now)`。删除内嵌的 _MIGRATION_001 常量改为读文件（其内容与 001_initial.sql 一致时）——若不一致，停下来报告差异再决定，不擅自二选一。
3. 【前置：生产库 schema_version 对账回填】对生产库跑任何迁移循环**之前**，必须先把登记状态与真实已应用状态对齐（见 context 中已核实事实：仅登记 1/4/5，但 002/003/005_intel 实际已应用；002/003 是非幂等 ALTER TABLE，重放即 `duplicate column name`）。对账步骤：
   - 用 `PRAGMA table_info(...)` 探测 002（display_ready 等 4 列，已核实存在 count=4）与 003（user_prefs 迁移涉及的列/表）的实际应用状态；
   - 用 `SELECT name FROM sqlite_master WHERE type='table'` 探测 006（原 005_intel：intel_report / customer_prospect，Phase 5 已建）；
   - 每项探测命中即按第 2 步的整数映射 INSERT 登记行（version=2/3/6，description=对应迁移文件名，applied_at=now，可注明 "backfilled"），未命中则留待迁移循环正常执行；
   - 对账后断言：生产库 schema_version 应含 1~6 全部整数版本。
4. 对账完成后，对生产库运行 init_db 的迁移循环：**仅以回填后的 schema_version 登记判定跳过/执行，绝不依赖 DDL 自身幂等性**。**007 排除在本任务的生产库应用之外**——其含对 data_provenance 的破坏性 DELETE 去重，必须等 Task 9 checkpoint 批准后在 Task 10 应用（007 在新建库/内存库回放路径中正常参与，DELETE 对新库为空操作）。
  </action>
  <verify>
    <automated>python3 -c "
from schema.db import init_db
c = init_db(':memory:')
cols = [r[1] for r in c.execute('PRAGMA table_info(exhibition_brand)')]
assert 'country_cn' in cols and 'city_en' in cols and 'is_international_source' in cols
tables = [r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")]
assert 'intel_report' in tables and 'customer_prospect' in tables
rows = c.execute('SELECT version, description FROM schema_version').fetchall()
assert all(isinstance(r[0], int) and r[1] for r in rows), rows
print('ok')"；sqlite3 mwlab.db "SELECT version FROM schema_version ORDER BY version" 输出含 1~6（不含 7）；ls schema/migrations/ 无重复编号</automated>
  </verify>
  <done>新建库与生产库 schema 一致；迁移编号唯一且回放顺序确定；生产库 schema_version 与真实已应用状态对账一致（1~6 全登记，形状未变）；对生产库未重放 002/003（无 duplicate column 报错）；007 未对生产库应用（留待 Task 10）；export_monthly.py / extract_geo.py 在 init_db 新建库上不再 no such column</done>
  <commit>fix(schema): renumber intel migration to 006, init_db applies all migrations, reconcile production schema_version [CORE-04][INTEL-12][D-06]</commit>
</task>

<task type="auto">
  <name>Task 8: 编写一次性生产数据清洗脚本（先 dry-run）</name>
  <files>tools/fix_audit_data.py</files>
  <action>
新建 `tools/fix_audit_data.py`（一次性脚本，不做抽象封装），三个修复函数 + `--db`（默认 mwlab.db）+ `--dry-run`（默认开启，需显式 `--apply` 才写库）：
1. fix_data_source [CORE-02 存量]：对 exhibition_edition.data_source 含 '/' 的行执行 `'/'.join(dict.fromkeys(s.split('/')))`，输出修复行数（预期 ≈3086+1247+37+ 其它组合）。
2. fix_truncated_cities [CORE-03 存量]：从 geo_dict.CN_CITIES 构建映射 {四字城市[2:] : 四字城市}（如 浩特→呼和浩特）；映射值唯一才修复，歧义截断（同一两字对应多个四字城市）只列出报告不自动改。对 exhibition_edition.city（及 brand 表若有 city 字段）命中映射键且自身不是合法城市的值执行替换，输出明细。
3. report_provenance [CORE-05 存量]：报告 data_provenance 中 (brand_id, source_url) 重复组数及每组样例行——即 007 迁移的 DELETE 将删除的行预览（按"保留 MIN(rowid)"规则标注每组哪些行将删、哪一行保留，输出总将删行数 + 前 10 组样例）。**007 对生产库的应用已推迟到 Task 10，本报告是 Task 9 checkpoint 审阅 DELETE 的依据。**
dry-run 输出统计与样例（每类前 10 行 before→after），不写库。
  </action>
  <verify>
    <automated>python3 tools/fix_audit_data.py --db mwlab.db --dry-run 正常退出且输出三类统计（含 007 DELETE 预览的组数/将删行数/样例）；运行前后 mwlab.db md5 不变</automated>
  </verify>
  <done>dry-run 报告生成，含将修复的精确行数与样例、007 将删行预览，供 checkpoint 审阅</done>
  <commit>feat(tools): one-off audit data cleanup script with dry-run [CORE-02][CORE-03]</commit>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9: CHECKPOINT — 审阅 dry-run 报告（含 007 DELETE 预览），确认对生产库执行清洗与 007 迁移</name>
  <what-built>Task 8 的清洗脚本已对生产库完成 dry-run，输出了 data_source 去重与城市名修复的精确行数和 before→after 样例，以及 007 迁移将对 data_provenance 执行的 DELETE 预览（重复组数、将删行数、保留/将删样例）；007 尚未对生产库应用；Task 1 已完成备份。</what-built>
  <how-to-verify>
1. 查看 dry-run 输出：data_source 修复行数应与审计实测量级吻合（jufair/jufair/jufair 3,086 条、cnexpo×4 1,247 条等）；
2. 抽查城市修复样例：浩特→呼和浩特、木齐→乌鲁木齐 应在列；不应出现把合法两字城市改掉的行；
3. 审阅 007 DELETE 预览：重复组数与将删行数是否合理；抽查样例确认"按 (brand_id, source_url) 保留 MIN(rowid)"不会误删唯一溯源行；
4. 确认备份文件 backups/mwlab_pre_wave2_*.db 存在。
确认后将执行 python3 tools/fix_audit_data.py --apply 实际写库，并对生产库应用 007 迁移（DELETE 去重 + 唯一索引）——均不可逆，但有备份。
  </how-to-verify>
  <resume-signal>回复 approved 执行清洗与 007 迁移，或指出样例中的问题</resume-signal>
</task>

<task type="auto">
  <name>Task 10: 执行清洗 + 应用 007 迁移 + 运行核心测试套件</name>
  <files>mwlab.db (数据修复 + 007 迁移应用)</files>
  <action>
1. `python3 tools/fix_audit_data.py --db mwlab.db --apply`，记录实际修复行数。
2. 对生产库应用 007_provenance_unique.sql（走 Task 7 建立的 schema_version 守卫路径，执行后登记 version=7）。应用后校验：`SELECT COUNT(*) FROM (SELECT 1 FROM data_provenance GROUP BY brand_id, source_url HAVING COUNT(*)>1)` 为 0；唯一索引 idx_provenance_brand_url 存在；实际删除行数与 Task 9 审阅的预览一致（偏差则停下报告）。
3. 清洗后校验：`SELECT COUNT(*) FROM exhibition_edition WHERE data_source LIKE '%jufair%jufair%'` 应为 0；`SELECT COUNT(*) FROM exhibition_edition WHERE city IN ('浩特','木齐','哈尔','版纳')` 应为 0；`PRAGMA integrity_check` ok；exhibition_edition 总行数与清洗前一致（只改值不删行；data_provenance 行数减少量 = 007 预览的将删行数）。
4. 运行 `pytest tests/test_merge_engine.py tests/test_schema.py -x`，全绿。
  </action>
  <verify>
    <automated>pytest tests/test_merge_engine.py tests/test_schema.py -x 退出码 0；上述两条 COUNT 查询均输出 0；data_provenance 重复组计数为 0 且 sqlite3 mwlab.db "SELECT version FROM schema_version WHERE version=7" 输出 7</automated>
  </verify>
  <done>生产库存量污染清零、行数符合预期、完整性通过；007 已应用且删除量与预览一致；核心测试全绿；merge_engine 解除"禁止运行"状态（在 SUMMARY 中显式声明）</done>
  <commit>fix(data): dedupe data_source, repair truncated cities, apply provenance unique migration [CORE-02][CORE-03][CORE-05]</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 清洗脚本 → 生产库 | 破坏性 UPDATE，经备份 + dry-run + checkpoint 三重防护 |
| 007 迁移 → 生产库 | 破坏性 DELETE 去重，经备份 + DELETE 预览 + checkpoint 后才应用 |
| 爬虫源数据 → merge_engine | 不可信外部数据进入合并逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | merge_engine 重跑覆盖生产数据 | mitigate | CORE-01/02/05 幂等化（Task 2/3） |
| T-06-08 | Denial of Service | 清洗脚本/007 DELETE 误改生产数据 | mitigate | .backup 快照 + 默认 dry-run + DELETE 预览 + checkpoint（Task 1/8/9/10） |
| T-06-09 | Tampering | 模糊匹配误并不同品牌 | mitigate | 双门限 + LOW-CONF 日志（Task 5） |
| T-06-10 | Denial of Service | 对生产库重放非幂等 ALTER TABLE | mitigate | schema_version 对账回填 + 仅按登记判定（Task 7） |
</threat_model>

<verification>
- 全部 behavior 断言通过（hex ID、双跑幂等、四字城市、跨年日期）
- init_db 新建库 schema 与生产一致，迁移编号无冲突；生产库 schema_version 对账后 1~7 可追溯
- 生产库清洗后污染计数为 0、行数符合预期、integrity ok
- pytest 核心套件全绿
</verification>

<success_criteria>
CORE-01~08/10、INTEL-12、D-06 共 11 项关闭；merge_engine.py 解除运行禁令，Phase 1b 全量合并不再有数据坍缩风险。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-code-audit/06-02-SUMMARY.md`，须包含：备份路径、schema_version 对账回填明细、清洗实际行数、007 实际删除行数、merge_engine 解禁声明。
</output>
