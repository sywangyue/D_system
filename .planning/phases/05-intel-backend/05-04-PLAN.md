---
phase: 05-intel-backend
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - .claude/skills/industry-research/SKILL.md
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
  - REQ-04
  - REQ-21
  - REQ-22

must_haves:
  truths:
    - ".claude/skills/industry-research/SKILL.md 存在且有有效的 YAML frontmatter"
    - "frontmatter 包含 disable-model-invocation: true（人工触发原则 D-02）"
    - "Skill 包含 !`python3 tools/intel/db_query.py industry-research` 命令（DB 优先注入）"
    - "Skill 提示词要求输出行业展会地图、竞争格局、TAM/SAM/SOM 估算、Porter 五力分析"
    - "Skill 末尾包含 python3 tools/intel/report_writer.py 调用命令（结果沉淀 D-03）"
    - "Skill 明确说明不允许 LLM 虚构展会数据（REQ-23）"
  artifacts:
    - path: ".claude/skills/industry-research/SKILL.md"
      provides: "行业调研 Claude Code Skill"
      contains: "disable-model-invocation: true"
  key_links:
    - from: ".claude/skills/industry-research/SKILL.md"
      to: "tools/intel/db_query.py"
      via: "!`python3 tools/intel/db_query.py industry-research`"
      pattern: "db_query\\.py industry-research"
    - from: ".claude/skills/industry-research/SKILL.md"
      to: "tools/intel/report_writer.py"
      via: "python3 tools/intel/report_writer.py --type industry_research"
      pattern: "report_writer\\.py"
---

<objective>
创建行业调研 Claude Code Skill：从 DB 获取行业展会地图，结合 WebSearch 分析市场趋势，输出 TAM/SAM/SOM 估算 + Porter 五力分析 + 竞争格局报告，并持久化入库。

Purpose: BD 团队通过 `/industry-research 机械和设备` 触发，Skill 确保所有展会数字来自 DB（禁止虚构），LLM 负责框架分析。

Output:
- .claude/skills/industry-research/SKILL.md — 完整 Skill 文件
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-RESEARCH.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-02-SUMMARY.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-03-SUMMARY.md

<interfaces>
<!-- db_query.py 行业查询命令格式 -->
```bash
python3 tools/intel/db_query.py industry-research "$ARGUMENTS"
python3 tools/intel/db_query.py industry-research "$ARGUMENTS" --l2 "子类"
# 输出: 行业概览（品牌数量、城市分布）+ 展会品牌列表（规模排序）
```

<!-- report_writer.py 写入命令格式 -->
```bash
python3 tools/intel/report_writer.py \
  --type industry_research \
  --industry_l1 "$ARGUMENTS" \
  --content "$(cat /tmp/industry_report.md)"
```

<!-- SKILL.md 规范的 frontmatter 字段 -->
```yaml
name: display-name
description: 使用场景描述
argument-hint: "[industry_l1 或 industry_l2]"
disable-model-invocation: true
allowed-tools: Bash WebSearch Read Write
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 .claude/skills/industry-research/SKILL.md</name>
  <files>.claude/skills/industry-research/SKILL.md</files>

  <read_first>
    - .planning/phases/05-intel-backend/05-RESEARCH.md — "Pattern 1: Claude Code Skill 结构（DB 优先注入模式）"章节、"Claude Code Skill 规范"章节
    - .planning/phases/05-intel-backend/05-CONTEXT.md — D-11, D-12, D-13（行业调研输入/输出/方法论）
    - .planning/phases/05-intel-backend/05-02-SUMMARY.md — db_query.py 支持的命令格式
  </read_first>

  <action>
创建 .claude/skills/industry-research/ 目录并写入 SKILL.md。

文件内容如下（完整内容，不要简化或省略任何段落）：

```markdown
---
name: industry-research
description: 对目标行业进行展会竞争格局调研。输入行业标签（如「机械和设备」），从 mwlab.db 获取该行业完整展会地图，结合 WebSearch 分析市场趋势，输出 TAM/SAM/SOM 估算、Porter 五力分析和切入点建议。
argument-hint: "[industry_l1 或 industry_l1/industry_l2，例: 机械和设备 或 机械和设备/机床]"
disable-model-invocation: true
allowed-tools: Bash WebSearch Read Write
---

## 行业调研任务

**目标行业**: $ARGUMENTS

---

## 第一步：从 DB 获取行业展会地图（禁止跳过，禁止虚构数据）

> 以下数据由脚本自动注入，直接来自 mwlab.db，是本次调研的唯一可信基础数据来源。
> **规则：报告中所有展会名称、数量、规模数字必须来自以下 DB 数据，禁止 LLM 虚构。**

!`python3 tools/intel/db_query.py industry-research "$ARGUMENTS"`

---

## 第二步：WebSearch 补充市场趋势

使用 WebSearch 工具搜索以下内容（每项搜索1-2次，获取近2年数据）：

1. `"$ARGUMENTS" 展会市场规模 2024 2025`
2. `"$ARGUMENTS" 行业发展趋势 中国 展览会`
3. `UFI 统计 "$ARGUMENTS" 行业 exhibition market size`

> 重要：WebSearch 结果仅用于宏观趋势和 TAM 数据补充，不用于覆盖 DB 中的具体展会数据。

---

## 第三步：分析与报告生成

基于以上 DB 数据和 WebSearch 结果，在 /tmp/industry_report.md 生成完整 Markdown 报告，格式如下：

```markdown
# 行业调研报告：[行业名称]

**调研日期**：[YYYY-MM-DD]  
**数据来源**：mwlab.db（[品牌总数]个展会品牌）+ WebSearch

---

## 一、行业展会全景

### 1.1 基本规模
- 总展会品牌数：[数字，来自 DB]
- UFI 认证展会：[数字，来自 DB]
- 国际化展会：[数字，来自 DB]
- 主要城市：[城市分布，来自 DB]

### 1.2 头部展会（规模 TOP 10）
[列表，来自 DB，字段：展会名、brand_id、规模评分、MA潜力、主办方、城市]

---

## 二、竞争格局分析

### 2.1 市场集中度
[分析头部 3-5 家的规模占比，数据来自 DB scale_score 分布]

### 2.2 主办方竞争格局
[主要主办方列表，展会数量，来自 DB organizer 字段统计]

### 2.3 地域分布
[城市集中度分析，来自 DB city 字段]

---

## 三、市场规模估算（TAM/SAM/SOM）

### 3.1 TAM（全球展会市场）
[引用 WebSearch 搜索到的 UFI 或权威机构数据，如无数据则说明"暂无可信数据来源"]

### 3.2 SAM（中国目标行业展会市场）
- 可接触展会品牌数：[DB 数据]
- 估算方法：[DB 展会数 × 平均展会收入估算，说明估算假设]

### 3.3 SOM（MDS 可实际进入份额）
- 基于 is_international 和城市分布，估算 MDS 可接触比例
- [具体估算过程和结果]

---

## 四、Porter 五力分析（展会行业适配版）

| 维度 | 评估 | 数据依据 |
|------|------|---------|
| 新进入者威胁 | [高/中/低] | [近3年新增展会数，来自 DB year 分布] |
| 替代品威胁 | [高/中/低] | [WebSearch：线上展会/垂直电商动态] |
| 买方议价力 | [高/中/低] | [exhibitors_count 集中度，来自 DB] |
| 供方议价力 | [高/中/低] | [venue/city 稀缺性，来自 DB] |
| 竞争强度 | [高/中/低] | [同行业展会数量，来自 DB] |

---

## 五、切入点建议

### 5.1 高优先级目标（MA 潜力评分 ≥ 4）
[列表：展会名、brand_id、MA潜力、战略相关性，来自 DB ma_potential 字段]

### 5.2 进入策略建议
[基于以上数据，给出 2-3 条具体行动建议。每条建议说明数据依据。]

---

## 六、数据局限性说明

- mwlab.db 覆盖 [品牌数] 个展会品牌，可能不包含所有细分市场展会
- exhibition_relation 表当前数据量：[查询结果]
- TAM 估算基于假设，仅供参考
```

---

## 第四步：保存报告到 DB

报告生成完毕后，执行以下命令将报告持久化到 intel_report 表（D-03 结果沉淀原则）：

```bash
python3 tools/intel/report_writer.py \
  --type industry_research \
  --industry_l1 "$ARGUMENTS" \
  --content "$(cat /tmp/industry_report.md)"
```

执行成功后输出 `report_id=<N>` 表示入库成功。

---

## 注意事项

1. **禁止虚构**：第一步 DB 数据是唯一允许直接引用展会数字的来源
2. **WebSearch 用于宏观**：不用 WebSearch 数据覆盖 DB 中已有的展会信息
3. **数据缺口透明化**：如 DB 无某字段数据，在报告中明确说明"暂无数据"
4. **人工触发**：本 Skill 不做自动化，每次由 BD 团队手动执行
```

关键约束（per D-02, D-07, D-11, D-12, D-13, REQ-23, REQ-24）：
- `disable-model-invocation: true` 必须在 frontmatter 中（人工触发原则）
- `!` 命令调用 db_query.py 必须在 LLM 分析步骤之前（DB 优先）
- 报告模板明确标注每个数字的数据来源（DB vs WebSearch）
- 末尾有 report_writer.py 调用命令（结果沉淀）
  </action>

  <verify>
    <automated>grep -c "disable-model-invocation: true" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md" && grep -c "db_query.py industry-research" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md" && grep -c "report_writer.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md"</automated>
  </verify>

  <acceptance_criteria>
    - 文件存在：.claude/skills/industry-research/SKILL.md
    - grep -c "disable-model-invocation: true" SKILL.md 返回 1
    - grep -c "allowed-tools:" SKILL.md 返回 1，且值包含 "WebSearch"
    - grep -c "db_query.py industry-research" SKILL.md 返回 1（!命令注入）
    - grep -c "report_writer.py" SKILL.md 返回 1（结果沉淀命令）
    - grep -c "TAM" SKILL.md 返回 ≥ 1（TAM/SAM/SOM 框架）
    - grep -c "Porter" SKILL.md 返回 ≥ 1（Porter 五力）
    - grep -c "禁止虚构\|禁止 LLM 虚构" SKILL.md 返回 ≥ 1
    - SKILL.md 前5行包含 "---"（有 YAML frontmatter）
  </acceptance_criteria>

  <done>industry-research Skill 文件完整，DB 数据注入在 LLM 分析前执行，报告模板覆盖 TAM/SAM/SOM + Porter 五力，末尾写入 intel_report 表</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $ARGUMENTS → shell 命令 | 用户输入的行业名被嵌入 `!` 命令 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Injection | SKILL.md !命令中的 $ARGUMENTS | accept | 本地单用户工具，$ARGUMENTS 由 BD 团队自行输入行业标签，无外部攻击面；db_query.py 内部使用参数化 SQL |
| T-05-12 | Information Disclosure | WebSearch 查询包含行业名称 | accept | 行业调研查询为公开信息，不含敏感数据 |
</threat_model>

<verification>
验证 Skill 文件结构：

```bash
# 1. 检查文件存在
ls -la "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md"

# 2. 检查关键字段
grep "disable-model-invocation" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md"
grep "db_query.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md"
grep "report_writer.py" "/Volumes/databoard/AI Project/D_dashboard/.claude/skills/industry-research/SKILL.md"
```
</verification>

<success_criteria>
- SKILL.md 存在，YAML frontmatter 格式正确
- disable-model-invocation: true 确保人工触发
- !命令在报告生成前注入 DB 数据
- 报告模板含 TAM/SAM/SOM + Porter 五力 + 切入点建议
- 末尾调用 report_writer.py 实现结果沉淀
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-04-SUMMARY.md，记录：
- Skill 调用方式：/industry-research 机械和设备
- Skill 文件路径
- 报告结构的6个段落
</output>
