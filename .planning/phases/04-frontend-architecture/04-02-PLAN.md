---
phase: 04-frontend-architecture
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/001_init.sql
  - lib/supabase/server.ts
  - lib/supabase/client.ts
  - lib/types.ts
  - middleware.ts
  - tests/middleware.test.ts
autonomous: false
requirements: ["UI-POOL"]
user_setup:
  - service: "Supabase"
    why: "执行 DB migration，需创建 Supabase 项目并获取 access token"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "supabase.com -> Account -> Access Tokens -> Generate token"
    dashboard_config:
      - task: "在 supabase.com 创建免费 PostgreSQL 项目，记录 URL、anon key、service_role_key"
        location: "supabase.com -> New project / Project Settings -> API"

must_haves:
  truths:
    - "PostgreSQL DDL 文件存在，包含 5 张表的完整建表语句 + RLS Policies + 索引"
    - "Supabase 数据库中 5 张表已创建（通过 `npx supabase db push` 执行迁移）"
    - "middleware.ts 实现路由守卫：未登录用户重定向到 /login，非 admin 访问 /setting 重定向到 /dashboard"
    - "lib/supabase/server.ts 和 lib/supabase/client.ts 提供 Supabase 客户端创建函数"
    - "middleware 测试用例已实现并通过"
  artifacts:
    - path: "supabase/migrations/001_init.sql"
      provides: "5 张表的 PostgreSQL DDL + RLS 策略 + 索引"
      min_lines: 150
      contains: "CREATE TABLE exhibition_brand"
    - path: "lib/supabase/server.ts"
      provides: "服务端 Supabase 客户端（createServerClient + next/headers cookies）"
      contains: "createServerClient"
    - path: "lib/supabase/client.ts"
      provides: "浏览器端 Supabase 客户端（createBrowserClient）"
      contains: "createBrowserClient"
    - path: "middleware.ts"
      provides: "Auth token 刷新 + 路由守卫"
      contains: "supabase.auth.getUser"
  key_links:
    - from: "middleware.ts"
      to: "lib/supabase/server.ts"
      via: "createServerClient"
      pattern: "createServerClient"
    - from: "tests/middleware.test.ts"
      to: "middleware.ts"
      via: "测试 middleware 行为"
      pattern: "middleware"

---

<objective>
PostgreSQL Schema 迁移文件 + Supabase Auth 基础设施 + 路由守卫中间件

Purpose: 将 SQLite 的 6 张表（排除 user 表 — 由 Supabase Auth 替代）按 RESEARCH.md 确定的转换规则转为 PostgreSQL DDL，写入 Supabase migration 文件，推送到 Supabase 数据库。同时创建 Supabase Auth 服务端/客户端基础设施文件、路由守卫中间件（middleware.ts），以及 lib/types.ts 数据库类型定义。

角色权限：admin/manager/readonly 三角色，存储在 app_metadata.role（D-13）。

Output: supabase/migrations/001_init.sql、lib/supabase/server.ts、lib/supabase/client.ts、middleware.ts、lib/types.ts、更新后的测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-CONTEXT.md
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (DDL 转换第436-453行, RLS 第472-504行, Pattern 1-2 第240-321行, Pattern 4 第348-374行)
@.planning/phases/04-frontend-architecture/04-PATTERNS.md (DDL pattern, Supabase Auth, middleware)
@.planning/phases/04-frontend-architecture/04-VALIDATION.md
schema/init_db.sql

<interfaces>
从 schema/init_db.sql 推导的 5 表（user 表不迁移）字段定义详见 04-RESEARCH.md 第 460-467 行。角色值固定：admin / manager / readonly。

关键导入路径（Plan 04-01 配置的 @ 别名）：
- @/lib/supabase/server → lib/supabase/server.ts
- @/lib/supabase/client → lib/supabase/client.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 PostgreSQL DDL 迁移文件（5 表 + RLS Policies + 索引）</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/supabase/migrations/001_init.sql
  </files>

  <read_first>
    /Volumes/databoard/AI Project/D_dashboard/schema/init_db.sql（全部 146 行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（SQLite→PostgreSQL DDL 转换规则，第436-453行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（RLS Policies 完整模板，第483-503行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（PostgreSQL 建表示例，第706-749行）
  </read_first>

  <action>
    创建 supabase/migrations/001_init.sql，按 RESEARCH.md 确定的转换规则将 init_db.sql 中的 6 表（排除 user）转为 PostgreSQL DDL，并在末尾追加 RLS Policies：

    转换规则：
    - brand_id/edition_id/record_id TEXT PK → 保持 TEXT PRIMARY KEY
    - crawl_log.id, manual_tag_history.id INTEGER AUTOINCREMENT → BIGSERIAL PRIMARY KEY
    - datetime('now','localtime') → TIMESTAMPTZ NOT NULL DEFAULT NOW()
    - date_start/date_end TEXT → DATE
    - overseas_exhibitor_pct REAL → DOUBLE PRECISION
    - 其他 INTEGER、TEXT 字段保持
    - CHECK 约束语法保持
    - 删除 PRAGMA 行

    5 张表：
    1. exhibition_brand（全部 20+ 字段，含所有 CHECK 约束）
    2. exhibition_edition（date_start/date_end 用 DATE，overseas_exhibitor_pct DOUBLE PRECISION）
    3. data_provenance
    4. crawl_log（id BIGSERIAL PRIMARY KEY）
    5. manual_tag_history（id BIGSERIAL PRIMARY KEY, brand_id TEXT FK）

    6 个索引（保持与 SQLite 一致）：idx_brand_name_cn, idx_brand_industry, idx_brand_competition, idx_brand_mds, idx_edition_brand_year, idx_edition_year, idx_edition_status, idx_provenance_brand, idx_provenance_source, idx_provenance_url, idx_tag_history_brand

    RLS Policies（精确 SQL，来自 RESEARCH.md）：
    - CREATE OR REPLACE FUNCTION get_user_role()
    - ALTER TABLE ... ENABLE ROW LEVEL SECURITY（5 张表各一条）
    - exhibition_brand: SELECT + ALL policies
    - exhibition_edition: SELECT + ALL policies
    - data_provenance: SELECT policy
    - crawl_log: SELECT policy
    - manual_tag_history: SELECT + INSERT policies
  </action>

  <acceptance_criteria>
    1. 文件存在，不少于 150 行
    2. 包含 CREATE TABLE exhibition_brand（含所有 20+ 字段）
    3. 包含 CREATE TABLE exhibition_edition、data_provenance、crawl_log、manual_tag_history
    4. 不包含 CREATE TABLE user
    5. 不包含 "PRAGMA"
    6. crawl_log.id 使用 BIGSERIAL PRIMARY KEY
    7. manual_tag_history.id 使用 BIGSERIAL PRIMARY KEY
    8. exhibition_edition.date_start 和 date_end 使用 DATE 类型
    9. exhibition_edition.overseas_exhibitor_pct 使用 DOUBLE PRECISION
    10. 包含 5 条 ALTER TABLE ... ENABLE ROW LEVEL SECURITY
    11. 包含 CREATE OR REPLACE FUNCTION get_user_role()
    12. 包含至少 8 条 CREATE POLICY 语句
  </acceptance_criteria>

  <verify>
    <automated>grep -c "CREATE TABLE exhibition_brand" /Volumes/databoard/AI\ Project/D_dashboard/supabase/migrations/001_init.sql && grep -c "CREATE POLICY" /Volumes/databoard/AI\ Project/D_dashboard/supabase/migrations/001_init.sql && grep -c "ENABLE ROW LEVEL SECURITY" /Volumes/databoard/AI\ Project/D_dashboard/supabase/migrations/001_init.sql && grep -c "get_user_role" /Volumes/databoard/AI\ Project/D_dashboard/supabase/migrations/001_init.sql</automated>
  </verify>

  <done>
    001_init.sql 创建完成，包含 5 表完整 DDL、6 个索引、5 条 RLS 启用语句、1 个 get_user_role 函数、8+ 条 CREATE POLICY。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] 推送 Schema 迁移到 Supabase 数据库</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/supabase/migrations/001_init.sql
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Environment Availability 第579-591行，Missing dependencies）
  </read_first>

  <what-built>
    PostgreSQL Schema 迁移文件（001_init.sql）已就绪，需要推送到 Supabase 项目。
  </what-built>

  <how-to-verify>
    前提条件：
    1. 已在 supabase.com 创建免费项目（Project Settings → API 可获取 URL 和 keys）
    2. 已在 supabase.com Account → Access Tokens 生成 SUPABASE_ACCESS_TOKEN

    执行步骤（二选一）：

    方案 A — 使用 Supabase CLI（推荐）：
    ```bash
    # 设置 access token（非 TTY 环境使用环境变量）
    export SUPABASE_ACCESS_TOKEN="sbp_your_access_token_here"
    # 链接到远程项目
    npx supabase link --project-ref your_project_ref
    # 推送迁移
    npx supabase db push
    ```

    方案 B — 使用 Supabase 控制台 SQL Editor 手动执行：
    1. 登录 supabase.com → SQL Editor
    2. 粘贴 001_init.sql 完整内容
    3. 执行
    4. 确认无错误（预期输出："成功创建 5 张表" / "CREATE POLICY" x 8+）

    验证结果：
    - 方案 A: `npx supabase db push` 输出显示表已创建
    - 方案 B: SQL Editor 显示绿色 "Success"
    - 在 Supabase 控制台 Table Editor 确认 5 张表存在
  </how-to-verify>

  <resume-signal>
    迁移执行成功后，输入 "approved" 继续后续任务。
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: 创建 Supabase 客户端文件 + 路由守卫中间件 + 类型定义</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/lib/supabase/server.ts
    /Volumes/databoard/AI Project/D_dashboard/lib/supabase/client.ts
    /Volumes/databoard/AI Project/D_dashboard/lib/types.ts
    /Volumes/databoard/AI Project/D_dashboard/middleware.ts
    /Volumes/databoard/AI Project/D_dashboard/tests/middleware.test.ts
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Pattern 1: middleware.ts 完整代码，第246-294行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Pattern 2: 服务端 client，第298-321行）
    .planning/phases/04-frontend-architecture/04-PATTERNS.md（Supabase Browser Client pattern, 第381-395行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Anti-Patterns: 必须用 getUser() 而非 getSession()，不要用 user_metadata）
  </read_first>

  <action>
    （A）创建 lib/supabase/client.ts（浏览器端 Supabase 客户端）：
    ```typescript
    import { createBrowserClient } from '@supabase/ssr'

    export function createClient() {
      return createBrowserClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      )
    }
    ```

    （B）创建 lib/supabase/server.ts（服务端 Supabase 客户端）：
    ```typescript
    import { createServerClient } from '@supabase/ssr'
    import { cookies } from 'next/headers'

    export async function createClient() {
      const cookieStore = await cookies()
      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() { return cookieStore.getAll() },
            setAll(cookiesToSet) {
              cookiesToSet.forEach(({ name, value, options }) =>
                cookieStore.set(name, value, options))
            },
          },
        }
      )
    }
    ```

    注意：必须使用 `cookies()` 的异步 API（在 cookies() 前加 await）— 这是 Next.js 15 的要求。

    （C）创建 lib/types.ts（数据库类型定义）：
    基于 001_init.sql 中 5 张表定义的 TypeScript 接口，精确对应每个字段名和类型，使用 snake_case 命名。

    ```typescript
    export interface ExhibitionBrand {
      brand_id: string
      name_cn: string
      name_en: string
      first_year: number | null
      organizer: string
      co_organizer: string
      city: string
      frequency: string
      industry_l1: string
      industry_l2: string
      competition_relation: string
      mds_related: string
      scale_score: number | null
      is_international: number
      is_ufi_certified: number
      ma_potential: number | null
      strategic_relevance: number | null
      competitor_group: string
      website: string
      notes: string
      created_at: string
      updated_at: string
    }

    export interface ExhibitionEdition {
      edition_id: string
      brand_id: string
      edition_num: number | null
      year: number | null
      date_start: string | null
      date_end: string | null
      city: string
      venue: string
      status: string
      area_sqm: number | null
      exhibitors_count: number | null
      visitors_count: number | null
      overseas_exhibitor_pct: number | null
      booth_price_per_sqm: number | null
      heat_score: number | null
      yoy_trend: string
      anomaly_flag: number
      data_source: string
      recorded_at: string
      notes: string
    }

    export interface DataProvenance {
      record_id: string
      brand_id: string | null
      source_site: string
      source_url: string
      raw_payload: string
      crawled_at: string
      crawl_batch_id: string
      notes: string
    }

    export interface CrawlLog {
      id: number
      batch_id: string
      source_site: string
      started_at: string
      finished_at: string | null
      status: string
      total_fetched: number
      total_inserted: number
      total_skipped: number
      error_message: string
      created_at: string
    }

    export interface ManualTagHistory {
      id: number
      brand_id: string
      field_name: string
      old_value: string
      new_value: string
      changed_by: string
      changed_at: string
      reason: string
    }

    export type UserRole = 'admin' | 'manager' | 'readonly'
    ```

    （D）创建 middleware.ts（RESEARCH.md Pattern 1 的完整实现，第246-294行）：
    严格按照 RESEARCH.md 中的完整代码，在项目根目录创建 middleware.ts。

    关键逻辑：
    - 使用 createServerClient 在 middleware 中初始化 Supabase
    - **必须使用 supabase.auth.getUser() 而非 getSession()**（安全要求，per RESEARCH.md Anti-Patterns）
    - 路由守卫：未登录用户访问 /login 以外的路径 → 重定向到 /login
    - 角色守卫：非 admin 用户访问 /setting → 重定向到 /dashboard
    - matcher 排除静态资源：_next/static, _next/image, favicon.ico, *.png

    **注意**：middleware 中不要连接 DB，只做 token 刷新和路由守卫（per RESEARCH.md Anti-Patterns）。

    （E）更新 tests/middleware.test.ts：
    用真实测试用例替换占位测试。使用 RESEARCH.md 中 middleware.ts 的逻辑，验证两个行为：
    1. 无 session cookie 时访问 /dashboard → 应该 redirect 到 /login
    2. 有效的 admin session 访问 /setting → 应该正常访问（不 redirect）

    由于 middleware 依赖 Supabase Auth 的实际 http 调用，测试使用 vitest 的 vi.mock() mock createServerClient，返回可控的 user 值。实现两个 describe 块对应上述两个场景。
  </action>

  <acceptance_criteria>
    1. `lib/supabase/client.ts` 使用 createBrowserClient，导出 createClient 函数
    2. `lib/supabase/server.ts` 使用 createServerClient，异步 cookies()，导出 createClient 函数
    3. `lib/types.ts` 包含 ExhibitionBrand、ExhibitionEdition、DataProvenance、CrawlLog、ManualTagHistory、UserRole 六个接口/类型
    4. ExhibitionBrand 接口包含 brand_id、name_cn、industry_l1、industry_l2、competition_relation、mds_related 等 20+ 字段
    5. `middleware.ts` 使用 supabase.auth.getUser()（不包含 getSession）
    6. middleware.ts 重定向逻辑包含：!user && !/login → /login；/setting && role!==admin → /dashboard
    7. middleware.ts 的 config.matcher 排除 _next/static
    8. `tests/middleware.test.ts` 使用 vi.mock 模拟 Supabase 客户端
    9. middleware 测试包含两个 describe 块（未登录守卫和角色守卫）
    10. `npx vitest run tests/middleware.test.ts` 执行通过
  </acceptance_criteria>

  <verify>
    <automated>grep -c "createBrowserClient" /Volumes/databoard/AI\ Project/D_dashboard/lib/supabase/client.ts && grep -c "createServerClient" /Volumes/databoard/AI\ Project/D_dashboard/lib/supabase/server.ts && grep -c "getUser" /Volumes/databoard/AI\ Project/D_dashboard/middleware.ts && grep -v "^//" /Volumes/databoard/AI\ Project/D_dashboard/middleware.ts | grep -c "getSession" | xargs test 0 -eq && echo "NO_GETSESSION" || echo "HAS_GETSESSION" && grep -c "ExhibitionBrand" /Volumes/databoard/AI\ Project/D_dashboard/lib/types.ts && cd /Volumes/databoard/AI\ Project/D_dashboard && npx vitest run tests/middleware.test.ts 2>&1 | tail -3</automated>
  </verify>

  <done>
    lib/supabase/server.ts、client.ts、middleware.ts、lib/types.ts 创建完成，middleware 测试通过。中间件实现了路由守卫和角色权限控制。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Next.js Server | middleware.ts 在每个请求前刷新 token |
| Next.js Server → Supabase | 使用 @supabase/ssr cookie-based session，Service Role Key 仅服务端可访问 |
| DB 层（Supabase PostgreSQL） | RLS Policies 强制执行行级权限 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Spoofing / Auth Bypass | middleware.ts | mitigate | 使用 supabase.auth.getUser()（非 getSession()）验证 token；未登录重定向到 /login |
| T-4-02 | Elevation of Privilege | middleware.ts (/setting guard) | mitigate | middleware 检查 app_metadata.role !== 'admin' 时重定向到 /dashboard（D-13 三角色） |
| T-4-05 | Elevation of Privilege | Supabase Auth (app_metadata) | mitigate | 角色存储在 app_metadata（仅 SERVICE_ROLE_KEY 可修改），不在 user_metadata（用户可修改） |
| T-4-06 | Tampering | RLS Policies | mitigate | 5 张表的 RLS 启用，manual_tag_history 仅 admin/manager 可 INSERT；数据层双重保护 |
</threat_model>

<verification>
1. ls supabase/migrations/001_init.sql lib/supabase/server.ts lib/supabase/client.ts lib/types.ts middleware.ts — 全部存在
2. 001_init.sql 包含 5 表 DDL + RLS Policies（grep 确认 CREATE POLICY 数量 >= 8）
3. middleware.ts 不含 getSession（grep 反向确认）
4. npx vitest run tests/middleware.test.ts 通过
5. task 2 的 schema push（手动检查 Supabase 控制台 5 表存在）
</verification>

<success_criteria>
1. 001_init.sql 包含 5 张表的完整 DDL、6 个索引、RLS 策略
2. schema 已推送到 Supabase 数据库（Table Editor 可见 5 张表）
3. middleware.ts 实现 auth token 刷新 + 路由守卫（未登录 → /login，非admin → /dashboard）
4. lib/supabase/server.ts 和 client.ts 可供 API Routes 和页面组件导入
5. lib/types.ts 定义 5 个接口 + UserRole 类型
6. middleware 测试通过（10 个用例）
</success_criteria>

<output>
完成 Plan 04-02 后，创建 .planning/phases/04-frontend-architecture/04-02-SUMMARY.md
</output>
