---
phase: 06-code-audit
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - CLAUDE.md
  - AGENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - MWLAB-2026-PRD-v1.1-merged.md
  - tests/test_merge_engine.py
  - tests/test_schema.py
  - tests/middleware.test.ts
  - tests/api/auth.test.ts
  - tests/api/people.test.ts
  - tests/test_qcc_client.py
  - tests/test_intel_tools.py
  - tests/test_crawlers.py
  - tests/fixtures/ (爬虫 HTML fixture)
autonomous: false
requirements: [AUD-05]
findings: [D-01, D-02, D-03, D-07]
must_haves:
  truths:
    - "权威文档不再引用已不存在的 tag_api.py；STATE.md 的 API 框架描述与代码现实（Next.js API Routes）一致"
    - "PRD 与 ROADMAP 的 Phase 编号有明确对照，执行侧以 ROADMAP 为准"
    - "部署目标（Railway vs 阿里云）经客户确认并在三份文档中统一"
    - "Wave 1-4 的每个 BLOCKER/Critical 修复都有对应回归测试，套件全绿"
  artifacts:
    - path: "tests/test_qcc_client.py"
      provides: "201/102/超时 mock 分支测试"
    - path: "tests/test_crawlers.py"
      provides: "解析 fixture 回归（含 CRWL-01 空串不覆盖）"
    - path: "tests/middleware.test.ts"
      provides: "替换 expect(true) 占位为真实头剥离/401 测试"
  key_links:
    - from: "tests/test_merge_engine.py"
      to: "merge_engine.py CORE-01/02/03 修复"
      via: "hex ID / 双跑幂等 / 四字城市用例"
---

<objective>
Wave 5 · 文档对齐 + 关键回归测试。修正 D-01/02/03/07 四项文档漂移（D-04/05 属客户决策归 06-06，D-06 已在 06-02 修复），并为 Wave 1-4 的全部关键修复补上回归测试，把"审计绕开 BLOCKER 路径"的测试缺口堵死。

Purpose: 文档是 Claude 每次任务的输入，漂移文档会持续产生错误决策；无回归测试的修复在下一次改动中随时复发。
Output: 与代码现实一致的权威文档 + 覆盖全部关键路径的测试套件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-audit/AUDIT.md
@.planning/phases/06-code-audit/REVIEW-api.md
@.planning/phases/06-code-audit/REVIEW-core.md
@.planning/phases/06-code-audit/REVIEW-intel.md
@.planning/phases/06-code-audit/REVIEW-crawlers.md

前置：Wave 1-4 全部修复已合入，本计划的测试验证那些修复。dashboard.test.ts 的 401 断言翻转已在 06-01 Task 2 完成，本计划只扩展不重复。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 移除 tag_api.py 失效引用 + STATE.md 框架描述修正 [D-01][D-02]</name>
  <files>CLAUDE.md, AGENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
1. [D-01] `grep -rn "tag_api" CLAUDE.md AGENTS.md .planning/ROADMAP.md .planning/STATE.md` 找出全部引用，逐处处理：关键文件路径表中删除 tag_api.py 行；正文叙述改为现实（打标走 tools/export_for_tagging.py / import_tags.py，PRD v1.1 已记录 FastAPI 移除）。只改失实引用，不顺手改写周边内容。
2. [D-02] STATE.md 中 "API 框架：FastAPI + JWT" 改为 "Next.js API Routes + JWT (middleware.ts 验签)"。grep STATE.md 其它 FastAPI 字样一并核对。
  </action>
  <verify>
    <automated>grep -rn "tag_api" CLAUDE.md AGENTS.md .planning/ROADMAP.md .planning/STATE.md 输出为空；grep -n "FastAPI" .planning/STATE.md 输出为空</automated>
  </verify>
  <done>四份文档无失效引用，框架描述与代码一致</done>
  <commit>docs: remove stale tag_api references, correct API framework to Next.js routes [D-01][D-02]</commit>
</task>

<task type="auto">
  <name>Task 2: Phase 编号对照统一 [D-03]</name>
  <files>MWLAB-2026-PRD-v1.1-merged.md, .planning/ROADMAP.md</files>
  <action>
[D-03] PRD 的 Phase 5（部署与性能优化）与 ROADMAP 的 Phase 5（情报后端）编号冲突。最小修复（不重排任何一方既有编号）：
1. PRD 增加一节"Phase 编号对照（v1.1 后）"小表：PRD Phase ↔ ROADMAP Phase 的映射，并注明"执行与状态追踪以 ROADMAP 编号为准"。
2. ROADMAP 顶部加一行注释指向 PRD 对照表。
若梳理映射时发现两套编号存在无法对照的实质冲突（同名不同义阶段），停下来在 SUMMARY 报告，不擅自重编号。
  </action>
  <verify>
    <automated>grep -n "编号对照" MWLAB-2026-PRD-v1.1-merged.md 命中；grep -n "ROADMAP 编号为准\|编号对照" .planning/ROADMAP.md 命中</automated>
  </verify>
  <done>两套编号有权威对照，后续文档引用 Phase N 时歧义可消解</done>
  <commit>docs: add PRD/ROADMAP phase numbering cross-reference table [D-03]</commit>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: CHECKPOINT — 确认真实部署目标 [D-07]</name>
  <decision>当前及可预期的部署目标是哪一个：Railway（PRD 记载）还是阿里云（CLAUDE.md/STATE.md 记载 + 仓库内有阿里云 SSH 私钥）？</decision>
  <context>PRD 说部署 Railway，CLAUDE.md/STATE 说阿里云，两者必有一处失实。文档将按你的回答统一改写，不改任何部署代码或配置。</context>
  <options>
    <option id="aliyun">
      <name>阿里云</name>
      <pros>与 MWlab.pem 私钥、现有服务器凭证一致</pros>
      <cons>PRD 需更新</cons>
    </option>
    <option id="railway">
      <name>Railway</name>
      <pros>与 PRD 一致</pros>
      <cons>CLAUDE.md/STATE.md 需更新，且需解释阿里云凭证用途</cons>
    </option>
    <option id="both-or-other">
      <name>两者并存 / 其它</name>
      <pros>如实记录混合现状</pros>
      <cons>需说明各自承担的角色</cons>
    </option>
  </options>
  <resume-signal>回复 aliyun / railway / both-or-other（附一句说明）</resume-signal>
</task>

<task type="auto">
  <name>Task 4: 按确认结果统一部署目标描述 [D-07]</name>
  <files>MWLAB-2026-PRD-v1.1-merged.md, CLAUDE.md, .planning/STATE.md</files>
  <action>按 Task 3 的回答，将三份文档中的部署目标描述统一为确认值；被否定的一方改写为现实（如 "Railway（已弃用，2026-06 确认改为阿里云）"或反之）。仅改部署目标相关语句。</action>
  <verify>
    <automated>grep -in "railway\|阿里云" MWLAB-2026-PRD-v1.1-merged.md CLAUDE.md .planning/STATE.md 的输出中三份文档表述一致、无互相矛盾</automated>
  </verify>
  <done>部署目标单一事实源</done>
  <commit>docs: unify deployment target per client confirmation [D-07]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 5: merge_engine 回归测试（BLOCKER 路径全覆盖）</name>
  <files>tests/test_merge_engine.py, tests/test_schema.py</files>
  <behavior>
    - test_next_brand_id_with_hex_ids: 库含 EXPO-D92BC0D6 时返回数字序列下一个值 [CORE-01]
    - test_merge_idempotent_double_run: 同源数据 run_merge 两次，第二次后 brand/edition/provenance 行数与 data_source 值均不变 [CORE-02][CORE-05]
    - test_normalize_city_four_char: 呼和浩特/乌鲁木齐/齐齐哈尔/西双版纳 原样保留；湖北武汉→武汉 [CORE-03]
    - test_parse_date_cross_year_and_same_month: 跨年区间结束年+1；同月日范围可解析 [CORE-08]
    - test_init_db_applies_all_migrations: init_db(':memory:') 后含 country_cn/city_en/is_international_source/display_ready 列与 intel 两表 [CORE-04]
  </behavior>
  <action>将 06-02 验证用的一次性断言固化为 pytest 用例，追加到 tests/test_merge_engine.py（migration 用例进 tests/test_schema.py）。复用现有 conftest.py fixture 风格。这些正是审计指出"恰好绕开全部 BLOCKER"的缺口用例。</action>
  <verify>
    <automated>pytest tests/test_merge_engine.py tests/test_schema.py -x 退出码 0，且新增用例数 ≥5（pytest -q 计数比修改前多 ≥5）</automated>
  </verify>
  <done>五个 behavior 全部为正式用例且绿色；任何人回退 CORE-01/02/03/04/08 修复会立即红</done>
  <commit>test(merge): regression coverage for hex ids, idempotency, 4-char cities, date ranges, migrations [CORE-01..05,08,04 回归]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 6: API/middleware 安全回归测试</name>
  <files>tests/middleware.test.ts, tests/api/auth.test.ts, tests/api/people.test.ts</files>
  <behavior>
    - middleware: 携带伪造 x-user-email/x-user-role 头且无 token 的 /api/* NextRequest → 401，且注入头被剥离 [API-01 回归]
    - middleware: 合法 JWT cookie → 放行且 x-user-* 为验签所得值
    - auth: login 错误口令 → 401，正确口令 → 200 + Set-Cookie 含 HttpOnly、body 无 token [API-05 回归]
    - people: 无身份头 GET → 401 [API-03 回归]；readonly 角色 POST → 403 [API-04 回归]
  </behavior>
  <action>
1. 重写 tests/middleware.test.ts——删除全部 `expect(true).toBe(true)` 占位（审计：62 行核心鉴权代码零覆盖），用 next/server 的 NextRequest 构造真实请求调用 middleware 导出函数断言上述行为。
2. 新建 tests/api/auth.test.ts（login 是最关键零测试路径）与 tests/api/people.test.ts（写接口 403 矩阵），handler 直调模式参照现有 users.test.ts，但角色头注入仅用于 handler 单测层（middleware 层已有头剥离测试兜底，两层合起来才是完整契约）。
3. 确认 dashboard.test.ts 的 401 断言（06-01 已翻转）仍在并扩一个"合法用户 200"用例。
  </action>
  <verify>
    <automated>npm test 退出码 0；grep -c "expect(true).toBe(true)" tests/middleware.test.ts 输出 0</automated>
  </verify>
  <done>middleware 真实覆盖；login/写接口/未认证三类契约有回归；占位测试清零</done>
  <commit>test(api): real middleware header-strip and 401 tests, login and rbac regression [API-01..05 回归]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 7: intel 工具 mock 测试与冒烟测试</name>
  <files>tests/test_qcc_client.py, tests/test_intel_tools.py</files>
  <behavior>
    - qcc_client: mock 响应 Status=201 → Result=[] 且 Message 不含错误字样；102 → Message 以 STOP_BATCH 开头；超时 → 收敛结构返回不抛栈 [INTEL-06 回归]
    - insert_prospects: --json 同文件插两次 → 第二次 0 新增 [INTEL-02/03 回归]
    - db_query: brand-research 多命中 → 输出含"另有 N 条匹配"；industry-research "L1/L2" → 不报"暂无数据" [INTEL-04/05 回归]
    - report_writer + export_prospects: 建报告→插线索→按 report_id 导出 csv 行数一致；report_id 无线索时报错退出 [INTEL-01 回归]
  </behavior>
  <action>新建两个测试文件：test_qcc_client.py 用 unittest.mock patch HTTP 层（无需真实 Key，审计称"上线前最便宜的回归保障"）；test_intel_tools.py 用临时 SQLite（经 init_db 全量迁移创建）跑五个脚本的 subprocess/函数级冒烟。test_schema.py 如缺 intel 两表 CHECK 约束用例（report_type/source_type/prospect_score 边界）一并补在 test_intel_tools.py。</action>
  <verify>
    <automated>pytest tests/test_qcc_client.py tests/test_intel_tools.py -x 退出码 0</automated>
  </verify>
  <done>tools/intel 五脚本从零测试到关键路径全覆盖；qcc 状态码分支有 mock 回归</done>
  <commit>test(intel): qcc status mocks and intel tools smoke tests [INTEL-01..06 回归]</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 8: 爬虫解析 fixture 回归测试</name>
  <files>tests/test_crawlers.py, tests/fixtures/</files>
  <behavior>
    - jufair parse_list_page / parse_detail_page: 标准 fixture 解析出预期字段
    - 详情 UPDATE 不以空串覆盖既有值 [CRWL-01 回归]
    - cnexpo 错位版式（多插一个 p）→ venue 为空而非脏文本 [CRWL-04 回归]
    - insert_batch 含重复 URL → 返回真实新增数 [CRWL-10 回归]
    - 单日与全写日期格式解析成功 [CRWL-14 回归]
  </behavior>
  <action>将 06-04 各任务的一次性验证脚本固化为 tests/test_crawlers.py；HTML fixture 存 tests/fixtures/（从 06-04 验证时使用的样本沉淀，敏感内容裁剪到最小必要片段）。纯函数 + 内存 SQLite，零网络。</action>
  <verify>
    <automated>pytest tests/test_crawlers.py -x 退出码 0，且测试过程无任何网络请求（不 mock 之外调用 requests 即抛错——给 Session.request 打 fail-fast monkeypatch）</automated>
  </verify>
  <done>爬虫从零测试到解析层有 fixture 回归；静默数据损毁类 bug 可被拦截</done>
  <commit>test(crawler): parse fixtures and no-overwrite regression [CRWL-01,04,10,14 回归]</commit>
</task>

<task type="auto">
  <name>Task 9: 全量套件收尾</name>
  <files>(无修改)</files>
  <action>运行 `pytest tests/ -x` 与 `npm test` 全量两套。任何红用例修因不改果。统计并在 SUMMARY 记录：测试文件数、用例总数（对比审计基线："14 个 route 仅 3 个有实质测试、爬虫/intel 零测试"）。</action>
  <verify>
    <automated>pytest tests/ -x 退出码 0 且 npm test 退出码 0</automated>
  </verify>
  <done>双套件全绿；测试覆盖缺口（AUDIT.md 第五节）关闭</done>
  <commit>test: full suite green after wave-5 regression coverage</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 未来 Claude 决策 | 漂移文档导致错误实现（D 类风险的本质） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Repudiation | 修复无回归测试可被静默回退 | mitigate | Task 5-8 固化全部关键断言 |
| T-06-18 | Spoofing | 测试以可伪造头作为鉴权契约 | mitigate | middleware 层真实头剥离测试（Task 6） |
</threat_model>

<verification>
- grep 验证四项文档漂移清零
- pytest + npm test 双套件全绿
- 新增用例覆盖审计点名的全部缺口（CORE-01/02/03/04/08、API-01~05、INTEL-01~06、CRWL-01/04/10/14）
</verification>

<success_criteria>
D-01/02/03/07 共 4 项关闭；AUDIT.md 第五节测试覆盖总评中点名的缺口全部有正式用例且绿色。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-code-audit/06-05-SUMMARY.md`，含测试覆盖前后对比数据。
</output>
