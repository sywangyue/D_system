---
phase: 04-frontend-architecture
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - middleware.ts
  - app/page.tsx
  - components/layout/Sidebar.tsx
  - components/layout/AppShell.tsx
  - lib/types.ts
autonomous: true
requirements: [UI-POOL]
must_haves:
  truths:
    - "未登录用户访问 /dashboard 被重定向到 /login"
    - "登录用户可以看到侧边栏导航（含 MD Logo 和 4 层入口）"
    - "Sidebar 显示当前用户邮箱和退出按钮"
    - "app/page.tsx 根路径重定向到 /dashboard 或 /login"
    - "lib/types.ts 不再包含 Supabase Database 类型（移除 db 泛型）"
  artifacts:
    - path: "middleware.ts"
      provides: "JWT cookie 路由守卫"
      contains: "jwtVerify import, session cookie check, admin-only /setting guard"
    - path: "components/layout/Sidebar.tsx"
      provides: "MD 品牌侧边栏导航"
      exports: "Sidebar (default)"
    - path: "components/layout/AppShell.tsx"
      provides: "应用布局壳（sidebar + main content）"
      contains: "layout with sidebar for non-login routes"
  key_links:
    - from: "middleware.ts" to: "lib/auth.ts" via: "JWT cookie decode"
    - from: "Sidebar.tsx" to: "lib/auth.ts" via: "getUserInfo() + clearAuth()"
---

<objective>
重写 Next.js 中间件、根页面、布局组件和类型定义，从 Supabase Auth 迁移到 JWT Cookie 认证体系。同时更新 Sidebar 品牌标识和导航结构以反映 4 层 Dashboard 架构（D-06）。

Purpose: 淘汰 Supabase Auth（D-16 JWT 3 角色保留）、实现路由级认证守卫、更新 Sidebar 为 MD 品牌样式
Output: 修改 5 个文件，删除 Supabase Auth 依赖
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 2: 中间件 JWT 验证 lines 572-613, 系统架构图 BFF 模式 lines 254-302, 反模式 lines 669-676)
@./lib/db.ts (04-01 Task 2 创建——better-sqlite3 单例)
@./lib/auth.ts (04-01 Task 2 创建——客户端认证工具)
@./.env.local (JWT_SECRET 环境变量)
</context>

<interfaces>
从 04-RESEARCH.md 提取的关键接口：

middleware.ts 模式（使用 jose 库，不是 jsonwebtoken——Edge Runtime 兼容）：
```typescript
// jose 是 Edge Runtime 兼容的 JWT 库
import { jwtVerify } from 'jose'
const JWT_SECRET = new TextEncoder().encode(process.env.JWT_SECRET || 'dev-secret')

// 在 middleware 中：
const token = request.cookies.get('session')?.value
const { payload } = await jwtVerify(token, JWT_SECRET)
// payload = { user_id, email, role, exp }
```

Sidebar 导航项（4 层 + 设置/退出）：
```typescript
NAV_ITEMS = [
  { href: '/dashboard', label: 'Dashboard', icon: 'layout-dashboard' },
  { href: '/calendar', label: '日历', icon: 'calendar' },
  { href: '/map', label: '地图', icon: 'map' },
  { href: '/setting', label: '设置', icon: 'settings', adminOnly: true },
]
```

lib/types.ts 需要保留的类型：Brand, Edition, DataProvenance, CrawlLog, ManualTagHistory, KpiData, IndustryDistribution, DashboardResponse, TagUpdateRequest, TAG_FIELDS
需要移除的类型：Database（Supabase 泛型类型）、TableRelationship
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写 middleware.ts——JWT Cookie 路由守卫</name>
  <files>middleware.ts</files>
  <read_first>
    @./middleware.ts (当前 Supabase 版本——完整覆盖重写)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 2: Middleware JWT 验证 lines 572-613)
    @./.env.local (JWT_SECRET 值)
  </read_first>
  <action>
  完整重写 /Volumes/databoard/AI Project/D_dashboard/middleware.ts，从 Supabase Auth 切换到 JWT Cookie 验证：

  ```typescript
  import { NextResponse, type NextRequest } from 'next/server'
  import { jwtVerify } from 'jose'

  const JWT_SECRET = new TextEncoder().encode(
    process.env.JWT_SECRET || 'mwlab-dev-secret-2026'
  )

  export async function middleware(request: NextRequest) {
    const token = request.cookies.get('session')?.value
    const { pathname } = request.nextUrl

    // 公开路径——始终放行
    if (
      pathname === '/login' ||
      pathname.startsWith('/_next') ||
      pathname.startsWith('/api/auth') ||
      pathname.match(/\.(svg|png|jpg|jpeg|gif|webp|ico)$/)
    ) {
      return NextResponse.next()
    }

    // 无 token → 重定向到 /login
    if (!token) {
      return NextResponse.redirect(new URL('/login', request.url))
    }

    try {
      const { payload } = await jwtVerify(token, JWT_SECRET)

      // 将用户信息注入请求头（API Routes 可读取）
      const requestHeaders = new Headers(request.headers)
      requestHeaders.set('x-user-email', payload.email as string)
      requestHeaders.set('x-user-role', payload.role as string)

      // admin-only 路由守卫：/setting
      if (pathname.startsWith('/setting') && payload.role !== 'admin') {
        return NextResponse.redirect(new URL('/dashboard', request.url))
      }

      return NextResponse.next({ request: { headers: requestHeaders } })
    } catch {
      // token 无效或过期
      return NextResponse.redirect(new URL('/login', request.url))
    }
  }

  export const config = {
    matcher: [
      '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
    ],
  }
  ```

  关键变更：
  - 移除 @supabase/ssr 和 lib/supabase/middleware 的 updateSession 调用
  - 使用 jose 的 jwtVerify（Edge Runtime 兼容——保留默认 runtime）
  - session cookie 名称为 "session"
  - 公开路径包括 /login、/_next、/api/auth（FastAPI 认证端点）
  - admin-only 路由守卫：/setting → role=admin
  - 注入 x-user-email 和 x-user-role 请求头供 API Routes 使用
  - 无效/过期 token → 重定向到 /login
  - 原 config.matcher 保持不变
  </action>
  <verify>
    <automated>
      grep -q "jwtVerify" middleware.ts && grep -q "session" middleware.ts && grep -q "x-user-role" middleware.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "supabase" middleware.ts` == 0（无 Supabase 残留）
    - `grep -c "jwtVerify" middleware.ts` >= 1
    - `grep -c "session" middleware.ts` >= 1（cookie 名称）
    - `grep -c "x-user-role" middleware.ts` >= 1
    - `grep -c "/setting" middleware.ts` >= 1（admin 守卫）
    - config.matcher 排除静态资源
  </acceptance_criteria>
  <done>
    middleware.ts 使用 jose JWT 验证替代 Supabase Auth，包含 /login 放行和 /setting admin-only 守卫
  </done>
</task>

<task type="auto">
  <name>Task 2: 重写 app/page.tsx + Sidebar.tsx + AppShell.tsx——移除 Supabase</name>
  <files>app/page.tsx, components/layout/Sidebar.tsx, components/layout/AppShell.tsx</files>
  <read_first>
    @./app/page.tsx (当前使用 supabase getUser 重定向——完整重写)
    @./components/layout/Sidebar.tsx (当前使用 lib/supabase/client——用 lib/auth.ts 替代)
    @./components/layout/AppShell.tsx (当前布局——保持结构更新 MD)
    @./lib/auth.ts (04-01 创建的客户端认证工具)
  </read_first>
  <action>
  修改 3 个文件，从 Supabase Auth 切换到 JWT + localStorage：

  **A) app/page.tsx** — 根路径重定向
  结果文件应包含：
  ```typescript
  import { cookies } from 'next/headers'
  import { redirect } from 'next/navigation'
  import { jwtVerify } from 'jose'

  export default async function Home() {
    const cookieStore = await cookies()
    const token = cookieStore.get('session')?.value

    if (token) {
      try {
        const secret = new TextEncoder().encode(process.env.JWT_SECRET || 'mwlab-dev-secret-2026')
        await jwtVerify(token, secret)
        redirect('/dashboard')
      } catch {
        redirect('/login')
      }
    } else {
      redirect('/login')
    }
  }
  ```
  关键：移除 `import { createClient } from "@/lib/supabase/server"`，使用 cookies() + jose。

  **B) components/layout/Sidebar.tsx** — 替换 Supabase Auth
  保留现有导航结构（NAV_ITEMS 数组 + MD Logo 区域），但：
  - 移除 `import { createClient } from "@/lib/supabase/client"`
  - 导入 `import { getUserInfo, clearAuth } from "@/lib/auth"`
  - useEffect 中从 `getUserInfo()` 读取 userEmail 和 isAdmin（替代 supabase.auth.getUser）
  - handleLogout 调用 `clearAuth()` 替代 `supabase.auth.signOut()`
  - NAV_ITEMS 中图标用 lucide-react 组件（LayoutDashboard, Calendar, Map, Settings, LogOut）
  - 登录态用 `useState(() => getUserInfo())` 初始化（避免闪白）
  - 导航标签：Dashboard / 日历 / 地图 / 设置

  **C) components/layout/AppShell.tsx** — 更新布局
  - 移除 `import { createClient } from "@/lib/supabase/server"`（如果存在）
  - 保持现有结构（登录页无 sidebar，其他页面 flex h-screen）
  - 添加 MD Logo 导入（可选：暂时用文字 "MWLAB" 替代，04-04 会处理品牌细节）
  </action>
  <verify>
    <automated>
      grep -c "supabase" app/page.tsx | grep -q 0 && grep -c "getUserInfo\|getUser" components/layout/Sidebar.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx: `grep -c "supabase"` == 0
    - app/page.tsx: `grep -c "jwtVerify\|jose"` >= 1
    - Sidebar.tsx: `grep -c "supabase"` == 0
    - Sidebar.tsx: `grep -c "getUserInfo\|lib/auth"` >= 1
    - Sidebar.tsx: `grep -c "clearAuth"` >= 1
    - Sidebar.tsx: NAV_ITEMS 包含 Dashboard / 日历 / 地图 / 设置
    - AppShell.tsx: login 路径跳过 sidebar
  </acceptance_criteria>
  <done>
    app/page.tsx、Sidebar.tsx、AppShell.tsx 全部无 Supabase 引用，使用 JWT/localeStorage 认证
  </done>
</task>

<task type="auto">
  <name>Task 3: 清理 lib/types.ts——移除 Supabase Database 类型</name>
  <files>lib/types.ts</files>
  <read_first>
    @./lib/types.ts (当前完整文件——删除 Supabase 泛型，保留数据行类型)
  </read_first>
  <action>
  修改 /Volumes/databoard/AI Project/D_dashboard/lib/types.ts：

  保留以下导出（这些被 API routes 和组件使用）：
  - Brand 接口
  - Edition 接口
  - DataProvenance 接口
  - CrawlLog 接口
  - ManualTagHistory 接口
  - KpiData 接口
  - IndustryDistribution 接口
  - DashboardResponse 接口
  - ApiError 接口
  - TagUpdateRequest 接口
  - TAG_FIELDS 常量

  移除以下内容：
  - `Database` 类型（Supabase GenericTable 泛型——不再需要）
  - `TableRelationship` 接口
  - 注释中的 "Supabase Type Helpers" 段落
  - 注释中的 "Required by @supabase/supabase-js" 说明

  注意：不要改动 Brand、Edition 等数据行接口的字段——它们直接映射 SQLite mwlab.db 表，当前字段是正确的。
  </action>
  <verify>
    <automated>
      grep -c "interface Brand" lib/types.ts && grep -c "interface KpiData" lib/types.ts && ! grep -q "Supabase Type Helpers" lib/types.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface Brand" lib/types.ts` >= 1
    - `grep -c "interface KpiData" lib/types.ts` >= 1
    - `grep -c "TableRelationship" lib/types.ts` == 0（已移除）
    - `grep -c "type Database" lib/types.ts` == 0（已移除）
    - `grep -c "Supabase" lib/types.ts` == 0（无 Supabase 引用）
    - `grep -c "TAG_FIELDS" lib/types.ts` >= 1（保留打标常量）
  </acceptance_criteria>
  <done>
    lib/types.ts 保留所有数据行接口和 API 响应类型，移除 Supabase 专用泛型
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> middleware | JWT session cookie 在每次请求时被验证 |
| middleware -> protected routes | JWT payload 注入 x-user-* 请求头 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | middleware session cookie | mitigate | jose jwtVerify 验证 HMAC-SHA256 签名，防 cookie 伪造 |
| T-04-06 | Elevation of Privilege | /setting 路由 | mitigate | middleware 检查 JWT role === 'admin'，否则 302 到 /dashboard |
| T-04-07 | Information Disclosure | 未登录访问 | mitigate | middleware 检查 cookie 存在且有效，否则 302 到 /login |
</threat_model>

<verification>
1. `grep -r "supabase" middleware.ts app/page.tsx components/layout/ lib/types.ts --include='*.{ts,tsx}' | wc -l` — 结果为 0
2. `grep "jwtVerify" middleware.ts` — 存在
3. `grep "getUserInfo" components/layout/Sidebar.tsx` — 存在
4. `grep "interface Brand" lib/types.ts` — 存在
5. `grep "TableRelationship" lib/types.ts` — 不存在
</verification>

<success_criteria>
- middleware.ts 无 Supabase 引用，使用 jose JWT 验证
- app/page.tsx 根路径根据 session cookie 重定向到 /dashboard 或 /login
- Sidebar.tsx 从 lib/auth.ts 读取用户信息，无 Supabase
- AppShell.tsx 保持布局结构，无 Supabase
- lib/types.ts 移除 Supabase Database 类型，保留数据行接口
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-02-SUMMARY.md
</output>
