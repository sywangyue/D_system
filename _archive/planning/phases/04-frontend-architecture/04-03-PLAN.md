---
phase: 04-frontend-architecture
plan: 03
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - app/api/dashboard/route.ts
  - app/api/map/markers/route.ts
  - app/api/calendar/events/route.ts
  - app/api/brands/[id]/route.ts
  - app/api/brands/[id]/tags/route.ts
  - app/api/users/route.ts
  - app/api/setting/status/route.ts
autonomous: true
requirements: [UI-POOL]
must_haves:
  truths:
    - "GET /api/dashboard 返回展览面积/展商/观众/集团 KPI + 品牌列表 + 行业分布 + 年比趋势（基于 mwlab.db 真实数据）"
    - "GET /api/map/markers 返回城市聚合标注数据（city/count/lat/lng/top_exhibitions/is_china）"
    - "GET /api/calendar/events 返回按日期排序的展会日程"
    - "GET /api/brands/[id] 返回品牌详情及届次列表"
    - "PATCH /api/brands/[id]/tags 代理到 FastAPI tag_api.py 实现打标"
    - "GET /api/users 返回用户列表（admin only）"
    - "GET /api/setting/status 返回数据状态 + 系统信息"
  artifacts:
    - path: "app/api/dashboard/route.ts"
      provides: "Dashboard KPI 聚合查询（better-sqlite3）含年比趋势"
      contains: "getDb() import, SQL aggregation for area/exhibitors/visitors/organizers, year-trend query"
    - path: "app/api/brands/[id]/tags/route.ts"
      provides: "打标 PATCH 代理（→ FastAPI tag_api.py）"
      contains: "fetch() to FASTAPI_URL"
  key_links:
    - from: "所有 API routes" to: "lib/db.ts" via: "getDb() → better-sqlite3 查询"
    - from: "tags/route.ts" to: "tag_api.py" via: "fetch(FASTAPI_URL/api/brands/{id}/tags)"
---

<objective>
重写所有 7 个 Next.js API Route 文件，从 Supabase 查询迁移到 better-sqlite3 直连（BFF 模式/只读）+ FastAPI 代理（写操作/认证）。同时增加年比趋势数据以满足 TrendChart 组件需求（B1 修复）。这是实现 D-12（真实 mwlab.db 数据验证）的关键计划。

Purpose: 淘汰 Supabase 数据层（D-15 SQLite），建立 BFF 混合架构——读直接查 SQLite，写代理到 FastAPI
Output: 7 个 app/api/*/route.ts 文件重写
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (BFF 混合架构 diagram lines 254-302, Dashboard API Route 模板 lines 395-444, Dashboard 4 层 SQL 查询模式 lines 447-482, 反模式 lines 669-676)
@./lib/db.ts (04-01 创建——better-sqlite3 单例)
@./.env.local (FASTAPI_URL=http://localhost:8000)
@./tag_api.py (目前打标 PATCH 端点——需要代理到此处)
</context>

<interfaces>
从 RESEARCH.md 提取的 SQL 查询模板：

KPI 聚合 SQL:
```sql
SELECT COALESCE(SUM(e.area_sqm), 0) as total_area,
       COALESCE(SUM(e.exhibitors_count), 0) as total_exhibitors,
       COALESCE(SUM(e.visitors_count), 0) as total_visitors,
       COUNT(DISTINCT b.organizer) as total_organizers
FROM exhibition_brand b
JOIN exhibition_edition e ON e.brand_id = b.brand_id
WHERE 1=1  -- dynamic filters appended
```

年比趋势 SQL（B1 修复——新增，供 TrendChart 组件使用）：
```sql
SELECT e.year, COALESCE(SUM(e.area_sqm), 0) as area_sqm
FROM exhibition_brand b
JOIN exhibition_edition e ON e.brand_id = b.brand_id
${where}
GROUP BY e.year
ORDER BY e.year
```

城市聚合 SQL (map):
```sql
SELECT e.city, COUNT(DISTINCT b.brand_id) as exhibition_count,
       GROUP_CONCAT(DISTINCT b.name_cn, ', ') as exhibition_names
FROM exhibition_brand b
JOIN exhibition_edition e ON e.brand_id = b.brand_id
WHERE e.city != ''
GROUP BY e.city ORDER BY exhibition_count DESC
```

FastAPI proxy 模式 (PATCH tags):
```typescript
const res = await fetch(`${process.env.FASTAPI_URL}/api/brands/${id}/tags`, {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify(body),
})
const data = await res.json()
return NextResponse.json(data, { status: res.status })
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写 BFF 只读 API Routes（dashboard + map + calendar + brand-detail）含年比趋势</name>
  <files>app/api/dashboard/route.ts, app/api/map/markers/route.ts, app/api/calendar/events/route.ts, app/api/brands/[id]/route.ts</files>
  <read_first>
    @./app/api/dashboard/route.ts (当前 Supabase 版本——完整重写)
    @./app/api/map/markers/route.ts (当前 Supabase 版本——保持 cityCoords，替换数据源)
    @./app/api/calendar/events/route.ts (当前 Supabase 版本——替换数据源)
    @./app/api/brands/[id]/route.ts (当前 Supabase 版本——替换数据源)
    @./lib/db.ts (better-sqlite3 getDb 调用)
    @./lib/types.ts (Brand, Edition, KpiData, DashboardResponse 类型)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md lines 395-482 (SQL 查询模式)
  </read_first>
  <action>
  重写 4 个 BFF 只读 API 路由，将所有 supabase.from() 查询替换为 better-sqlite3 参数化查询：

  **通用模式（每个文件）**：
  ```typescript
  import { NextRequest, NextResponse } from 'next/server'
  import { getDb } from '@/lib/db'
  ```

  **A) app/api/dashboard/route.ts** — 核心聚合查询（B1 修复：新增年比趋势）
  - FROM 子句: exhibition_brand b JOIN exhibition_edition e
  - 动态 WHERE: industry_l2、competition_relation（多选）、mds_related
  - 返回 4 个查询结果:
    a) KPI 聚合行 (total_area, total_exhibitors, total_visitors, total_organizers)
    b) 品牌列表 (SELECT b.*)
    c) 行业分布 (SELECT industry_l2, COUNT(*) GROUP BY industry_l2)
    d) **年比趋势 (yearTrend)** [B1 修复]：
       ```sql
       SELECT e.year, COALESCE(SUM(e.area_sqm), 0) as area_sqm
       FROM exhibition_brand b
       JOIN exhibition_edition e ON e.brand_id = b.brand_id
       ${where}
       GROUP BY e.year
       ORDER BY e.year
       ```
       - 返回 { year: number, area_sqm: number }[] 数组
       - 应用与 KPI 查询相同的动态 WHERE 条件（保持过滤联动一致性）
       - 供 dashboard-content.tsx 中 overview/trend SubTab 的 TrendChart 组件使用
  - 使用参数化查询，`?` 占位符
  - 过滤参数：
    - industry_l2: 从 URL searchParams 读取
    - competition_relation: 逗号分隔，用 `IN (...)` 处理（需要动态生成 `?,?,?`）
    - mds_related: 单个值
  - 如果 competition_relation 为空字符串或 "全部"，不加此过滤
  - 如果 mds_related 为空或 "全部"，不加此过滤
  - 更新 return 语句：`return NextResponse.json({ kpis: kpiRow, brands, industryDistribution, yearTrend })`

  **B) app/api/map/markers/route.ts** — 城市聚合
  - 保留现有 cityCoords 坐标映射表（不要删除或改变坐标）
  - 从 Supabase 改为 better-sqlite3 查询 exhibition_edition + exhibition_brand
  - 查询：按 city 聚合，count 展会数量，取前 3 个展会名称作为 top_exhibitions
  - is_china 判断：保留现有逻辑（exhibition_brand.is_international == 0 为国内）
  - 返回 { markers: CityMarker[] }

  **C) app/api/calendar/events/route.ts** — 日历事件
  - 查询 exhibition_edition（date_start 非空）+ exhibition_brand
  - 按 date_start 升序排列
  - 返回 { events: CalendarEvent[] }

  **D) app/api/brands/[id]/route.ts** — 品牌详情
  - 查询 exhibition_brand WHERE brand_id = ?
  - 查询 exhibition_edition WHERE brand_id = ? ORDER BY year DESC
  - 返回 { brand, editions }

  注意：所有文件必须移除 `import { createClient } from "@/lib/supabase/server"`。
  不要在 API routes 中设置 `export const runtime = 'edge'`（better-sqlite3 需要 Node.js runtime）。
  </action>
  <verify>
    <automated>
      for f in app/api/dashboard/route.ts app/api/map/markers/route.ts app/api/calendar/events/route.ts app/api/brands/\[id\]/route.ts; do
        echo "--- $f ---"
        grep -c "supabase" "$f" || true
        grep -c "getDb\|better-sqlite3" "$f" || true
      done
    </automated>
  </verify>
  <acceptance_criteria>
    - 所有 4 个文件中 `grep -c "supabase"` == 0
    - 所有 4 个文件中 `grep -c "getDb\|@/lib/db"` >= 1
    - dashboard/route.ts: 包含 "total_area", "total_exhibitors", "yearTrend", "industryDistribution"
    - dashboard/route.ts: `grep -c "yearTrend\|GROUP BY e.year\|ORDER BY e.year"` >= 1（年比趋势查询）
    - map/markers/route.ts: 保留 "cityCoords" 对象（坐标映射不丢失）
    - calendar/events/route.ts: 包含 "ORDER BY date_start"
    - brands/[id]/route.ts: 包含 "edition_id" 和 "brand_id"
    - 所有文件中 `grep -c "runtime.*edge"` == 0
  </acceptance_criteria>
  <done>
    4 个 BFF 只读 API 路由全部使用 better-sqlite3 直连 mwlab.db，无 Supabase 引用；
    dashboard API 新增 yearTrend 年比趋势数据（B1 修复）
  </done>
</task>

<task type="auto">
  <name>Task 2: 重写代理 + 管理 API Routes（tags + users + status）</name>
  <files>app/api/brands/[id]/tags/route.ts, app/api/users/route.ts, app/api/setting/status/route.ts</files>
  <read_first>
    @./app/api/brands/[id]/tags/route.ts (当前 Supabase 版本——改为 FastAPI proxy)
    @./app/api/users/route.ts (当前 Supabase admin API 调用——改为 SQLite 直查)
    @./app/api/setting/status/route.ts (当前 Supabase 查询——改为 better-sqlite3)
    @./lib/db.ts (better-sqlite3 getDb)
    @./lib/types.ts (CrawlLog 类型)
    @./.env.local (FASTAPI_URL)
  </read_first>
  <action>
  重写 3 个 API 路由：

  **A) app/api/brands/[id]/tags/route.ts** — PATCH 代理到 FastAPI
  - 从请求头读取 x-user-email 和 x-user-role（由 middleware 注入）
  - 验证 role 不是 readonly（admin/manager 可打标）
  - 构造 PATCH 请求到 `{FASTAPI_URL}/api/brands/{id}/tags`
  - 传递 body（field_name, new_value, changed_by）
  - 返回 FastAPI 的响应（透传 status code + body）
  - 错误处理：FastAPI 不可用返回 503 "打标服务暂不可用"
  - 注意：这是 D-17 保留 FastAPI 的实现——写操作始终走 FastAPI 的验证逻辑

  **B) app/api/users/route.ts** — GET 用户列表（admin only）
  - 从请求头读取 x-user-role
  - role !== 'admin' → 403
  - 使用 better-sqlite3 直接查询 user 表：
    ```sql
    SELECT user_id, email, role, is_active, last_login FROM user ORDER BY email
    ```
  - 返回 { users: [...], total: N }
  - 注意（W2 修复——关于 BFF 而非 FastAPI proxy 的决策理由）：RESEARCH.md 的 Architectural Responsibility Map
    将 "Setting 页用户管理" 归入 FastAPI，但本计划使用 Next.js BFF 直查 SQLite。理由：
    1. user 表与主业务数据同库（mwlab.db），无需额外 HTTP 跳转
    2. 该端点仅为只读查询（不创建/修改用户），无业务逻辑校验需求
    3. middleware 已在路由前实施 role=admin 前置检查，等价于 FastAPI 的 JWT 验证
    4. 保持 D-15 SQLite 直连策略一致性——所有只读查询统一走 BFF 层

  **C) app/api/setting/status/route.ts** — GET 系统状态（admin only）
  - 从请求头读取 x-user-role
  - role !== 'admin' → 403
  - 使用 better-sqlite3 查询：
    - brand 总数: SELECT COUNT(*) FROM exhibition_brand
    - edition 总数: SELECT COUNT(*) FROM exhibition_edition
    - 最近采集: SELECT started_at, finished_at, status FROM crawl_log ORDER BY started_at DESC LIMIT 1
  - system_info 保留现有结构（node_version 使用 process.version）
  - 移除 supabase_project_ref（改为 db_type: "SQLite"）
  - 返回 { data_status: {...}, system_info: {...} }
  </action>
  <verify>
    <automated>
      grep -c "FASTAPI_URL\|fetch" app/api/brands/\[id\]/tags/route.ts && grep -c "row_factory\|getDb" app/api/users/route.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - tags/route.ts: `grep -c "FASTAPI_URL\|fetch"` >= 1；`grep -c "supabase"` == 0
    - tags/route.ts: `grep -c "x-user-role"` >= 1（RBAC 检查）
    - users/route.ts: `grep -c "getDb\|better-sqlite3"` >= 1；`grep -c "supabase"` == 0
    - users/route.ts: `grep -c "admin"` >= 1（role 检查）
    - status/route.ts: `grep -c "getDb\|better-sqlite3"` >= 1；`grep -c "supabase"` == 0
    - status/route.ts: `grep -c "crawl_log"` >= 1
  </acceptance_criteria>
  <done>
    tags 路由代理到 FastAPI，users + status 使用 better-sqlite3，全部无 Supabase 引用
  </done>
</task>

<task type="auto">
  <name>Task 3: 清理 Supabase 残留文件</name>
  <files>.env.local</files>
  <read_first>
    @./.env.local (当前——移除 Supabase 密钥行)
  </read_first>
  <action>
  清理不再需要的 Supabase 相关文件和配置：

  1. 删除 lib/supabase/ 目录：
  ```bash
  rm -rf lib/supabase/
  ```

  2. 从 .env.local 中删除以下行（保留 JWT_SECRET、FASTAPI_URL、NEXT_PUBLIC_APP_URL）：
  - NEXT_PUBLIC_SUPABASE_URL
  - NEXT_PUBLIC_SUPABASE_ANON_KEY
  - SUPABASE_SERVICE_ROLE_KEY（如果存在）

  3. 检查无其他文件仍引用 supabase：
  ```bash
  grep -r "supabase" app/ components/ lib/ --include='*.{ts,tsx}' || echo "No supabase references found"
  ```
  如果还有残留引用，记录下来（后续 04-04 的组件更新会移除未覆盖的引用）。
  </action>
  <verify>
    <automated>
      ! test -f lib/supabase/server.ts && ! test -f lib/supabase/client.ts && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `ls lib/supabase/ 2>/dev/null` 返回空（目录不存在）
    - `.env.local` 无 NEXT_PUBLIC_SUPABASE_URL 行
    - `.env.local` 无 NEXT_PUBLIC_SUPABASE_ANON_KEY 行
    - `grep -c "supabase" app/api/` 所有文件 == 0
  </acceptance_criteria>
  <done>
    lib/supabase/ 目录完全移除，.env.local 无 Supabase 密钥，API 代码无 Supabase 引用
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Next.js API -> mwlab.db | BFF 层直接从 SQLite 读取数据 |
| Next.js API -> FastAPI tags | 打标操作代理到 FastAPI（含验证逻辑） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | 所有 API 查询 | mitigate | better-sqlite3 参数化查询（`?` 占位符），禁止字符串拼接 |
| T-04-09 | Tampering | PATCH tags 代理 | mitigate | role 检查（admin/manager only）+ FastAPI 端字段白名单验证 |
| T-04-10 | Information Disclosure | user/status API | mitigate | middleware x-user-role 检查 + 路由内二次验证 role=admin |
</threat_model>

<verification>
1. `grep -r "supabase" app/api/ --include='*.ts' | wc -l` — 结果为 0
2. `grep "getDb\|better-sqlite3" app/api/dashboard/route.ts` — 存在
3. `grep "yearTrend\|GROUP BY e.year" app/api/dashboard/route.ts` — 存在（B1 修复验证）
4. `grep "cityCoords" app/api/map/markers/route.ts` — 存在（坐标映射保留）
5. `grep "FASTAPI_URL" app/api/brands/\[id\]/tags/route.ts` — 存在
6. `ls lib/supabase/ 2>/dev/null; echo $?` — 目录不存在
</verification>

<success_criteria>
- 7 个 API 路由全部无 Supabase 引用
- 只读查询使用 better-sqlite3 直接读取 mwlab.db（BFF 模式）
- **dashboard API 新增 yearTrend 响应**，供 TrendChart 组件使用（B1 修复）
- tags PATCH 代理到 FastAPI tag_api.py
- users + status 使用 better-sqlite3 查询 SQLite user/crawl_log 表
- lib/supabase/ 目录已删除
- .env.local 无 Supabase 密钥
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-03-SUMMARY.md
</output>
