---
phase: 04-frontend-architecture
plan: 06
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - app/calendar/page.tsx
  - components/CalendarView.tsx
  - app/map/page.tsx
  - components/MapView.tsx
  - app/setting/page.tsx
autonomous: true
requirements: ["UI-POOL"]

must_haves:
  truths:
    - "Calendar 页面按月/周视图显示展会排期，事件块绿色样式"
    - "点击展会事件弹出详情弹窗（展会名称/场馆/城市/展商数量）"
    - "Map 页面渲染全球展会地理分布图，按 city 聚合，CircleMarker 热力点"
    - "Setting 页面仅 admin 可访问（middleware 层已保证），显示用户列表和爬虫状态"
    - "Map 和 Calendar 使用 dynamic import + ssr:false 避免 SSR 错误"
  artifacts:
    - path: "app/calendar/page.tsx"
      provides: "Calendar 页面（dynamic import CalendarView）"
      contains: "dynamic"
    - path: "components/CalendarView.tsx"
      provides: "react-big-calendar 封装，含详情弹窗"
      contains: "react-big-calendar"
    - path: "app/map/page.tsx"
      provides: "Map 页面（dynamic import MapView）"
      contains: "dynamic"
    - path: "components/MapView.tsx"
      provides: "Leaflet 地图封装，city 聚合 CircleMarker"
      contains: "react-leaflet"
    - path: "app/setting/page.tsx"
      provides: "admin-only 设置页面（用户列表+爬虫状态）"
      contains: "crawl_log"
  key_links:
    - from: "components/CalendarView.tsx"
      to: "react-big-calendar"
      via: "Calendar + dateFnsLocalizer"
      pattern: "momentLocalizer|dateFnsLocalizer"
    - from: "components/MapView.tsx"
      to: "react-leaflet"
      via: "MapContainer + TileLayer + CircleMarker"
      pattern: "MapContainer"

---

<objective>
Calendar 日历页 + Map 地理分布页 + Setting 系统设置页

Purpose: 实现三个功能页面——展会日历（react-big-calendar 月/周视图）、全球展会地图（react-leaflet 城市聚合热力点）、系统设置页（admin-only 用户列表 + 爬虫日志）。Map 和 Calendar 使用 dynamic import + ssr:false 加载（Leaflet 和 react-big-calendar 是纯客户端组件）。

Output: app/calendar/page.tsx、components/CalendarView.tsx、app/map/page.tsx、components/MapView.tsx、app/setting/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-UI-SPEC.md（CalendarView 第303-329行, MapView 第333-363行, SettingPage 第401-423行, Interaction Contracts Map 第485-491行, Calendar 第495-501行, Copywriting Contract 第104-147行）
@.planning/phases/04-frontend-architecture/04-CONTEXT.md（D-16 Calendar 功能范围, D-17 Map 使用 Leaflet, D-07 Setting admin-only）
@.planning/phases/04-frontend-architecture/04-PATTERNS.md（Leaflet/Calendar dynamic import 第399-440行）

<interfaces>
Plan 04-03 创建组件的导入路径：
- @/components/Sidebar → SidebarNav（已在内布局中）

API 端点（Plan 04-04 创建）：
- GET /api/brands? (for calendar/map data)
- GET /api/users (for setting page)
- GET /crawl_log (for setting page data status)

Plan 04-02 创建文件：
- @/lib/supabase/client → createClient()

UI-SPEC 定义的关键值：
- Calendar: react-big-calendar + date-fns localizer
- Map: react-leaflet MapContainer + TileLayer + CircleMarker + Popup
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 Calendar 日历页面（react-big-calendar）</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/app/calendar/page.tsx
    /Volumes/databoard/AI Project/D_dashboard/components/CalendarView.tsx
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-UI-SPEC.md（CalendarView 第303-329行, Calendar Interaction 第495-501行, Copywriting 第104-147行）
    .planning/phases/04-frontend-architecture/04-PATTERNS.md（react-big-calendar Dynamic Import pattern 第420-440行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Pitfall 5: CSS 导入, 第540-543行）
  </read_first>

  <action>
    （A）创建 components/CalendarView.tsx（"use client"）：

    严格遵循 UI-SPEC CalendarView 合约：

    **导入**：
    ```typescript
    import { Calendar, dateFnsLocalizer } from 'react-big-calendar'
    import { format, parse, startOfWeek, getDay } from 'date-fns'
    import 'react-big-calendar/lib/css/react-big-calendar.css'
    ```

    **localizer**：使用 date-fns（不是 moment）：
    ```typescript
    const locales = { 'zh-CN': require('date-fns/locale/zh-CN') }
    const localizer = dateFnsLocalizer({
      format, parse, startOfWeek: () => startOfWeek(new Date(), { weekStartsOn: 1 }), getDay,
      locales,
    })
    ```

    **数据获取**：
    - 使用 useEffect 在 mount 时从 Supabase 获取所有 exhibition_edition 数据
    - SELECT: edition_id, brand_id, date_start, date_end, year, city, venue, exhibitors_count
    - 同时 JOIN exhibition_brand 获取 name_cn
    - 将数据转换为 react-big-calendar 的 event 格式：{ id, title (name_cn), start (date_start), end (date_end), resource: { city, venue, exhibitors_count } }

    **Props**：无（自包含页面级组件）

    **视图**：month（默认）和 week，使用 Toolbar 切换

    **Event 样式**（自定义 eventPropGetter）：
    - 背景 #DCFCE7
    - border-left: 3px solid #22C55E
    - 字体 #16A34A, text-xs
    - 用 CSS 覆盖默认 rbc-event 样式（在 globals.css 中已定义，确认匹配）

    **详情弹窗**（点击事件时打开）：
    - Overlay: fixed inset-0 bg-black/40 z-50
    - Panel: centered flex items-center justify-center
    - Card: white rounded-xl p-6 max-w-md shadow-xl
    - 字段：展会名称 (name_cn)、场馆 (venue)、城市 (city)、展商数量 (exhibitors_count)
    - 关闭按钮：top-right lucide X 图标，aria-label="关闭"
    - 动画：opacity transition 150ms

    **本地化**：月份/周几使用中文（zh-CN locale）

    （B）创建 app/calendar/page.tsx（dynamic import）：
    ```typescript
    import dynamic from 'next/dynamic'

    const CalendarView = dynamic(() => import('@/components/CalendarView'), {
      ssr: false,
      loading: () => <div className="h-96 bg-gray-100 animate-pulse rounded-xl" />,
    })

    export default function CalendarPage() {
      return (
        <div>
          <h1 className="text-2xl font-semibold text-gray-900 mb-6">日历</h1>
          <div className="bg-white rounded-xl border border-gray-200 p-4">
            <CalendarView />
          </div>
        </div>
      )
    }
    ```
  </action>

  <acceptance_criteria>
    1. `components/CalendarView.tsx` 以 'use client' 开头
    2. 导入 react-big-calendar 的 Calendar 和 dateFnsLocalizer
    3. 导入 react-big-calendar CSS: import 'react-big-calendar/lib/css/react-big-calendar.css'
    4. 使用 date-fns（非 moment）创建 localizer
    5. 事件块样式：绿色背景 + 绿色左边框（eventPropGetter）
    6. 点击事件时弹出详情弹窗，包含 name_cn / venue / city / exhibitors_count
    7. `app/calendar/page.tsx` 使用 dynamic + ssr:false（非直接 import CalendarView）
    8. dynamic loading 使用 animate-pulse skeleton
  </acceptance_criteria>

  <verify>
    <automated>grep -c "react-big-calendar" /Volumes/databoard/AI\ Project/D_dashboard/components/CalendarView.tsx && grep -c "dateFnsLocalizer" /Volumes/databoard/AI\ Project/D_dashboard/components/CalendarView.tsx && grep -c "ssr: false" /Volumes/databoard/AI\ Project/D_dashboard/app/calendar/page.tsx && grep -c "animate-pulse" /Volumes/databoard/AI\ Project/D_dashboard/app/calendar/page.tsx</automated>
  </verify>

  <done>
    Calendar 页面创建完成：react-big-calendar 月/周视图、绿色事件块、点击详情弹窗、dynamic import。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 Map 地理分布页面（react-leaflet）</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/app/map/page.tsx
    /Volumes/databoard/AI Project/D_dashboard/components/MapView.tsx
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-UI-SPEC.md（MapView 第333-363行, Map Interaction 第485-491行）
    .planning/phases/04-frontend-architecture/04-PATTERNS.md（Leaflet/react-leaflet Dynamic Import pattern 第399-418行）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Pitfall 3: Leaflet dynamic import 必须, 第527-530行）
  </read_first>

  <action>
    （A）创建 components/MapView.tsx（"use client"）：

    严格遵循 UI-SPEC MapView 合约：

    **导入**：
    ```typescript
    'use client'
    import { useEffect, useState } from 'react'
    import { MapContainer, TileLayer, CircleMarker, Popup } from 'react-leaflet'
    import 'leaflet/dist/leaflet.css'
    import { createClient } from '@/lib/supabase/client'
    ```

    **数据获取**：
    - 使用 useEffect mount 时从 Supabase 获取所有 exhibition_brand（city + brand_id）
    - SELECT: brand_id, name_cn, city
    - 按 city 字段聚合，计算每个城市的品牌数量
    - 使用预定义的城市坐标映射表（硬编码主要中国城市 + 国际城市坐标）
    - 结果格式：{ city: string, count: number, position: [lat, lng] }[]

    **注意**：由于没有完整的经纬度数据，使用 OpenStreetMap Nominatim API 或预定义城市坐标映射表。简化方案：预定义主要城市的坐标（北京、上海、广州、深圳等 20+ 中国城市 + 主要国际城市），City field 匹配时使用对应坐标。不匹配的城市使用默认中心点或跳过。

    **Map 配置**：
    ```typescript
    <MapContainer center={[35, 105]} zoom={4} className="h-[500px] w-full rounded-xl">
      <TileLayer
        url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
        attribution='&copy; OpenStreetMap contributors'
      />
      {/* cityCluster 数组.map → CircleMarker */}
    </MapContainer>
    ```

    **CircleMarker（每个 city 一个）**：
    - radius: 6 + Math.min(count * 1.5, 20)（min 6px, max 26px）
    - fillColor: #22C55E, fillOpacity: 0.7
    - color: #16A34A, weight: 2

    **Popup**（点击标记时）：
    - 城市名（text-sm font-semibold）
    - "N 个展会"（text-xs text-gray-600）

    **Loading**：数据加载中显示 animate-pulse skeleton

    （B）创建 app/map/page.tsx（dynamic import）：
    ```typescript
    import dynamic from 'next/dynamic'

    const MapView = dynamic(() => import('@/components/MapView'), {
      ssr: false,
      loading: () => <div className="h-[500px] bg-gray-100 animate-pulse rounded-xl" />,
    })

    export default function MapPage() {
      return (
        <div>
          <h1 className="text-2xl font-semibold text-gray-900 mb-6">地图</h1>
          <MapView />
        </div>
      )
    }
    ```
  </action>

  <acceptance_criteria>
    1. `components/MapView.tsx` 以 'use client' 开头
    2. 导入 'leaflet/dist/leaflet.css' 和 react-leaflet 的 MapContainer/TileLayer/CircleMarker/Popup
    3. MapContainer 初始 center=[35, 105], zoom=4, TileLayer 使用 OpenStreetMap
    4. CircleMarker 使用 city 聚合数据，radius 随 count 动态变化
    5. Popup 显示城市名 + "N 个展会"
    6. `app/map/page.tsx` 使用 dynamic + ssr:false
    7. loading 使用 h-[500px] animate-pulse skeleton
  </acceptance_criteria>

  <verify>
    <automated>grep -c "MapContainer" /Volumes/databoard/AI\ Project/D_dashboard/components/MapView.tsx && grep -c "CircleMarker" /Volumes/databoard/AI\ Project/D_dashboard/components/MapView.tsx && grep -c "Popup" /Volumes/databoard/AI\ Project/D_dashboard/components/MapView.tsx && grep -c "ssr: false" /Volumes/databoard/AI\ Project/D_dashboard/app/map/page.tsx</automated>
  </verify>

  <done>
    Map 页面创建完成：react-leaflet 世界地图、城市聚合 CircleMarker 热力点、Popup 显示展會数量、dynamic import。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Setting 系统设置页面（admin-only）</name>
  <files>
    /Volumes/databoard/AI Project/D_dashboard/app/setting/page.tsx
  </files>

  <read_first>
    .planning/phases/04-frontend-architecture/04-UI-SPEC.md（SettingPage 第401-423行, Copywriting Contract 第104-147行, 第142-144行）
    .planning/phases/04-frontend-architecture/04-CONTEXT.md（D-07 Setting admin-only, D-13 角色定义）
    .planning/phases/04-frontend-architecture/04-RESEARCH.md（Pattern 2: 服务端 client, 第298-321行）
  </read_first>

  <action>
    创建 app/setting/page.tsx（"use client"）：

    严格遵循 UI-SPEC SettingPage 合约。注意：middleware.ts（Plan 04-02）已在服务端保证只有 admin 可访问 /setting，此页面 UI 层无需再做角色判断。

    **Section 1 — 用户管理**：
    - 白色圆角卡片（rounded-xl border p-6），heading "用户管理" text-base font-semibold text-gray-900
    - 表格：全宽 text-sm，列：邮箱 / 角色 / 状态
    - 行高 h-11，border-b border-gray-100
    - 角色 badge（pill 样式）：
      - admin: bg-green-100 text-green-700
      - manager: bg-violet-100 text-violet-700
      - readonly: bg-gray-100 text-gray-600
    - 数据来源：fetch('/api/users')（Plan 04-04 创建的端点）
    - Loading: animate-pulse skeleton
    - 空状态："暂无用户数据"

    **Section 2 — 数据更新状态**：
    - 白色圆角卡片（rounded-xl border p-6 mt-6），heading "数据更新状态" text-base font-semibold
    - 数据来源：从 Supabase crawl_log 表直接查询（最近 10 条，按 started_at DESC）
    - 显示字段：source_site / started_at / status / total_inserted
    - Status badge：
      - success: bg-green-100 text-green-700
      - failed: bg-red-100 text-red-700
      - running: bg-blue-100 text-blue-700
      - partial: bg-amber-100 text-amber-700
    - 最后采集时间：显示最新的 started_at 值
    - Loading: 表格行骨架屏

    **数据获取**：
    ```typescript
    // 用户列表
    const userRes = await fetch('/api/users')
    const { users } = await userRes.json()

    // 爬虫日志
    const supabase = createClient()
    const { data: logs } = await supabase
      .from('crawl_log')
      .select('*')
      .order('started_at', { ascending: false })
      .limit(10)
    ```

    注意：/api/users 端点需要 admin 权限（已在 API Route 中验证），如果当前用户不是 admin（但能访问到此页面，说明 middleware 已允许），fetch 会返回 403。这个场景理论上不会发生，但添加 .catch 处理。

    **文案**：所有中文文案使用 UI-SPEC Copywriting Contract（系统设置、用户管理、数据更新状态、最后采集时间等）。
  </action>

  <acceptance_criteria>
    1. `app/setting/page.tsx` 以 'use client' 开头
    2. 页面标题 "系统设置"（text-2xl font-semibold text-gray-900）
    3. Section 1 heading "用户管理"，包含表格列（邮箱/角色/状态）
    4. 角色 badge 三种样式（admin=green, manager=violet, readonly=gray）
    5. Section 2 heading "数据更新状态"
    6. 爬虫日志显示 source_site / started_at / status / total_inserted
    7. Status badge 四种颜色（success/failed/running/partial）
    8. Loading 使用 animate-pulse skeleton
    9. 空状态显示 "暂无用户数据"
  </acceptance_criteria>

  <verify>
    <automated>grep -c "用户管理" /Volumes/databoard/AI\ Project/D_dashboard/app/setting/page.tsx && grep -c "数据更新状态" /Volumes/databoard/AI\ Project/D_dashboard/app/setting/page.tsx && grep -c "/api/users" /Volumes/databoard/AI\ Project/D_dashboard/app/setting/page.tsx && grep -c "crawl_log" /Volumes/databoard/AI\ Project/D_dashboard/app/setting/page.tsx && grep -c "animate-pulse" /Volumes/databoard/AI\ Project/D_dashboard/app/setting/page.tsx</automated>
  </verify>

  <done>
    Setting 页面创建完成：用户管理表格 + 角色 badge + 爬虫日志状态面板 + loading/空状态处理。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Map tile server | 用户浏览器请求 OpenStreetMap tiles（无敏感数据） |
| 浏览器 → /api/users | admin-only 用户列表，middleware + API 双保护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-11 | Information Disclosure | Setting page | accept | middleware 层已在服务端确保只有 admin 可访问 /setting；Setting 页面即使被非 admin 访问到，/api/users 端点也会返回 403。UI 层作为第三道防护。 |
</threat_model>

<verification>
1. ls app/calendar/page.tsx components/CalendarView.tsx app/map/page.tsx components/MapView.tsx app/setting/page.tsx — 5 个文件全部存在
2. CalendarView 使用 react-big-calendar + date-fns localizer
3. MapView 使用 react-leaflet MapContainer + CircleMarker + Popup
4. Calendar 和 Map 页面均使用 dynamic + ssr:false
5. Setting 页面包含用户管理 + 数据更新状态两个 section
</verification>

<success_criteria>
1. Calendar 页面：react-big-calendar 月/周视图，绿色事件块，点击详情弹窗
2. Map 页面：react-leaflet 世界地图，城市聚合 CircleMarker，Popup 显示展会数量
3. Setting 页面：admin-only 用户列表表格 + 爬虫日志状态面板
4. 所有客户端渲染组件正确使用 dynamic import + ssr:false
5. 所有中文字案符合 UI-SPEC Copywriting Contract
</success_criteria>

<output>
完成 Plan 04-06 后，创建 .planning/phases/04-frontend-architecture/04-06-SUMMARY.md
</output>
