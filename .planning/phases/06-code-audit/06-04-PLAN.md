---
phase: 06-code-audit
plan: 04
type: execute
wave: 3
depends_on: []  # 刻意为空：与 06-03 同波并行（零文件重叠），对 wave 1/2 无真实文件依赖，仅按波次调度
files_modified:
  - crawlers/jufair_crawler.py
  - crawlers/cnexpo_crawler.py
autonomous: true
requirements: [AUD-03]
findings: [CRWL-01, CRWL-02, CRWL-03, CRWL-04, CRWL-05, CRWL-06, CRWL-07, CRWL-08, CRWL-09, CRWL-10, CRWL-11, CRWL-12, CRWL-13, CRWL-14, CRWL-15, CRWL-16, CRWL-17]
must_haves:
  truths:
    - "详情页缺字段时不再清空列表页已采集的值"
    - "重跑可补爬 detail_crawled=0 的 4,046 条存量记录的详情"
    - "连续失败达到阈值时整个运行终止，不再对目标站持续轰炸"
    - "单次运行全程使用同一个 UA；429/503 与 403 同等退避"
    - "每次运行在 crawl_log 表留下 running→success/failed/partial 记录"
  artifacts:
    - path: "crawlers/jufair_crawler.py"
      provides: "CRWL-01/02/03/05/06/07/09/10/11/12/13/15/16/17 修复"
    - path: "crawlers/cnexpo_crawler.py"
      provides: "CRWL-03/04/05/06/07/08/09/10/11/12/14/16/17 修复"
  key_links:
    - from: "fetch_page 失败路径"
      to: "全局熔断计数器"
      via: "连续失败 ≥5 终止运行并写 crawl_log status=failed"
    - from: "--detail 模式"
      to: "raw 表 detail_crawled=0 存量行"
      via: "WHERE detail_crawled=0 补爬查询"
---

<objective>
Wave 4 · 爬虫恢复前修复。落实 REVIEW-crawlers 的约 30 行最小改动清单（雷区 5：致封因素前 4 项 + 数据损毁两项）及其余中危项，使爬虫具备恢复采集的前提。**本计划只改代码，不发起任何对目标站的真实请求**——恢复采集是 Phase 1b 的事。

Purpose: 当前代码的请求模式足以解释本次 IP 封禁，且封禁后行为会主动加剧封禁；CRWL-01 的空串覆盖与 CRWL-02 的断点缺失直接命中本次 4,046/8,400 中断现场。
Output: 礼貌、可熔断、可续爬、可观测的两个爬虫。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-audit/AUDIT.md
@.planning/phases/06-code-audit/REVIEW-crawlers.md
@CLAUDE.md

与 06-03 同波并行：文件零重叠（crawlers/ vs tools/intel + skills）。验证全部使用本地 HTML fixture 与内存 SQLite，零外网请求。
</context>

<tasks>

<task type="auto">
  <name>Task 1: 详情页空串覆盖修复 + detail_crawled=0 断点补爬 [CRWL-01][CRWL-02]</name>
  <files>crawlers/jufair_crawler.py</files>
  <action>
1. [CRWL-01] jufair_crawler.py:382-393：UPDATE 字典构造 `updates = {k: extra.get(k, "") for k in [...]}` 改为仅包含 `extra` 中实际存在且非空的键，SQL 动态拼接对应 SET 子句（仍参数化绑定）。详情页缺"展览面积"等字段时，列表页已采集的 area_str/visitors_str/exhibitors_str 保持原值。
2. [CRWL-02] jufair_crawler.py:372,378-379：`--detail` 模式增加存量补爬路径——除本次 new_records 外，查询 `SELECT source_url, ... FROM raw_jufair WHERE detail_crawled=0` 的存量记录并入详情爬取队列（利用既有标志位，非新功能）。成功后置 detail_crawled=1（确认现有代码已置位逻辑，没有则补上）。
  </action>
  <verify>
    <automated>python3 一次性脚本：内存库先插入含 area_str='5万平米' 的行，再模拟 extra 缺 area_str 的详情 UPDATE，断言 area_str 仍为 '5万平米'；插入 detail_crawled=0 的行，断言补爬查询能将其选出</automated>
  </verify>
  <done>空串不再覆盖；4,046 条中断记录重跑可补齐详情</done>
  <commit>fix(crawler): detail update keeps existing values, backfill detail_crawled=0 records [CRWL-01][CRWL-02]</commit>
</task>

<task type="auto">
  <name>Task 2: 反爬指纹与退避修复：固定 UA、429/503 退避、403 计数器保留、批间长休止 [CRWL-05][CRWL-09][CRWL-11]</name>
  <files>crawlers/jufair_crawler.py, crawlers/cnexpo_crawler.py</files>
  <action>
按 REVIEW-crawlers 最小改动清单，两个爬虫同步修改：
1. [CRWL-09] 删除每请求 `_rotate_ua` 调用（jufair:72、cnexpo:66），改为运行启动时 random.choice 选定一个 UA 并全程固定（Session Cookie 与 UA 指纹一致）。
2. [CRWL-05a] fetch_page 中 429/503 并入现有 403 退避分支，响应含 `Retry-After` 头时尊重该值（取 max(Retry-After, 计划退避秒数)）。
3. [CRWL-05b] jufair 的 200 响应软封禁检测：页面无 `.exh-info-wrap` 且无翻页结构时，按异常处理（计入失败、打印 [WARN] 疑似验证码/封禁页）而非"无2026年数据，停止"静默结束（jufair:348-350 与 265-267 的 has_target_year 误判路径）。
4. [CRWL-11] 删除"单 URL 三次 403 放弃时 `_consecutive_403 = 0`"（jufair:91、cnexpo:79），计数器仅在收到 200 时清零（jufair:79 已有该逻辑），使跨 URL 退避真正升级。
5. [最小清单·长休止] fetch_page 内加请求计数，每 50 个请求 sleep random.uniform(60,120) 秒并打印休止日志。
  </action>
  <verify>
    <automated>grep -n "_rotate_ua" crawlers/*.py 在请求路径中无调用（仅定义可保留或删除）；grep -n "429\|503" crawlers/jufair_crawler.py crawlers/cnexpo_crawler.py 各命中退避分支；python3 -c "import ast; ast.parse(open('crawlers/jufair_crawler.py').read()); ast.parse(open('crawlers/cnexpo_crawler.py').read()); print('ok')"</automated>
  </verify>
  <done>单次运行单一 UA；429/503/Retry-After 被尊重；退避可跨 URL 升级；每 50 请求有 60-120s 长休止；200 验证码页不再被误判为数据自然结束</done>
  <commit>fix(crawler): fixed UA per run, 429/503 backoff with Retry-After, persistent 403 counter, batch pauses [CRWL-05][CRWL-09][CRWL-11]</commit>
</task>

<task type="auto">
  <name>Task 3: 全局熔断 + cnexpo continue 前补 sleep [CRWL-03]</name>
  <files>crawlers/jufair_crawler.py, crawlers/cnexpo_crawler.py</files>
  <action>
1. 两爬虫增加全局连续失败计数器：每个 URL 全部重试失败时 +1，任一 URL 成功时清零；连续失败 ≥5 时打印 [ABORT] 并终止整个运行（jufair 的 crawl_all:417-422 不再继续下一个月/类型；cnexpo:331-333 不再继续剩余链接），终止时走 Task 4 的 crawl_log failed/partial 收尾。
2. cnexpo_crawler.py:331-333：`if not detail: continue` 之前补 `time.sleep(_jitter_delay())`（1 行），消除封禁期间零间隔连发。
  </action>
  <verify>
    <automated>python3 一次性脚本：monkeypatch fetch_page 恒返回 None，调用主循环，断言发起的 fetch 调用次数 ≤ 5×重试数（熔断生效）而非全量目标数</automated>
  </verify>
  <done>被封后最多再发 5 个 URL 的请求即停；cnexpo 失败路径不再跳过 sleep</done>
  <commit>fix(crawler): global circuit breaker after 5 consecutive failures, sleep before cnexpo continue [CRWL-03]</commit>
</task>

<task type="auto">
  <name>Task 4: crawl_log 写入 + 失败汇总与非零退出 + insert_batch 真实计数 [CRWL-06][CRWL-07][CRWL-10]</name>
  <files>crawlers/jufair_crawler.py, crawlers/cnexpo_crawler.py</files>
  <action>
1. [CRWL-06] 两爬虫运行开始时向 crawl_log（schema/init_db.sql:81-94 已定义）INSERT 一行 status='running'（batch_id、started_at）；结束/异常/熔断时 UPDATE 为 success / failed / partial 并写 finished_at、total_fetched、total_inserted。用 try/finally 保证异常路径也收尾。
2. [CRWL-07] 累计失败计数；运行结束失败数 >0 时打印 "⚠ 部分失败 N 条" 而非"✅ 任务完成"（jufair:587-590、cnexpo:499-502），并 sys.exit(1)（全部失败/熔断）或 exit(2)（partial），成功才 exit(0)。
3. [CRWL-10] insert_batch（jufair:158-177、cnexpo:137-155）返回值改用 `conn.total_changes` 差值，报告真实插入数而非提交条数。
  </action>
  <verify>
    <automated>python3 一次性脚本：内存库跑一次含重复 URL 的 insert_batch，断言返回值等于真实新增行数；模拟失败运行断言 crawl_log 终态为 failed 且进程退出码非 0</automated>
  </verify>
  <done>每次运行在 crawl_log 可查询始末与计数；失败可被退出码感知；新增统计不再虚高</done>
  <commit>fix(crawler): write crawl_log lifecycle, non-zero exit on failures, real insert counts [CRWL-06][CRWL-07][CRWL-10]</commit>
</task>

<task type="auto">
  <name>Task 5: cnexpo 详情解析标签锚定 + 日期正则放宽 [CRWL-04][CRWL-14]</name>
  <files>crawlers/cnexpo_crawler.py</files>
  <action>
1. [CRWL-04] cnexpo_crawler.py:231-247：弃用 paragraphs[2..5] 位置索引，改为按标签文本锚定提取（遍历 `<p>`，按"时间/日期"、"地点/展馆"、"主办"等关键词与内容特征匹配，参照 jufair 的标签匹配写法）；venue 写入前校验文本含"馆/中心/会展/广场"等场馆关键词之一，不满足则置空并打印 [WARN]——宁缺勿错。
2. [CRWL-14] cnexpo_crawler.py:232：日期正则从仅 `\d{4}.\d{2}.\d{2} - \d{2}.\d{2}` 放宽，覆盖单日（`\d{4}.\d{2}.\d{2}` 无区间）与全写结束日期（`... - \d{4}.\d{2}.\d{2}`）两种真实格式；删除 :295-297 注释中不存在的"按年份过滤"承诺（注释与代码对齐，不新增过滤逻辑——全年份入库是现状行为，保持）。
  </action>
  <verify>
    <automated>python3 一次性脚本：用三个本地 HTML fixture（正常版式 / 多插一个公告 p / 单日日期）调用 parse_detail_page，断言：错位版式下 venue 为空而非无关文本；单日与全写日期均解析成功</automated>
  </verify>
  <done>版式微变不再静默写脏数据；三种日期格式可解析；注释不再承诺不存在的行为</done>
  <commit>fix(crawler): label-anchored cnexpo detail parsing with venue validation, widen date regex [CRWL-04][CRWL-14]</commit>
</task>

<task type="auto">
  <name>Task 6: 关键词过滤落库 + 编码兜底 + 代理失败即退出 [CRWL-08][CRWL-12][CRWL-13]</name>
  <files>crawlers/jufair_crawler.py, crawlers/cnexpo_crawler.py</files>
  <action>
1. [CRWL-08] cnexpo_crawler.py:336-341：keyword 不匹配的记录同样落库（数据本就全字段抓回，过滤本意是给下游用——加 print 说明"不匹配关键词，已入库供下游过滤"），消除重复运行对同一详情页的反复请求。
2. [CRWL-12] 两爬虫 fetch_page（jufair:80、cnexpo:70）：响应头未声明 charset 时设置 `resp.encoding = resp.apparent_encoding`，防 GBK 页面乱码静默入库。
3. [CRWL-13] jufair_crawler.py:576-578：--proxy 指定但 Tor 验证失败时，打印错误并 sys.exit(1)，不再静默回退直连（被封 IP 裸连会加剧封禁）。
  </action>
  <verify>
    <automated>python3 一次性脚本：mock 不匹配关键词的详情结果，断言仍 INSERT；mock 无 charset 响应断言 encoding 被设为 apparent_encoding；jufair --proxy 验证失败路径断言 SystemExit(1)</automated>
  </verify>
  <done>关键词重跑不再放大请求量；中文编码有兜底；代理失败不再静默直连</done>
  <commit>fix(crawler): persist non-keyword records, apparent_encoding fallback, exit on proxy failure [CRWL-08][CRWL-12][CRWL-13]</commit>
</task>

<task type="auto">
  <name>Task 7: 列表页统计单位校验 + TARGET_YEAR 参数化 + 时间戳日志 [CRWL-15][CRWL-16][CRWL-17]</name>
  <files>crawlers/jufair_crawler.py, crawlers/cnexpo_crawler.py</files>
  <action>
1. [CRWL-15] jufair_crawler.py:233-240：scale_divs 位置取数前用 `.unitText` 单位文本校验字段归属（平方米→area、人→visitors、家→exhibitors），单位不符则该字段置空并 [WARN]，不再盲按索引。
2. [CRWL-16] jufair:25 TARGET_YEAR 提为 `--year` CLI 参数（默认 2026）；jufair:337 `/exhibition-0-0-{type}-0-0-{month}-` 与 cnexpo:303 `/events/1000/0/` 魔法 URL 模式各加一行注释说明字段含义。不做其它配置化。
3. [CRWL-17] 两爬虫各加一个 `log(msg)` 函数（print 前缀 `[YYYY-mm-dd HH:MM:SS]`），全文 print 调用替换为 log（最小改动方案，不引入 logging 框架）。事后取证可回答"封禁发生在几点"。
  </action>
  <verify>
    <automated>python3 crawlers/jufair_crawler.py --help 显示 --year 参数；python3 一次性脚本断言单位错位的 fixture 下 area_str 为空且有 WARN；运行任一 dry 路径输出行首含时间戳</automated>
  </verify>
  <done>统计字段归属有单位校验；跨年使用不改源码；全部输出带时间戳</done>
  <commit>fix(crawler): unit-validated stats parsing, --year param, timestamped logging [CRWL-15][CRWL-16][CRWL-17]</commit>
</task>

<task type="auto">
  <name>Task 8: 运行测试套件收尾</name>
  <files>(无修改)</files>
  <action>运行 `pytest tests/ -x` 确认无回归（爬虫此前零测试，本计划的行为断言以一次性脚本完成，正式 fixture 回归测试在 06-05 落档）。两文件 `python3 -m py_compile` 通过。**再次确认：本计划全程未对 jufair/cnexpo 发起真实请求。**</action>
  <verify>
    <automated>pytest tests/ -x 退出码 0；python3 -m py_compile crawlers/jufair_crawler.py crawlers/cnexpo_crawler.py 退出码 0</automated>
  </verify>
  <done>套件全绿；爬虫解除"恢复采集前禁改"状态（SUMMARY 中声明，实际恢复采集由 Phase 1b 决定）</done>
  <commit>test(crawler): suite green after wave-4 crawler fixes</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 目标站 HTML → 解析器 | 不可信版式进入字段提取（CRWL-04/15） |
| 爬虫 → 目标站 | 请求节奏与指纹决定封禁风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Tampering | 详情页空串覆盖已采数据 | mitigate | 非空键 UPDATE（Task 1） |
| T-06-14 | Denial of Service | 封禁后持续轰炸→长期封禁 | mitigate | 熔断 + 退避 + 长休止（Task 2/3） |
| T-06-15 | Tampering | venue 兜底正则写入无关文本 | mitigate | 标签锚定 + 关键词校验（Task 5） |
| T-06-16 | Repudiation | 批次无任何运行记录 | mitigate | crawl_log 生命周期写入（Task 4） |
</threat_model>

<verification>
- 全部一次性行为断言通过（空串不覆盖、熔断、真实计数、解析 fixture、退出码）
- 两文件编译通过、pytest 全绿
- 零外网请求
</verification>

<success_criteria>
CRWL-01~17 共 17 项关闭；REVIEW-crawlers"恢复爬取前完成"清单（前 4 项致封因素 + 数据损毁）全部落实。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-code-audit/06-04-SUMMARY.md`，须声明：爬虫已具备恢复采集前提，但真实采集恢复属 Phase 1b 范围。
</output>
