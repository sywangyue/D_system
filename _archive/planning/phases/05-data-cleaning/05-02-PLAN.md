---
phase: 05-data-cleaning
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/clean_brands.py
  - scripts/data/jufair_l2_crawler.py
  - tests/test_clean_brands.py
autonomous: false
requirements:
  - CLEAN-MDS
  - CLEAN-JUFAIR-L2
user_setup:
  - service: jufair.com
    why: "CLEAN-JUFAIR-L2 二级分类爬取"
    env_vars: []
    dashboard_config:
      - task: "在 Mac Mini 上运行 `python3 scripts/clean_brands.py jufair-l2 --export jufair_cats.json` 输出分类 JSON"
        location: "Mac Mini（大陆 IP）终端"

must_haves:
  truths:
    - "mds 子命令执行后：杜塞境外展 Excel 匹配到的品牌 mds_related=1"
    - "mds 子命令执行后：Excel 中不存在于 DB 的 MD 品牌已 INSERT 到 exhibition_brand"
    - "jufair-l2 子命令可生成爬虫脚本，在 Mac Mini 输出 JSON 分类文件"
    - "jufair-l2 --import 可将 JSON 分类模糊匹配到 exhibition_brand，标注 industry_l1 + industry_l2"
  artifacts:
    - path: "scripts/clean_brands.py"
      provides: "完善的主脚本，增加 mds + jufair-l2 子命令"
      contains: "def cmd_mds"
    - path: "scripts/data/jufair_l2_crawler.py"
      provides: "jufair.com 二级分类爬虫，输出 JSON"
      exports: ["crawl_jufair_categories", "JUFAIR_PARENT_CATEGORIES"]
    - path: "tests/test_clean_brands.py"
      provides: "追加 CLEAN-MDS + CLEAN-JUFAIR-L2 单元测试"
      contains: "test_parse_md_excel"
  key_links:
    - from: "cmd_mds"
      to: "杜塞境外展时间表_for update_2026.xlsx"
      via: "openpyxl.load_workbook with data_only=True"
    - from: "cmd_jufair_l2 (--export)"
      to: "jufair.com/exhibition-{parentId}-{subId}-..."
      via: "requests + BeautifulSoup"
    - from: "cmd_jufair_l2 (--import)"
      to: "exhibition_brand.industry_l1 + industry_l2"
      via: "fuzzy matching with difflib.SequenceMatcher"
---

<objective>
实现 mds 和 jufair-l2 两个子命令，完成全部四项数据清洗功能。

Purpose:
- mds：匹配杜塞境外展 Excel 文件中的展会到 exhibition_brand，标记 mds_related=1，缺失展会补充入库
- jufair-l2：提供 jufair.com 二级分类爬虫脚本，支持 --export（在 Mac Mini 爬取）和 --import（本机模糊匹配）

Output:
- scripts/clean_brands.py — 追加 mds + jufair-l2 完整子命令
- scripts/data/jufair_l2_crawler.py — 独立爬虫模块
- tests/test_clean_brands.py — 追加测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-data-cleaning/05-RESEARCH.md
@.planning/phases/05-data-cleaning/05-VALIDATION.md
@scripts/clean_brands.py
@scripts/data/name_en_patterns.py
@scripts/data/md_category_rules.py
@schema/init_db.sql
@tests/conftest.py
@merge_engine.py
@tools/export_for_tagging.py
@crawlers/jufair_crawler.py

<interfaces>
From scripts/clean_brands.py (created in Plan 1):
- backup_table(conn: sqlite3.Connection) — 创建快照
- cmd_name_en(args), cmd_industry(args) — 已实现的子命令

From merge_engine.py (existing fuzzy matching pattern):
```python
import difflib
ratio = difflib.SequenceMatcher(None, text_a, text_b).ratio()
```

From crawlers/jufair_crawler.py (existing request pattern):
```python
import requests
from bs4 import BeautifulSoup
resp = requests.get(url, headers=HEADERS, timeout=30)
soup = BeautifulSoup(resp.text, 'html.parser')
```

From tools/export_for_tagging.py (existing openpyxl pattern):
```python
import openpyxl
wb = openpyxl.load_workbook(filepath, data_only=True)
ws = wb.active
```

Excel column layout (verified from actual file):
- Row 1-2: Title/metadata
- Row 3: Headers (序号, 类别, 杜塞全球展会母展, 日期, 卫星展-CN, 卫星展-EN, 地点, 展会日期下届)
- Row 4-72: Data (68-69 data rows)
- Col B: Category (类别) — one of 6 MD categories
- Col C: Parent exhibition name (母展) — merged cells, carry forward
- Col E: Satellite show CN name (卫星展-CN)
- Col F: Satellite show EN name (卫星展-EN)
- Col G: Location
- Col H: Next date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现 mds 子命令 — Excel 解析 + 品牌匹配 + 标记/补充</name>
  <files>
    scripts/clean_brands.py
    tests/test_clean_brands.py
  </files>
  <read_first>
    scripts/clean_brands.py — 现有骨架，需添加 mds 子命令
    schema/init_db.sql — exhibition_brand 完整 DDL（INSERT 时需所有字段）
    tools/export_for_tagging.py — 项目内 openpyxl 使用模式
  </read_first>
  <action>
    1. 在 `scripts/clean_brands.py` 中实现 `cmd_mds(args)`：
       a. 添加 import：`import openpyxl`，`from pathlib import Path`
       b. Excel 路径硬编码：`EXCEL_PATH = BASE_DIR / "杜塞境外展时间表_for update_2026.xlsx"`
       c. 读取函数 `parse_md_excel(filepath) -> list[dict]`：
          - `openpyxl.load_workbook(filepath, data_only=True)` 防止宏注入
          - 从第 4 行开始遍历到 max_row（第 1-2 行元数据，第 3 行表头）
          - 跟踪 `current_cat`（Col B）— 遇到非空值更新
          - 跟踪 `current_parent_cn`（Col C）— 遇到非空值更新
          - 读取卫星展 CN（Col E）、卫星展 EN（Col F）、地点（Col G）、下届日期（Col H）
          - 每条记录格式：`{"category": str, "parent_cn": str, "parent_en": str（从父展名提取, "sat_cn": str, "sat_en": str, "location": str, "next_date": str}`
          - 父展英文名提取规则：从 C 列字符串末尾提取括号内英文（如 "德国德鲁巴国际印刷展览会 drupa" → "drupa"），若无则留空
       d. 品牌匹配函数 `match_brand_multistrategy(conn, name_cn, name_en, threshold=0.80) -> str | None`：
          - Strategy 1（首选）：`SELECT brand_id FROM exhibition_brand WHERE name_en = ?`（精确匹配英文名）
          - Strategy 2：`SELECT brand_id FROM exhibition_brand WHERE name_cn = ?`（精确匹配中文名）
          - Strategy 3：`SELECT brand_id FROM exhibition_brand WHERE name_cn LIKE ?`（前 10 字子串匹配）
          - Strategy 4：`SELECT brand_id FROM exhibition_brand WHERE organizer LIKE '%杜塞尔%'`（主办方含杜塞）
          - Strategy 5：difflib.SequenceMatcher 对所有 name_cn 做模糊匹配，阈值 0.80，最小字符串长度 >= 6
          - 返回 brand_id 或 None
       e. 对 Excel 每条记录：
          - 优先匹配卫星展（sat_cn + sat_en），再匹配父展（parent_cn）
          - 匹配成功 → `UPDATE exhibition_brand SET mds_related = ? WHERE brand_id = ?`（值设为 category，如 "机械和设备"）
          - 匹配失败且为父展 → 准备 INSERT 新品牌
       f. INSERT 新品牌逻辑：
          - 生成 brand_id：`EXPO-{uuid4().hex[:8].upper()}`
          - 填入 name_cn = parent_cn, name_en = parent_en（若空则调用 generate_name_en 生成）
          - 填入 industry_l1 = 匹配的 category
          - mds_related = category（与 mds_related 值相同）
          - 其他字段留空
          - 打印警告："NEW BRAND (needs review): {name_cn}"
       g. 打印统计：总行数、已匹配行数、新品牌数、未匹配行数
       h. 使用 --dry-run 模式（不执行 UPDATE/INSERT）
    2. 在 `tests/test_clean_brands.py` 追加测试：
       - `test_parse_md_excel()` — 使用 openpyxl 读取实际 Excel 文件，验证返回 list 且每行包含 category, parent_cn, sat_cn, sat_en
       - `test_match_brand_multistrategy()` — 使用 conftest 的 exhibition_brand_conn fixture，创建 3 条已知品牌，测试精确匹配/子串匹配/无匹配
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 scripts/clean_brands.py mds --dry-run 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cmd_mds" scripts/clean_brands.py` 返回值 >= 2（定义 + 注册）
    - `grep -c "openpyxl" scripts/clean_brands.py` 返回值 >= 1
    - `grep -c "data_only=True" scripts/clean_brands.py` 返回值 >= 1
    - `python3 scripts/clean_brands.py mds --dry-run` 输出包含 Excel 行数、匹配数、新品牌数、未匹配数
    - `python3 -m pytest tests/test_clean_brands.py::test_parse_md_excel tests/test_clean_brands.py::test_match_brand_multistrategy -x -q` 全部通过
  </acceptance_criteria>
  <done>
    mds 子命令完整，可读取 Excel、匹配品牌、标记 mds_related 并补充缺失品牌
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 jufair.com 二级分类爬虫模块</name>
  <files>
    scripts/data/jufair_l2_crawler.py
    tests/test_clean_brands.py
  </files>
  <read_first>
    crawlers/jufair_crawler.py — 现有 jufair 爬虫请求/超时/错误处理模式
  </read_first>
  <action>
    1. 创建 `scripts/data/jufair_l2_crawler.py`：
       a. 导入：`requests`, `bs4.BeautifulSoup`, `json`, `re`, `time`, `logging`
       b. `JUFAIR_BASE = "https://www.jufair.com"`
       c. `HEADERS` 字典：从 crawlers/jufair_crawler.py 复制 User-Agent 和 Cookie 模式
       d. `JUFAIR_PARENT_CATEGORIES` 列表（从 RESEARCH.md 确认的 18 个父分类硬编码）：
          ```python
          # 从 jufair.com 首页提取的 18 个一级分类
          # 注意：这些是 jufair 的行业分类，与 MD 6 类别不同
          # 在匹配阶段再建立两个分类体系之间的映射
          ```
          - RESEARCH.md 未列出完整的 18 个分类名称，因此在这里使用可扩展的设计：
            `JUFAIR_PARENT_CATEGORIES = []  # 留空，运行时通过 crawl_parent_categories() 动态获取`
          - `crawl_parent_categories() -> list[dict]`：请求 jufair.com 首页，从 DOM 提取父分类名称和 URL，返回 `[{"name": str, "url": str, "parent_id": str}]`
          - 若爬取失败，回退到 `HARDCODED_PARENTS`（从爬虫已有数据或测试时手动填入的兜底值）
       e. `crawl_subcategories(parent_url: str, parent_id: str) -> list[dict]`：
          - URL 模式：`/exhibition-{parentId}-{subId}-0-0-0-0-1/`
          - 请求分类页面，提取子分类名称和 ID
          - 返回 `[{"name": str, "sub_id": str, "parent_id": str}]`
       f. `crawl_jufair_categories() -> dict`：
          - 先调 `crawl_parent_categories()` 获取所有父分类
          - 对每个父分类调 `crawl_subcategories()` 获取子分类
          - 返回 `{"parent_categories": [...], "subcategories": [...], "crawled_at": "ISO datetime"}`
          - 带 3 次重试（与 crawlers/jufair_crawler.py 的模式一致），请求间隔 1 秒
       g. `export_categories(data: dict, filepath: str)` — 写入 JSON
       h. `load_categories(filepath: str) -> dict` — 从 JSON 读取
    2. 在 `tests/test_clean_brands.py` 追加测试：
       - `test_jufair_url_pattern()` — 验证 URL 生成逻辑正确
       - `test_export_import_categories()` — 验证 export → JSON → import 的 round-trip
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 -m pytest tests/test_clean_brands.py::test_jufair_url_pattern tests/test_clean_brands.py::test_export_import_categories -x -q 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - scripts/data/jufair_l2_crawler.py 存在且导出 crawl_jufair_categories, export_categories, load_categories
    - `grep -c "requests.get" scripts/data/jufair_l2_crawler.py` 返回值 >= 1
    - `grep -c "BeautifulSoup" scripts/data/jufair_l2_crawler.py` 返回值 >= 1
    - pytest 测试全部通过
  </acceptance_criteria>
  <done>
    jufair 爬虫模块创建完成，可在 Mac Mini 独立运行输出 JSON
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: 实现 jufair-l2 子命令（导入 + 模糊匹配）+ 用户部署 Mac Mini 爬取</name>
  <files>
    scripts/clean_brands.py
  </files>
  <read_first>
    scripts/clean_brands.py — 现有骨架，添加 jufair-l2 子命令
    scripts/data/jufair_l2_crawler.py — 爬虫模块接口
  </read_first>
  <action>
    1. 在 `scripts/clean_brands.py` 中实现 `cmd_jufair_l2(args)`：
       a. 添加 import：`from scripts.data.jufair_l2_crawler import crawl_jufair_categories, export_categories, load_categories`
       b. 添加 `--export` 参数（默认 None，输出 JSON 文件路径）
       c. 添加 `--import` 参数（默认 None，输入 JSON 文件路径）
       d. 添加 `--threshold` 参数（默认 0.80，模糊匹配阈值）
       e. 逻辑：
          - 如果 `args.export`：调 `crawl_jufair_categories()` → `export_categories()` → 打印完成信息
          - 如果 `args.import`：调 `load_categories()` → 对 exhibition_brand 做模糊匹配
          - 模糊匹配算法：
            * 遍历 exhibition_brand 所有行，取 name_cn
            * 对每个 jufair 子分类名称，使用 difflib.SequenceMatcher 与 name_cn 比较
            * 最佳匹配超过 threshold 则记录 `{brand_id, industry_l1=parent_cat_name, industry_l2=sub_cat_name, match_score}`
            * 低于 threshold 的匹配输出到 "needs_review.csv"（含 brand_id, name_cn, suggested_l1, suggested_l2, score）
            * 执行 UPDATE exhibition_brand SET industry_l1=?, industry_l2=? WHERE brand_id=?
            * 注意：jufair 分类名称与 MD 6 类别不同，industry_l1 存储 jufair 父分类名（后续可手动映射到 MD 类别）
          - 打印统计：总行数、匹配行数、需人工复核行数、未匹配行数
       f. 注册 jufair-l2 subparser

    2. 用户部署说明（任务完成后打印消息）：
       - "jufair-l2 爬取需在大陆 IP 环境执行。请将 clean_brands.py + jufair_l2_crawler.py 复制到 Mac Mini"
       - "在 Mac Mini 运行: python3 scripts/clean_brands.py jufair-l2 --export jufair_cats.json"
       - "将输出的 jufair_cats.json 复制回本机"
       - "运行: python3 scripts/clean_brands.py jufair-l2 --import jufair_cats.json"
  </action>
  <verify>
    <automated>cd /Volumes/databoard/AI\ Project/D_dashboard && python3 scripts/clean_brands.py jufair-l2 --help 2>&1 | grep -q "export\|import" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cmd_jufair_l2" scripts/clean_brands.py` 返回值 >= 2（定义 + 注册）
    - `python3 scripts/clean_brands.py jufair-l2 --help` 显示 --export 和 --import 参数
    - `grep -c "difflib.SequenceMatcher" scripts/clean_brands.py` 返回值 >= 1（模糊匹配）
    - `python3 scripts/clean_brands.py jufair-l2 --import nonexistent.json 2>&1 | grep -i "error\|not found"` — 文件不存在的错误处理
  </acceptance_criteria>
  <done>
    jufair-l2 子命令完成，用户在 Mac Mini 执行爬取后可通过 --import 导入匹配
  </done>
  <what-built>
    1. `python3 scripts/clean_brands.py jufair-l2 --export <output.json>` — 可在 Mac Mini 执行，爬取 jufair.com 分类结构输出 JSON
    2. `python3 scripts/clean_brands.py jufair-l2 --import <input.json> --threshold 0.80` — 导入 JSON，模糊匹配到 exhibition_brand，标注 industry_l1 + industry_l2
  </what-built>
  <how-to-verify>
    1. 将 scripts/clean_brands.py 和 scripts/data/jufair_l2_crawler.py 复制到 Mac Mini（大陆 IP）
    2. 在 Mac Mini 运行: `python3 scripts/clean_brands.py jufair-l2 --export jufair_cats.json`
    3. 确认输出 jufair_cats.json 包含 parent_categories 和 subcategories
    4. 将 jufair_cats.json 复制回开发机
    5. 运行: `python3 scripts/clean_brands.py jufair-l2 --import jufair_cats.json --dry-run`
    6. 确认预览输出显示匹配统计
    7. 运行正式导入: `python3 scripts/clean_brands.py jufair-l2 --import jufair_cats.json`
    8. 查询 DB 确认 industry_l1 和 industry_l2 已更新
  </how-to-verify>
  <resume-signal>
    完成 Mac Mini 爬取并导入 JSON 后，确认执行结果
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/clean_brands.py → mwlab.db | 本地 SQLite 写操作，同一信任域 |
| scripts/clean_brands.py → 杜塞境外展时间表.xlsx | 外部 Excel 文件解析 |
| scripts/data/jufair_l2_crawler.py → jufair.com | 外部网站 HTTP 请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | SQL UPDATE/INSERT | mitigate | 所有 SQL 使用参数化查询（`conn.execute(sql, (val,))`），禁止 f-string 拼接 |
| T-05-02 | Spoofing | Excel 宏注入 | mitigate | openpyxl 始终使用 `data_only=True`，不执行宏 |
| T-05-03 | Information Disclosure | Jufair HTTP 请求 | accept | 同现有爬虫模式，无可识别个人信息传输 |
| T-05-04 | Repudiation | INSERT 新品牌 | mitigate | --dry-run 预览后再执行；backup_table() 在 INSERT 前快照 |
</threat_model>

<verification>
1. `python3 scripts/clean_brands.py mds --dry-run` 输出 Excel 匹配统计
2. `python3 scripts/clean_brands.py jufair-l2 --help` 显示 export/import 参数
3. `python3 -m pytest tests/test_clean_brands.py -x -q` 全部测试通过
4. 用户在 Mac Mini 完成 jufair 爬取后，`--import` 可正常匹配
</verification>

<success_criteria>
- [ ] mds 子命令完整：Excel 解析、品牌匹配、mds_related 更新、新品牌补充
- [ ] jufair_l2_crawler.py 可独立运行爬取（Mac Mini）
- [ ] jufair-l2 子命令支持 --export 和 --import
- [ ] 全部测试通过（包括新追加的 mds 和 jufair-l2 测试）
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-cleaning/05-02-SUMMARY.md`
</output>
