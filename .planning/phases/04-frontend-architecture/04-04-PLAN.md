---
phase: 04-frontend-architecture
plan: 04
type: execute
wave: 0
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/ui/KpiCard.tsx
  - components/ui/FilterTabs.tsx
  - components/ui/TrendBadge.tsx
  - components/charts/IndustryPieChart.tsx
  - components/calendar/EventModal.tsx
  - components/map/Legend.tsx
autonomous: true
requirements: [UI-POOL]
must_haves:
  truths:
    - "MD Orange (#fe5c00) 替代 Green (#22C55E) 作为全局主色"
    - "所有 CSS 变量使用 --md-* 前缀（MD 品牌规范）"
    - "Inter 字体全局加载"
    - "KPI 卡片使用高对比度大号数字 + 科技感样式"
    - "FilterTabs 选中态使用 MD Orange 颜色"
    - "TrendBadge 上升/下降使用 MD 色系"
    - "IndustryPieChart 使用 MD 色板（orange/magenta/red/yellow）替代旧绿色系"
    - "全局无绿色（#22C55E）残留"
  artifacts:
    - path: "app/globals.css"
      provides: "MD 品牌设计令牌 + 科技感基础样式"
      contains: "--color-md-orange: #fe5c00, --md-* tokens, glass-card class"
    - path: "app/layout.tsx"
      provides: "MD 品牌根布局"
      contains: "Inter font import via next/font"
  key_links:
    - from: "globals.css" to: "layout.tsx" via: "--font-inter CSS variable"
    - from: "KpiCard.tsx" to: "globals.css" via: "theme() / var(--color-*)"
---

<objective>
全面替换品牌样式从 Hirezy 绿色到 Messe Dusseldorf Corporate Design Manual 橙色系（D-01, D-02, D-04）。更新所有 UI 组件为科技感风格（D-09）——玻璃态卡片、高对比度、MD Orange 选中态。

Purpose: 实现 MD 品牌规范集成（D-01 色板、D-02 字体、D-09 科技感、D-10 Pill 过滤）
Output: 1 个 CSS 文件 + 1 个布局 + 6 个 UI 组件更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (MD 品牌设计令牌 lines 128-206, globals.css 代码示例 lines 856-917, KpiCard 科技感版本 lines 920-954, 反模式 Pitfall 2 lines 720-726)
@./app/globals.css (当前绿色主题——完整重写)
@./app/layout.tsx (当前布局——更新 Inter font)
@./components/ui/KpiCard.tsx (绿色系——改用 MD Orange)
@./components/ui/FilterTabs.tsx (绿色选中态——改用 MD Orange)
@./components/ui/TrendBadge.tsx (绿色趋势——改用 MD 色系)
@./components/charts/IndustryPieChart.tsx (绿色系色板——改用 MD 色板)
</context>

<interfaces>
从 RESEARCH.md 提取的 MD 品牌令牌：

```css
/* MD 品牌色板 — 从 CD Manual PDF 提取 [VERIFIED] */
--color-md-orange: #fe5c00;
--color-md-orange-dark: #e55300;
--color-md-red: #FF3400;
--color-md-magenta: #e60070;
--color-md-light-orange: #ff8c00;
--color-md-yellow: #ffc500;
--color-md-grey: #9c9c9c;

/* 语义 Token（映射到组件） */
--color-accent: var(--color-md-orange);
--color-accent-dark: var(--color-md-orange-dark);
--color-accent-surface: #fff3ec;     /* MD Orange 10% 透明度 */
```

字体规范：
```css
--font-family: 'Inter', 'Arial', ui-sans-serif, system-ui, -apple-system, sans-serif;
```

玻璃态卡片：
```css
.glass-card {
  background: rgba(255, 255, 255, 0.7);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: 12px;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 重写 globals.css 为 MD 品牌令牌 + 科技感样式</name>
  <files>app/globals.css</files>
  <read_first>
    @./app/globals.css (当前绿色主题——完整重写)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md lines 856-917 (globals.css 代码示例 brand tokens + glass card)
  </read_first>
  <action>
  完整重写 /Volumes/databoard/AI Project/D_dashboard/app/globals.css：

  ```css
  @import "tailwindcss";

  @theme {
    /* MD 品牌色板 — 从 CD Manual PDF 提取 */
    --color-md-orange: #fe5c00;
    --color-md-orange-dark: #e55300;
    --color-md-red: #FF3400;
    --color-md-magenta: #e60070;
    --color-md-light-orange: #ff8c00;
    --color-md-yellow: #ffc500;
    --color-md-grey: #9c9c9c;
    --color-md-grey-20: #e8e8e8;
    --color-md-grey-40: #d0d0d0;
    --color-md-grey-60: #b0b0b0;
    --color-md-grey-80: #a0a0a0;

    /* 语义 Token */
    --color-surface: #F4F6F8;
    --color-surface-card: #FFFFFF;
    --color-accent: var(--color-md-orange);
    --color-accent-dark: var(--color-md-orange-dark);
    --color-accent-surface: #fff3ec;
    --color-destructive: #EF4444;
    --color-border: #E5E7EB;
    --color-text-primary: #111827;
    --color-text-secondary: #6B7280;

    /* 科技感增强 */
    --color-glass-bg: rgba(255, 255, 255, 0.7);
    --color-glass-border: rgba(255, 255, 255, 0.3);

    /* 布局常量 */
    --spacing-sidebar: 220px;
    --spacing-nav-item: 44px;
  }

  @layer base {
    html {
      font-family: "Inter", "Arial", ui-sans-serif, system-ui, -apple-system, sans-serif;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    body {
      color: var(--color-text-primary);
      background-color: var(--color-surface);
    }

    :focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--color-accent);
      border-radius: 2px;
    }
  }

  @layer components {
    .glass-card {
      background: var(--color-glass-bg);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      border: 1px solid var(--color-glass-border);
      border-radius: 12px;
    }

    .kpi-value {
      font-size: 2.25rem;
      font-weight: 600;
      line-height: 1.1;
      letter-spacing: -0.02em;
    }
  }
  ```

  关键变更（与旧文件对比）：
  - 移除 --color-accent=#22C55E → --color-accent=var(--color-md-orange)
  - 移除 --color-accent-dark=#16A34A → --color-accent-dark=var(--color-md-orange-dark)
  - 移除 --color-accent-surface=#DCFCE7 → --color-accent-surface=#fff3ec
  - 移除 --color-purple-surface / --color-purple-text（不再需要）
  - 移除 --color-trend-neutral（统一用 --md-grey-40）
  - 新增 --color-md-orange, --md-* 系列品牌色
  - 新增 .glass-card 组件类
  - 新增 .kpi-value 组件类
  </action>
  <verify>
    <automated>
      grep -c "#fe5c00\|--color-md-orange" app/globals.css && ! grep -q "#22C55E\|green-" app/globals.css && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "--color-md-orange" app/globals.css` >= 1
    - `grep -c "#22C55E" app/globals.css` == 0
    - `grep -c "#fe5c00" app/globals.css` >= 1
    - `grep -c "glass-card" app/globals.css` >= 1
    - `grep -c "kpi-value" app/globals.css` >= 1
    - `grep -c "--color-accent:" app/globals.css` >= 1
    - `grep -c "Inter.*Arial" app/globals.css` >= 1
    - `grep -c "--color-purple" app/globals.css` == 0（紫色移除）
  </acceptance_criteria>
  <done>
    globals.css 使用 MD 品牌色板（橙色系），含 glass-card + kpi-value 组件类，无绿色残留
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 layout.tsx + KpiCard + TrendBadge——MD 品牌 + 科技感</name>
  <files>app/layout.tsx, components/ui/KpiCard.tsx, components/ui/TrendBadge.tsx</files>
  <read_first>
    @./app/layout.tsx (当前——保持结构，确保 Inter font)
    @./components/ui/KpiCard.tsx (当前——替换绿色引用为 MD Orange)
    @./components/ui/TrendBadge.tsx (当前——替换绿色/红色为 MD 色系)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md lines 920-954 (KpiCard 科技感版本)
  </read_first>
  <action>
  修改 3 个文件：

  **A) app/layout.tsx** — 确保 Inter font 加载
  - 保留现有结构：Inter font, AppShell, globals.css import
  - 确保 `<html>` 标签使用 `className={inter.variable}`（已存在）
  - 标题改为 "MWLAB 2026 | 竞争盘面看板"
  - 添加 lang="zh-CN"

  **B) components/ui/KpiCard.tsx** — 科技感 + MD 橙色
  具体修改：
  - 所有 `bg-accent-surface` 保持（--color-accent-surface 已改为 MD Orange）
  - 高亮卡片 hover shadow: 从 `rgba(34,197,94,0.15)` 改为 `rgba(254,92,0,0.12)`
  - 高亮卡片 label 颜色: 从 `text-green-700` 改为 `text-[var(--color-md-orange)]` 或使用 tailwind 的 `text-accent-dark`
  - 加载骨架屏: 保持现有 animate-pulse（颜色无关）
  - 错误/空状态: 保持现有逻辑（红色系）
  - 添加 hover 玻璃态效果：标准卡片 hover 使用 `hover:shadow-[0_8px_24px_rgba(254,92,0,0.08)]`
  - 确保 kpi-value 类应用在大数字上

  **C) components/ui/TrendBadge.tsx** — MD 色系趋势徽章
  Let me check what the current TrendBadge looks like...
  
  Start by reading the file, then:
  - 上升: backgroundColor from `bg-green-100` to `bg-[#fff3ec]`, text from `text-green-600` to `text-[var(--color-md-orange-dark)]`
  - 平稳: backgroundColor `bg-gray-100`, text `text-gray-500`（保留中性色）
  - 下降: backgroundColor `bg-red-50`, text `text-[var(--color-md-red)]`（使用 MD Red #FF3400）
  </action>
  <verify>
    <automated>
      grep -c "md-orange\|rgba(254,92,0)" components/ui/KpiCard.tsx && grep -c "md-orange\|FF3400" components/ui/TrendBadge.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - KpiCard.tsx: `grep -c "rgba(254,92,0)"` >= 1（hover shadow 改为 MD Orange）
    - KpiCard.tsx: `grep -c "22C55E\|16A34A"` == 0（无绿色残留）
    - KpiCard.tsx: `grep -c "accent-surface"` >= 1（高亮卡片背景）
    - TrendBadge.tsx: `grep -c "fff3ec\|md-orange\|FF3400"` >= 1（上升/下降色）
    - layout.tsx: `grep -c "Inter"` >= 1
    - layout.tsx: `grep -c "竞争盘面"` >= 1
  </acceptance_criteria>
  <done>
    layout.tsx 加载 Inter font 和 MD 标题；KpiCard 使用 MD Orange 高亮和 hover；TrendBadge 使用 MD 色系
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 FilterTabs + IndustryPieChart + EventModal + Legend——MD 色板</name>
  <files>components/ui/FilterTabs.tsx, components/charts/IndustryPieChart.tsx, components/calendar/EventModal.tsx, components/map/Legend.tsx</files>
  <read_first>
    @./components/ui/FilterTabs.tsx (当前绿色选中态——改为 MD Orange)
    @./components/charts/IndustryPieChart.tsx (当前绿色系色板——改为 MD 色板)
    @./components/calendar/EventModal.tsx (当前——品牌色更新)
    @./components/map/Legend.tsx (当前——地图图例色更新)
  </read_first>
  <action>
  修改 4 个组件：

  **A) components/ui/FilterTabs.tsx** — MD Orange 选中态
  在 Pill 组件中：
  - 选中态: `bg-accent-surface border border-accent text-accent-dark font-semibold`
  - 未选中态: `bg-white border border-border text-gray-700 hover:bg-surface`
  - 注意：accent 系列变量已在 globals.css 中定义为 MD Orange，只需要确保 Tailwind 类名使用 text-accent-dark（而非 text-green-700）
  - 搜索并替换: `text-green-700` → `text-accent-dark`
  - 搜索并替换: `border-accent` → `border-accent`（已经正确——accent 指向 MD Orange）

  **B) components/charts/IndustryPieChart.tsx** — MD 色板
  将 CHART_COLORS 数组从：
  ```typescript
  const CHART_COLORS = [
    "#22C55E", "#3B82F6", "#F59E0B", "#8B5CF6",
    "#EF4444", "#06B6D4", "#F97316", "#6B7280",
  ]
  ```
  改为 MD 品牌色板：
  ```typescript
  const CHART_COLORS = [
    "#fe5c00",  // MD Orange (主色)
    "#e60070",  // MD Magenta
    "#ff8c00",  // MD Light Orange
    "#FF3400",  // MD Red
    "#9c9c9c",  // MD Grey
    "#ffc500",  // MD Yellow
    "#e55300",  // MD Orange Dark
    "#6B7280",  // grey-500 fallback
  ]
  ```

  **C) components/calendar/EventModal.tsx** — 品牌色更新
  仅修改 CSS 类（无结构性变更）：
  - eventStyle 中的 "竞争对手"（原红色）：保留红色 `#FEE2E2/#EF4444`（语义色不变）
  - "潜在伙伴" 背景从 `#DCFCE7` 改为 `#fff3ec`，边框从 `#22C55E` 改为 `#fe5c00`
  - default 色保留灰色系不变

  **D) components/map/Legend.tsx** — 地图图例颜色
  - 国内展会标记: 从 blue (#3B82F6) 改为 MD Orange (#fe5c00)
  - 国际展会标记: 保留或改为 MD Light Orange (#ff8c00)
  - 更新图例文字对应的颜色指示
  </action>
  <verify>
    <automated>
      grep -c "#fe5c00\|#e60070" components/charts/IndustryPieChart.tsx && ! grep -q "22C55E" components/ui/FilterTabs.tsx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - FilterTabs.tsx: `grep -c "text-accent-dark\|text-green-700"` grep 只有 text-accent-dark（无 text-green-700）
    - IndustryPieChart.tsx: `grep -c "#fe5c00\|#e60070\|#ff8c00"` >= 3（MD 色板在 CHART_COLORS 中）
    - IndustryPieChart.tsx: `grep -c "#22C55E"` == 0（无绿色）
    - EventModal.tsx: 潜在伙伴色从 green 改为 #fff3ec/#fe5c00
    - 所有文件: `grep -c "supabase"` == 0（无 Supabase 新引入）
  </acceptance_criteria>
  <done>
    FilterTabs 选中态为 MD Orange；PieChart 使用 MD 色板；EventModal + Legend 使用 MD 品牌色
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
无新的信任边界（纯客户端样式变更，无数据流变更）
## STRIDE Threat Register
无（纯样式/表现层，无安全影响）
</threat_model>

<verification>
1. `grep -r "#22C55E\|green-" app/ components/ --include='*.{tsx,css,ts}' | grep -v node_modules | grep -v '.next'` — 结果为 0（无绿色残留）
2. `grep -r "#fe5c00\|--color-md-orange" app/ components/ --include='*.{tsx,css,ts}'` — MD Orange 存在
3. `grep "glass-card\|kpi-value" app/globals.css` — 科技感组件类存在
4. `grep "Inter" app/layout.tsx` — Inter font 加载
</verification>

<success_criteria>
- globals.css 完全使用 MD 品牌色板（橙色主色），无绿色残留
- KpiCard 使用 MD Orange 高亮 + 科技感 hover 效果
- FilterTabs Pill 选中态使用 MD Orange 背景/边框
- TrendBadge 上升/下降使用 MD Orange/MD Red
- IndustryPieChart 色板使用 MD 品牌色（orange/magenta/red/yellow）
- EventModal 和 Legend 使用 MD 品牌色
- 全局 `grep -r "#22C55E" app/ components/` 返回 0
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-04-SUMMARY.md
</output>
