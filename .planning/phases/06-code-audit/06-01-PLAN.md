---
phase: 06-code-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - lib/api-guard.ts
  - lib/auth.ts
  - app/api/auth/login/route.ts
  - app/api/dashboard/route.ts
  - app/api/people/route.ts
  - "app/api/people/[id]/route.ts"
  - "app/api/people/[id]/contacts/route.ts"
  - "app/api/people/[id]/relations/route.ts"
  - "app/api/exhibition/[id]/route.ts"
  - "app/api/exhibition/[id]/relations/route.ts"
  - "app/api/exhibition/[id]/timeline/route.ts"
  - "app/api/timeline/[eventId]/route.ts"
  - app/login/page.tsx
  - tests/api/dashboard.test.ts
  - .gitignore
autonomous: false
requirements: [AUD-04]
findings: [API-01, API-02, API-03, API-04, API-05, API-06, API-07, API-08, HYG-01, HYG-02, HYG-04]
must_haves:
  truths:
    - "不带合法 JWT 的 /api/* 请求一律返回 401（伪造 x-user-role: admin 头无效）"
    - "readonly 角色持合法 token 调用任何写接口返回 403"
    - "登录后 token 仅存在于 HttpOnly cookie，响应 body 与 localStorage 中无 token"
    - "git ls-files 不再包含 mwlab.db，远端历史已清除该文件，3 个用户密码已重置"
  artifacts:
    - path: "middleware.ts"
      provides: "x-user-* 头剥离 + 无 token API 请求 401"
    - path: "lib/api-guard.ts"
      provides: "route handler 共享鉴权守卫（身份 + 角色 + is_active）"
    - path: ".gitignore"
      provides: "*.pem 与归档目录 ignore 规则"
      contains: "*.pem"
  key_links:
    - from: "middleware.ts"
      to: "所有 /api/* route"
      via: "requestHeaders.delete('x-user-email'/'x-user-role') 后再按验签注入"
      pattern: "delete\\(['\"]x-user-"
    - from: "app/api/**/route.ts"
      to: "lib/api-guard.ts"
      via: "handler 入口调用 requireUser / requireWriter"
      pattern: "api-guard"
---

<objective>
Wave 1 · 安全止血。封堵鉴权完全绕过（雷区 1）与仓库密钥泄露（雷区 2）：middleware 头剥离 + 401、GET 路由鉴权、写接口角色校验、HttpOnly cookie 签发、mwlab.db 出库 + 历史清理（checkpoint）+ 改密、.pem 防护、.gitignore 规则。

Purpose: 当前 API 层对未认证攻击者实质全开放，且含密码哈希与 PII 的生产库已推上 GitHub——这是全部 61 项发现中风险最高的 11 项，必须最先修。
Output: 鉴权闭环的 API 层 + 干净的仓库历史 + 重置后的用户口令。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-code-audit/AUDIT.md
@.planning/phases/06-code-audit/REVIEW-api.md
@CLAUDE.md

约束：不写兼容性代码；前端 UI 重设计不在范围内，但服务端 cookie 签发及其直接关联的最小前端改动（移除 JS 写 cookie / localStorage 存 token）在范围内（属"我们改动造成的孤儿代码清理"）。

验证前置条件：Task 1/2/3/4 的 curl 验证需 dev server 在 3000 端口运行。执行 `npm run dev &` 后台启动，并以 `curl -s --retry 15 --retry-connrefused -o /dev/null http://localhost:3000` 等待就绪后再跑各任务验证；计划结束时 kill 该进程。
</context>

<tasks>

<task type="auto">
  <name>Task 1: middleware 剥离 x-user-* 头 + 无 token API 请求返回 401</name>
  <files>middleware.ts</files>
  <action>
修复 API-01（REVIEW-api.md，middleware.ts:23, 31）：
1. middleware 入口处，无条件先从请求头删除外部传入的 `x-user-email` 与 `x-user-role`（`requestHeaders.delete(...)`），再根据 JWT 验签结果注入可信值。三条路径（有效 token / 无 token / 验签失败）都必须经过剥离。
2. 无 token 或验签失败时：若路径匹配 `/api/*` 且不在公开白名单（仅 `/api/auth/login`），直接返回 `NextResponse.json({error:'unauthorized'},{status:401})`，不再放行；非 API 页面保持现有重定向 /login 行为不变。
3. 删除 L23 附近"401 由 handler 处理"的失效注释。
不引入任何新依赖；jose 验签逻辑不动。
  </action>
  <verify>
    <automated>启动 dev server 后执行：curl -s -o /dev/null -w "%{http_code}" -H "x-user-role: admin" http://localhost:3000/api/users 期望输出 401；curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/dashboard 期望 401；带合法 session cookie 的请求仍返回 200</automated>
  </verify>
  <done>无 token / 伪造头 / 坏 token 三种请求对所有受保护 /api/* 均得 401；/api/auth/login 仍可匿名访问；合法登录用户功能不受影响</done>
  <commit>fix(api): strip x-user-* headers and return 401 for unauthenticated API requests [API-01]</commit>
</task>

<task type="auto">
  <name>Task 2: 共享鉴权守卫 + GET 路由补鉴权 + is_active 校验 + 翻转 dashboard 测试断言</name>
  <files>lib/api-guard.ts, app/api/dashboard/route.ts, app/api/people/route.ts, app/api/people/[id]/route.ts, app/api/exhibition/[id]/route.ts, tests/api/dashboard.test.ts</files>
  <action>
修复 API-02 / API-03 / API-06：
1. 新建 `lib/api-guard.ts`（该 helper 被 10+ 个 route 复用，符合最小代码原则）：
   - `requireUser(request): { email, role } | null` — 读取 middleware 注入的 `x-user-email` / `x-user-role`；缺失返回 null。读到 email 后用 lib/db.ts 查 `user` 表 `is_active`，`is_active=0` 也返回 null（API-06：让被禁用用户的存量 token 失效，middleware 边缘运行时无法查库，故在 handler 层做）。
   - `requireWriter(user): boolean` — `role !== 'readonly'`（供 Task 3 使用）。
2. 四个零鉴权 GET handler（dashboard/route.ts:8、people/route.ts:4、people/[id]/route.ts:4、exhibition/[id]/route.ts:4）入口加 `requireUser`，null 即返回 401。people/route.ts 的 GET 需补上 request 参数。
3. 翻转 tests/api/dashboard.test.ts:26-28 的"无认证返回 200"断言为期望 401（该断言固化了 API-02 漏洞；本任务修复后旧断言必红，属我们改动造成的孤儿，必须同步翻转，更全面的新增测试在 06-05）。
  </action>
  <verify>
    <automated>npm test -- dashboard 通过（断言已翻转为 401）；curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/people 期望 401</automated>
  </verify>
  <done>dashboard / people / people/[id] / exhibition/[id] 四组 GET 匿名访问均 401；被 is_active=0 禁用的用户持有效 token 也得 401；测试套件绿</done>
  <commit>fix(api): require authenticated user on dashboard/people/exhibition GET routes [API-02][API-03][API-06]</commit>
</task>

<task type="auto">
  <name>Task 3: 写接口角色校验 403 + JSON 解析与参数校验 + relations 回查修复</name>
  <files>app/api/people/route.ts, app/api/people/[id]/contacts/route.ts, app/api/people/[id]/relations/route.ts, app/api/exhibition/[id]/relations/route.ts, app/api/exhibition/[id]/timeline/route.ts, app/api/timeline/[eventId]/route.ts</files>
  <action>
修复 API-04 / API-07 / API-08，涉及 REVIEW-api.md 列出的 6 个写接口：
1. [API-04] 每个写 handler（people POST :18-20、contacts :8-9、people relations :8-9、exhibition relations :8-9、timeline POST :8-9、timeline DELETE :8-9）在 requireUser 之后加 `requireWriter` 校验，readonly 角色返回 403。
2. [API-07] 上述路由的 `await req.json()`（如 people/route.ts:22 等 5 处）统一用 try/catch 包裹，畸形 JSON 返回 400（参照 login route :11-14 的既有写法）；所有 `Number(id)` 动态路径参数先 `Number.isInteger(Number(id))` 校验，非法返回 400。不做超出此范围的字段级 schema 校验（最小改动）。
3. [API-08] exhibition/[id]/relations/route.ts:30-35 的插入后回查改为 `WHERE r.id = ?` + `result.lastInsertRowid`（参照 people/route.ts:41 的正确写法）。不在本任务加唯一索引（schema 改动归 Wave 2/3 的 migration 流程，且审计仅"考虑"级）。
  </action>
  <verify>
    <automated>用 readonly 用户登录获取 cookie 后：curl -s -o /dev/null -w "%{http_code}" -X POST -b "session=$READONLY_TOKEN" -H "Content-Type: application/json" -d '{"name":"x"}' http://localhost:3000/api/people 期望 403；curl -X POST 同路由传非法 JSON（-d '{'）期望 400 而非 500</automated>
  </verify>
  <done>6 个写接口对 readonly 全部 403；畸形 JSON 与非整数 path 参数得 400 不再 500；relations POST 返回的是新插入行</done>
  <commit>fix(api): enforce role check on write routes, validate json/params, fix relations re-query [API-04][API-07][API-08]</commit>
</task>

<task type="auto">
  <name>Task 4: login route 改为服务端签发 HttpOnly cookie，移除 body token 与 localStorage 存储</name>
  <files>app/api/auth/login/route.ts, lib/auth.ts, app/login/page.tsx</files>
  <action>
修复 API-05（login/route.ts:54-59、lib/auth.ts:16-18,49、app/login/page.tsx:136）：
1. login route 成功分支改为 `response.cookies.set('session', token, { httpOnly: true, secure: process.env.NODE_ENV === 'production', sameSite: 'lax', maxAge: 86400, path: '/' })`；响应 body 移除 token 字段（保留 user 白名单字段供前端展示）。
2. lib/auth.ts 删除 token 的 localStorage 读写（:16-18）与 `document.cookie` 写入（:49）。先 grep 全前端确认还有哪些位置消费 localStorage 中的 token——若发现除登录页之外的消费点，仅做"改为依赖 cookie 会话"的最小改动并在 SUMMARY 中列出，不做 UI 重构。
3. app/login/page.tsx:136 移除 JS 写 cookie 逻辑（服务端已 Set-Cookie），登录成功后直接跳转。
注意 secure 标志在本地 http 开发环境会导致 cookie 不下发，故按 NODE_ENV 条件设置。
  </action>
  <verify>
    <automated>curl -si -X POST http://localhost:3000/api/auth/login -H "Content-Type: application/json" -d '{"email":"<测试账号>","password":"<口令>"}' 的响应头含 Set-Cookie: session=...; HttpOnly; SameSite=lax 且 body 不含 token 字段；grep -rn "localStorage" lib/auth.ts 输出为空</automated>
  </verify>
  <done>会话凭证仅存在于 HttpOnly cookie；XSS 无法通过 document.cookie / localStorage 读取 token；登录-访问-登出全流程手工可走通</done>
  <commit>fix(auth): issue HttpOnly session cookie server-side, remove token from body and localStorage [API-05]</commit>
</task>

<task type="auto">
  <name>Task 5: mwlab.db 出库（非破坏步骤）+ .pem 防护 + 根目录杂物归档规则</name>
  <files>.gitignore</files>
  <action>
HYG-01 非破坏部分 + HYG-02 + HYG-04：
1. [HYG-01] `git rm --cached mwlab.db` 并提交——只动索引不删工作区文件。确认 `.gitignore` 已有 `*.db` 规则（已有则不重复添加）。
2. [HYG-02] `.gitignore` 增加 `*.pem` 与 `*.key` 规则；将根目录 `MWlab.pem` 移动到 `~/.ssh/MWlab.pem` 并 `chmod 600`；在任务输出中明确告知新路径（SSH 连接命令需更新 -i 参数）。
3. [HYG-04] `.gitignore` 增加 `_archive/` 规则；创建 `_archive/` 目录，将根目录的 `mwlab_backup_*.db`、`.gitignore.bak`、`*.docx`、`*.xlsx` 杂物移入。不删除任何文件，只归档。
4. 提交 .gitignore 与索引变更（杂物本身未被跟踪，无需提交）。
  </action>
  <verify>
    <automated>git ls-files | grep -c "mwlab.db" 输出 0；grep -c "pem" .gitignore 输出 ≥1；test -f ~/.ssh/MWlab.pem && echo ok 输出 ok；test ! -f MWlab.pem && echo moved 输出 moved</automated>
  </verify>
  <done>mwlab.db 不再被 git 跟踪；私钥已移出仓库且有 ignore 防护；根目录杂物归档；以上均不影响远端历史（历史清理在 Task 6/7）</done>
  <commit>chore(hygiene): untrack mwlab.db, ignore *.pem, archive root clutter [HYG-01][HYG-02][HYG-04]</commit>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 6: CHECKPOINT — 确认执行破坏性步骤：git 历史重写 + force push + 全员改密</name>
  <decision>是否立即执行 HYG-01 的破坏性收尾三步：① git-filter-repo 从全部历史中清除 mwlab.db；② force push 覆盖远端 github.com/sywangyue/D_system.git；③ 重置 user 表全部 3 个账号密码</decision>
  <context>
mwlab.db（含 3 个 bcrypt 密码哈希 + 全量业务数据 + 联系人 PII）已在远端历史中。仅 git rm --cached（Task 5 已完成）不会清除历史副本，必须重写历史并 force push。
风险须知：
- force push 后任何已有 clone 必须重新 clone（不能 pull）；
- git-filter-repo 会移除 origin remote 配置，需重新添加；
- 改密后 3 个用户需用新口令登录（新口令将在 Task 7 输出给你）；
- 执行前会自动核查仓库当前可见性（gh repo view sywangyue/D_system --json visibility）并报告。
若仓库曾经公开或 fork 过，历史清理也无法收回已泄露内容，改密是唯一有效止损。
  </context>
  <options>
    <option id="proceed">
      <name>立即执行三步（推荐）</name>
      <pros>泄露窗口最短；与 Wave 2 的生产库操作解耦干净</pros>
      <cons>需重新 clone、重新加 remote、通知用户新口令</cons>
    </option>
    <option id="defer">
      <name>暂缓历史清理，仅执行改密</name>
      <pros>不动远端历史</pros>
      <cons>密码哈希与 PII 继续留在远端历史中，风险持续</cons>
    </option>
  </options>
  <resume-signal>回复 proceed 或 defer</resume-signal>
</task>

<task type="auto">
  <name>Task 7: 执行历史清理 + force push + 重置全部用户密码（按 Task 6 决策）</name>
  <files>mwlab.db (user 表)</files>
  <action>
按 Task 6 的选择执行（HYG-01 破坏性部分）：
若 proceed：
1. 先做本地裸备份：`git clone --mirror . ../D_dashboard_pre_filter_repo.git`（保险快照，放仓库外）。
2. `pip3 install git-filter-repo`（若未装）；`git filter-repo --invert-paths --path mwlab.db --force`。
3. 重新添加 remote：`git remote add origin https://github.com/sywangyue/D_system.git`；`git push --force --all origin && git push --force --tags origin`。
4. 改密：复制 `cp mwlab.db _archive/mwlab_pre_pwreset_$(date +%Y%m%d).db` 后，为 user 表 3 个账号各生成 16 位随机口令，用项目登录校验所用的同一 bcrypt 库（先 grep app/api/auth/login/route.ts 确认是 bcrypt 还是 bcryptjs 及 cost）生成哈希，UPDATE user SET password_hash=...。明文新口令仅输出到终端给 Max，不写入任何文件。
若 defer：仅执行第 4 步改密，并在 SUMMARY 中记录"历史清理被客户暂缓"。
  </action>
  <verify>
    <automated>git log --all --oneline -- mwlab.db | wc -l 输出 0（proceed 时）；用旧口令调用 login API 期望 401，用新口令期望 200</automated>
  </verify>
  <done>远端历史不含 mwlab.db（或客户明确暂缓）；3 个账号旧口令全部失效、新口令可登录；本地有 filter-repo 前的镜像备份</done>
  <commit>chore(hygiene): purge mwlab.db from git history and reset all user passwords [HYG-01]</commit>
</task>

<task type="auto">
  <name>Task 8: 运行 API 层测试套件收尾</name>
  <files>(无修改)</files>
  <action>运行 `npm test` 全量 TS 测试。dashboard.test.ts 应为翻转后的 401 断言；users/setting 既有用例应仍绿（它们直接注入头调用 handler，单元层不受 middleware 改动影响）。若有红用例，修因不改果——禁止为了变绿而放松断言。</action>
  <verify>
    <automated>npm test 退出码 0</automated>
  </verify>
  <done>npm test 全绿；本计划全部提交完成</done>
  <commit>test(api): ensure suite green after wave-1 security fixes</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公网 → /api/* | 任意匿名 HTTP 请求（含伪造头）在此进入 |
| 前端 JS → 会话凭证 | XSS 可读取非 HttpOnly 存储 |
| 本仓库 → GitHub 远端 | 提交即对远端可见，历史不可变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | middleware.ts x-user-* 注入链 | mitigate | 入口删除外来 x-user-* 头，无 token 401（Task 1） |
| T-06-02 | Information Disclosure | dashboard/people/exhibition GET | mitigate | handler 层 requireUser 401（Task 2） |
| T-06-03 | Tampering | 6 个写接口 | mitigate | requireWriter 403 + 输入校验 400（Task 3） |
| T-06-04 | Information Disclosure | session token (localStorage/JS cookie) | mitigate | 服务端 HttpOnly cookie（Task 4） |
| T-06-05 | Information Disclosure | mwlab.db in git history | mitigate | filter-repo + force push + 改密（Task 6/7） |
| T-06-06 | Repudiation | logout 后 token 24h 有效 | accept | 内部系统、handler 层已加 is_active 校验，剩余窗口风险已向客户披露（API-06） |
</threat_model>

<verification>
- 匿名 / 伪造头 / 坏 token 对受保护 /api/* 全部 401
- readonly 写操作全部 403
- 登录响应 Set-Cookie 含 HttpOnly，body 无 token
- git ls-files 与 git log --all 均不含 mwlab.db
- npm test 全绿
</verification>

<success_criteria>
AUDIT.md 雷区 1 与雷区 2 关闭：API-01~08、HYG-01/02/04 共 11 项全部修复或经客户确认暂缓（仅 HYG-01 历史清理允许 defer），且现有测试套件通过。
</success_criteria>

<output>
完成后创建 `.planning/phases/06-code-audit/06-01-SUMMARY.md`，须包含：新口令交付确认（不含明文）、.pem 新路径、HYG-01 是否 proceed/defer。
</output>
