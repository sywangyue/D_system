# Python
__pycache__/
*.py[cod]
.Python
.venv/
venv/
.env
.env.*
!.env.local.template

# Local databases & runtime
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite3

# Crawler / local run logs (regenerable)
cnexpo_crawl_progress.log

# OS / IDE
.DS_Store
.idea/
.vscode/

# Local tooling (may contain secrets) — skills/ subdir is tracked
.claude/*
!.claude/skills

# Next.js
node_modules/
.next/
out/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Build outputs
.open-next/
.wrangler/

# Ralph internal state
scripts/ralph/.last-branch

# Supabase local
supabase/.temp/
.gstack/

# Agent internals
.agents/
.hermes/

# Data / output / logs (regenerable)
data/
exports/
logs/
output/

# 运营资料目录（不是代码）：资源下载接口读这两个目录，靠 rsync 直接同步到服务器，
# 不经过 git。见 docs/DEPLOY.md「上传清单」。
reports/
research/

# 知识库的 docs/ —— 合同、审批件、商业条件这类。**本仓库是 PUBLIC 的**，
# 进 git 等于全网可见且 git 历史永久留存。部署靠 rsync 不靠 git（DEPLOY.md
# 「同步知识库内容」），所以挡掉不影响上线，线上仍然能鉴权下载。
# 正文 index.md 与 public/knowledge/ 的图片不在此列，照常进 git。
knowledge/*/docs/

# SSH keys / certificates
*.pem
*.key

# Archived root clutter
_archive/

# 分析/导出产出物（由脚本再生成，不入库）
/*_中国境内展会清单.xlsx
/*_全部品牌展会.xlsx

# Backup / temp files
backups/
*.bak
mwlab.db.backup-*
screenshot_verify.png
skills-lock.json
organizer_checkpoint.txt
dedup_audit.log
