# 会话交接 · 2026-07-30

> 明早把这份文件内容贴给 Claude 即可续上下文。

---

## 一、今天完成的事

### 1. 数据修复（已入库）

三条 `country_cn` 推断错误已修，备份在 `data/backups/mwlab_20260730_pre_geofix.db`：

| brand_id | 展会 | country_cn |
|---|---|---|
| EXPO-4398 | 丹麦海宁礼品及家居用品展 | 中国 → **丹麦** |
| EXPO-4463 | 苏州康复设备与解决方案展 | 德国 → **中国** |
| EXPO-8261 | 苏州杜塞尔多夫康复设备展 | 德国 → **中国** |

根因：`is_international_source=inferred` 的推断规则把「丹麦海宁」认成浙江海宁、把品牌名里的「杜塞尔多夫」当成举办地。

### 2. 两份 Excel 导出（已交付）

- `exports/2026-08_全字段展会清单.xlsx` — 450 行 × 46 列
- `exports/2026-08_中国地区（不含台湾）全字段展会清单.xlsx` — 168 行 × 46 列（含香港 7 条）

### 3. 去重治理（核心成果）

**查明「治理没生效」的三个根因**：

1. `dedup.py:strip_edition` 正则 `^第\s*\d+\s*届` 只认阿拉伯数字、只认行首，中文「第二十四届」全部漏网 —— **已修**
2. 判定逻辑里 `venue` 根本没被 SELECT，`name_en` 查了但从不参与计算 —— 最强的两个判别信号被浪费
3. 名称相似度已到天花板：真重复 0.71–0.74，不同展会 0.70，仅差 0.03，**降门限必然误伤**

**新增分层判据**（`tools/export_dedup_review.py`，只读）：

| 层 | 判据 | 对数 |
|---|---|---:|
| A1 | 名称归一化后逐字相同（零误伤，已预填 Y） | 59 |
| A2 | 名称相似度 ≥0.80（含假阳性） | 123 |
| B | 同主办+同城市+同场馆+name_en 相同 | 257 |
| C | 同主办+同城市+同场馆+档期相同（多为同期平行展，默认 N） | 1314 |

### 4. 爬虫改造：`--refresh`

真正的「只增不更」根因在 `jufair_crawler.py:508` —— 已存在的 URL **在进 SQL 之前**就被过滤掉了，光改 `INSERT OR IGNORE` 没用。现改为 UPSERT + 字段级变更日志，默认行为不变。

### 5. 定时任务已部署

```
0 3 7,27 * *  scripts/run_pipeline.sh
```

采集(--refresh) → 合并(传 batch_id) → 分类(--only-empty) → 届次状态 → 展示池 → 导出复核表。

内置：改库前备份（留 10 份）、mkdir 原子锁、失败摘要落 `logs/pipeline_failures.log`、绝对路径 Python、`PYTHONUNBUFFERED=1`。

**刻意排除** `geo_backfill.py`（无条件覆盖 country_cn + 清空全表 notes）和 `dedup.py --execute`。

### 6. 试跑并修掉 3 个 bug

| bug | 说明 |
|---|---|
| `merge_engine.py` 分隔符 | `'/'.join()` 拼接但 schema CHECK 用 `+`，双源届次再合并拼出 `jufair+cnexpo/jufair` 直接崩。**既有 bug，被 --refresh 触发**。已改为集合合并 + 保护人工来源（手工/官网不被爬虫降级） |
| pipeline 失败后继续 | 合并崩了还跑完剩余 4 步，产出不含新数据的复核表。已给采集/合并加 `\|\| die` |
| 日志块缓冲 | 采集 33 分钟一个字不输出。已加 `PYTHONUNBUFFERED=1` |

### 7. AGENTS.md 已更新（5 处）

定时任务章节、字段来源分类（新增「人工修正会被覆盖的字段」）、文件索引、数据现状表、4 个易踩的坑。

---

## 二、当前数据状态

| 项 | 跑前 | 跑后 |
|---|---:|---:|
| exhibition_brand | 7,179 | **7,292** |
| exhibition_edition | 7,476 | **7,592** |
| data_provenance | 9,477 | **9,683** |
| display_ready=1 | 7,154 | **7,276（99.8%）** |
| raw_jufair | 6,843 | **6,945** |

非法 `data_source` 0 条，`industry_l1` 空值 0 条，3 条人工修正完好保留。

回滚点：`data/backups/mwlab_auto-20260730_pre_pipeline.db`

---

## 三、明天的待办

### ⬜ 待办 1（主线）：复核表过完 → 我写导入脚本

文件：`exports/dedup_review_auto-20260730.csv`（**1753 对**，用这份，不是早先那份 1551 对的）

只填第一列 `决定`：`Y`=合并 / `N`=保留两条。

- A1 层 59 对已预填 Y，通读确认即可
- A2/B 逐条判 —— 「郑州机器人展 × 郑州机床展」(0.84)、高交会 CHTF 多专题展这类要填 N
- C 层 1314 对**默认 N**，从中挑出少数真重复

过完发回，我写 `tools/import_dedup_review.py`，会先备份、先出 dry-run 差异，确认后才执行。

**验证用例**（治理效果的标尺）：
- 应合并：EXPO-4318 / EXPO-4319 / EXPO-8258（北京世界机器人大会，同主办同场馆）
- **不可合并**：EXPO-3122（北京机器人大会-北京机器人展，中国设备管理协会 / 国展朝阳馆 / 5 月，是另一个展）

### ⬜ 待办 2：两条 city_en 错误，等你拍板

| brand_id | 现值 | 应为 |
|---|---|---|
| EXPO-4398 | Haining | **Herning**（丹麦城市） |
| EXPO-4463 | Düsseldorf | **Suzhou** |

上次问了没答，超出当时范围所以没动。

### ⬜ 待办 3：8/7 03:00 首次 cron 自动跑，次日检查

```bash
cat logs/pipeline_failures.log              # 有内容就是失败了
tail -40 logs/pipeline_auto-20260807.log
```

### ✅ 已决定不做：阿里云搬迁

服务器 `47.79.17.71` 在**阿里云香港区**（非大陆），与「Jufair 仅限大陆 IP」硬约束冲突，且 890MB RAM 连 `npm run build` 都 OOM。结论是维持混合架构：爬虫/pipeline 留北京 Mac Mini，产出 db 同步到香港服务器。你已决定暂不动。

---

## 四、速查

```bash
# 手动跑完整 pipeline
bash scripts/run_pipeline.sh

# 单独重出复核表
python3 tools/export_dedup_review.py -o exports/dedup_review.csv
python3 tools/export_dedup_review.py --layer C -o exports/c.csv   # 按层拆

# 导出展会清单（全字段）
python3 tools/export_exhibitions.py --month 2026-08

# 回滚数据库
cp data/backups/mwlab_auto-20260730_pre_pipeline.db data/mwlab.db
```

**绝不要跑** `scripts/geo_backfill.py` —— 无条件覆盖 `city/city_en/country_cn/country_en`（会撤销上面那 3 条修复），且结尾无条件 `UPDATE exhibition_brand SET notes = ''` 清空全表备注。

**本次改动的文件**：`scripts/dedup.py`、`scripts/run_pipeline.sh`(新)、`tools/export_dedup_review.py`(新)、`tools/merge_engine.py`、`crawlers/jufair_crawler.py`、`AGENTS.md`。均未提交 git。
