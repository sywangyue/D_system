---
phase: 05-intel-backend
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - tools/intel/db_query.py
  - tools/intel/qcc_client.py
autonomous: true
requirements:
  - REQ-06
  - REQ-08
  - REQ-11
  - REQ-12
  - REQ-13
  - REQ-17
  - REQ-18
  - REQ-23

must_haves:
  truths:
    - "python3 tools/intel/db_query.py brand-research EXPO-0001 返回品牌信息文本"
    - "python3 tools/intel/db_query.py industry-research 机械和设备 返回该行业展会列表"
    - "python3 tools/intel/db_query.py company-history 上海浦东展览馆 返回该公司参展记录"
    - "python3 tools/intel/qcc_client.py 上海 返回搜索结果或占位符提示（当 API Key 未配置时）"
    - "QCC_APP_KEY 未设置时 qcc_client.py 不抛异常，返回 {'Status': 'PLACEHOLDER', 'Result': []} 格式"
    - "brand-research 查询竞争关系时，若 exhibition_relation 为空则 fallback 到同行业列表"
  artifacts:
    - path: "tools/intel/db_query.py"
      provides: "4种 DB 查询模式的命令行脚本"
      exports: ["brand_research(identifier)", "industry_research(industry_l1)", "company_history(company_name)", "edition_detail(edition_id)"]
    - path: "tools/intel/qcc_client.py"
      provides: "企查查 API 封装（含占位符降级）"
      exports: ["fuzzy_search(keyword, page_index, page_size)", "get_company_detail(key_no)"]
  key_links:
    - from: "tools/intel/db_query.py"
      to: "mwlab.db"
      via: "sqlite3.connect(DB_PATH)"
      pattern: "sqlite3\\.connect"
    - from: "tools/intel/qcc_client.py"
      to: "https://api.qichacha.com/FuzzySearch/GetList"
      via: "requests.get()"
      pattern: "FuzzySearch/GetList"
---

<objective>
构建情报后端的两个核心 Python 工具模块：DB 聚合查询脚本（供 skill !命令调用）和企查查 API 客户端（含占位符降级模式）。

Purpose: Wave 3-4 的四个 Skill 文件通过 `!` 命令调用 db_query.py 注入真实 DB 数据（实现 D-01 DB 优先原则），通过 qcc_client.py 查询企业工商信息（实现 REQ-12/REQ-18）。

Output:
- tools/intel/db_query.py — 4种查询模式的 CLI 脚本
- tools/intel/qcc_client.py — 企查查 API 封装，含 graceful degradation
</objective>

<execution_context>
@/Users/maxmacmini/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maxmacmini/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-CONTEXT.md
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-RESEARCH.md
@/Volumes/databoard/AI Project/D_dashboard/tools/export_for_tagging.py
@/Volumes/databoard/AI Project/D_dashboard/.planning/phases/05-intel-backend/05-01-SUMMARY.md

<interfaces>
<!-- exhibition_brand 关键字段（from init_db.sql） -->
```sql
brand_id TEXT PRIMARY KEY       -- EXPO-XXXX
name_cn TEXT NOT NULL
name_en TEXT NOT NULL DEFAULT ''
organizer TEXT NOT NULL DEFAULT ''
industry_l1 TEXT NOT NULL DEFAULT ''
industry_l2 TEXT NOT NULL DEFAULT ''
scale_score INTEGER             -- 1-10
ma_potential INTEGER            -- 1-5
strategic_relevance INTEGER     -- 1-5
is_ufi_certified INTEGER        -- 0/1
is_international INTEGER        -- 0/1
competition_relation TEXT       -- '是'/'否'/''
```

<!-- exhibition_edition 关键字段 -->
```sql
edition_id TEXT PRIMARY KEY     -- EXPO-XXXX-YYYY
brand_id TEXT NOT NULL REFERENCES exhibition_brand(brand_id)
year INTEGER
area_sqm INTEGER
exhibitors_count INTEGER
visitors_count INTEGER
city TEXT
status TEXT                     -- '已举办'/'即将举办'/'取消'/'延期'/''
```

<!-- exhibition_relation 表（当前为空，但结构存在） -->
```sql
-- 注意: 当前 0 条数据，查询此表时必须实现 fallback 逻辑
from_brand_id TEXT REFERENCES exhibition_brand(brand_id)
to_brand_id TEXT REFERENCES exhibition_brand(brand_id)
relation_type TEXT              -- '竞争'/'合作'/'母子'/'收购目标'/'参考标杆'/'同主办方'
notes TEXT
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 tools/intel/db_query.py（DB 数据注入脚本）</name>
  <files>tools/intel/db_query.py</files>

  <read_first>
    - tools/export_for_tagging.py — 确认 DB_PATH 计算方式（`Path(__file__).resolve().parent.parent.parent / "mwlab.db"`）、sqlite3.Row 用法、argparse 风格
    - .planning/phases/05-intel-backend/05-01-SUMMARY.md — 确认 intel_report/customer_prospect 表已创建
  </read_first>

  <behavior>
    - brand_research("EXPO-0001") → 返回包含 "### 品牌基本信息" 的多行文本
    - brand_research("不存在品牌") → 返回 "错误: 未找到品牌 '不存在品牌'"
    - industry_research("机械和设备") → 返回至少 1 行品牌数据（DB 有 2573 条）
    - industry_research("不存在行业") → 返回 "该行业暂无数据: 不存在行业"
    - brand_research(带 exhibition_relation 为空时) → 输出包含 "fallback 到同行业展会" 的提示文本
  </behavior>

  <action>
创建 tools/intel/db_query.py，实现以下功能（逐字段完整实现，不做简化）：

```python
#!/usr/bin/env python3
"""
tools/intel/db_query.py — Skill DB 数据注入脚本

供 Claude Code skill 的 !`command` 调用，输出格式化文本。
用法:
  python3 tools/intel/db_query.py brand-research "EXPO-0001"
  python3 tools/intel/db_query.py brand-research "中国国际机床展览会"
  python3 tools/intel/db_query.py industry-research "机械和设备"
  python3 tools/intel/db_query.py industry-research "机械和设备" --l2 "机床"
  python3 tools/intel/db_query.py company-history "上海浦东展览馆"
  python3 tools/intel/db_query.py edition-detail "EXPO-0001-2024"
"""
from __future__ import annotations

import argparse
import sqlite3
import sys
from pathlib import Path

_REPO_ROOT = Path(__file__).resolve().parent.parent.parent
DB_PATH = _REPO_ROOT / "mwlab.db"

# ── 查询上限（防止输出过大撑满 LLM 上下文，per Pitfall 3）
MAX_EDITIONS = 10
MAX_INDUSTRY_BRANDS = 50
MAX_RELATIONS = 20


def _connect() -> sqlite3.Connection:
    conn = sqlite3.connect(str(DB_PATH))
    conn.row_factory = sqlite3.Row
    return conn


def brand_research(identifier: str) -> str:
    """查询单一品牌完整信息（历史届次 + 竞争关系）"""
    conn = _connect()
    # 优先精确匹配 brand_id，其次模糊匹配 name_cn
    row = conn.execute(
        "SELECT * FROM exhibition_brand WHERE brand_id = ? OR name_cn LIKE ?",
        (identifier, f"%{identifier}%")
    ).fetchone()

    if not row:
        conn.close()
        return f"错误: 未找到品牌 '{identifier}'"

    brand_id = row["brand_id"]

    editions = conn.execute(
        "SELECT year, area_sqm, exhibitors_count, visitors_count, "
        "       city, venue, status, yoy_trend "
        "FROM exhibition_edition "
        "WHERE brand_id = ? ORDER BY year DESC LIMIT ?",
        (brand_id, MAX_EDITIONS)
    ).fetchall()

    # 竞争关系查询（exhibition_relation 当前为空，实现 fallback）
    relations = conn.execute(
        "SELECT b.name_cn, b.brand_id, r.relation_type, r.notes "
        "FROM exhibition_relation r "
        "JOIN exhibition_brand b ON b.brand_id = r.to_brand_id "
        "WHERE r.from_brand_id = ? LIMIT ?",
        (brand_id, MAX_RELATIONS)
    ).fetchall()

    # Fallback: 若竞争关系表为空，改用同行业展会列表
    relation_section: list[str] = []
    if relations:
        relation_section.append("### 竞争关系网络（来自 exhibition_relation 表）")
        for r in relations:
            relation_section.append(
                f"- [{r['relation_type']}] {r['name_cn']} ({r['brand_id']}): {r['notes'] or ''}"
            )
    else:
        relation_section.append(
            "### 同行业展会（exhibition_relation 表当前无数据，fallback 到同行业聚合）"
        )
        peers = conn.execute(
            "SELECT brand_id, name_cn, scale_score, ma_potential "
            "FROM exhibition_brand "
            "WHERE industry_l1 = ? AND brand_id != ? "
            "ORDER BY scale_score DESC NULLS LAST LIMIT 20",
            (row["industry_l1"], brand_id)
        ).fetchall()
        for p in peers:
            relation_section.append(
                f"- {p['name_cn']} ({p['brand_id']}) "
                f"规模={p['scale_score'] or 'N/A'} MA潜力={p['ma_potential'] or 'N/A'}"
            )

    conn.close()

    output = [
        "### 品牌基本信息",
        f"- brand_id: {row['brand_id']}",
        f"- 中文名: {row['name_cn']}",
        f"- 英文名: {row['name_en'] or ''}",
        f"- 主办方: {row['organizer'] or '未知'}",
        f"- 行业 L1: {row['industry_l1']} / L2: {row['industry_l2']}",
        f"- MA潜力评分: {row['ma_potential'] or '未评分'}（满分5）",
        f"- 战略相关性: {row['strategic_relevance'] or '未评分'}（满分5）",
        f"- 规模评分: {row['scale_score'] or '未评分'}（满分10）",
        f"- UFI认证: {'是' if row['is_ufi_certified'] else '否'}",
        f"- 国际化: {'是' if row['is_international'] else '否'}",
        f"- 竞品关系: {row['competition_relation'] or '未标注'}",
        f"- MDS关联: {row['mds_related'] or '无'}",
        "",
        f"### 历史届次数据（最近 {MAX_EDITIONS} 届）",
    ]
    if editions:
        for e in editions:
            output.append(
                f"- {e['year']}: 面积={e['area_sqm'] or 'N/A'}m², "
                f"展商={e['exhibitors_count'] or 'N/A'}, "
                f"观众={e['visitors_count'] or 'N/A'}, "
                f"城市={e['city'] or 'N/A'}, "
                f"趋势={e['yoy_trend'] or 'N/A'}"
            )
    else:
        output.append("（暂无历史届次数据）")

    output.append("")
    output.extend(relation_section)

    return "\n".join(output)


def industry_research(industry_l1: str, industry_l2: str | None = None) -> str:
    """聚合行业展会地图"""
    conn = _connect()

    sql = (
        "SELECT brand_id, name_cn, organizer, city, scale_score, "
        "       ma_potential, strategic_relevance, is_ufi_certified, "
        "       is_international, frequency "
        "FROM exhibition_brand "
        "WHERE industry_l1 = ? "
    )
    params: list = [industry_l1]
    if industry_l2:
        sql += "AND industry_l2 = ? "
        params.append(industry_l2)
    sql += "ORDER BY scale_score DESC NULLS LAST LIMIT ?"
    params.append(MAX_INDUSTRY_BRANDS)

    rows = conn.execute(sql, params).fetchall()

    if not rows:
        conn.close()
        label = f"{industry_l1}" + (f" / {industry_l2}" if industry_l2 else "")
        return f"该行业暂无数据: {label}"

    # 统计摘要
    total = conn.execute(
        "SELECT COUNT(*) as cnt FROM exhibition_brand WHERE industry_l1 = ?",
        (industry_l1,)
    ).fetchone()["cnt"]

    ufi_count = sum(1 for r in rows if r["is_ufi_certified"])
    intl_count = sum(1 for r in rows if r["is_international"])
    city_dist = {}
    for r in rows:
        city = r["city"] or "未知"
        city_dist[city] = city_dist.get(city, 0) + 1

    conn.close()

    output = [
        f"### 行业概览: {industry_l1}" + (f" / {industry_l2}" if industry_l2 else ""),
        f"- 总品牌数: {total}（本次展示前 {min(len(rows), MAX_INDUSTRY_BRANDS)} 条，按规模排序）",
        f"- UFI认证展会: {ufi_count} 个",
        f"- 国际化展会: {intl_count} 个",
        f"- 主要城市分布: {', '.join(f'{c}({n})' for c, n in sorted(city_dist.items(), key=lambda x: -x[1])[:5])}",
        "",
        f"### 展会品牌列表（规模从高到低，前 {len(rows)} 条）",
    ]
    for r in rows:
        output.append(
            f"- {r['name_cn']} ({r['brand_id']}) | "
            f"规模={r['scale_score'] or 'N/A'} | "
            f"MA={r['ma_potential'] or 'N/A'} | "
            f"城市={r['city'] or 'N/A'} | "
            f"主办方={r['organizer'] or 'N/A'}"
        )

    return "\n".join(output)


def company_history(company_name: str) -> str:
    """查询公司名称关联的所有参展记录（跨展会、跨年份）"""
    conn = _connect()
    # 在 exhibition_brand 的 organizer / name_cn 中模糊匹配
    rows = conn.execute(
        "SELECT b.brand_id, b.name_cn, b.organizer, b.industry_l1, "
        "       e.year, e.area_sqm, e.exhibitors_count, e.city, e.status "
        "FROM exhibition_edition e "
        "JOIN exhibition_brand b ON b.brand_id = e.brand_id "
        "WHERE b.organizer LIKE ? OR b.name_cn LIKE ? "
        "ORDER BY e.year DESC LIMIT 30",
        (f"%{company_name}%", f"%{company_name}%")
    ).fetchall()

    conn.close()

    if not rows:
        return f"未找到与 '{company_name}' 相关的参展记录"

    output = [
        f"### '{company_name}' 相关参展记录（共 {len(rows)} 条，最多显示30条）",
    ]
    for r in rows:
        output.append(
            f"- {r['year']} | {r['name_cn']} ({r['brand_id']}) | "
            f"行业={r['industry_l1']} | "
            f"面积={r['area_sqm'] or 'N/A'}m² | 城市={r['city'] or 'N/A'}"
        )

    return "\n".join(output)


def edition_detail(edition_id: str) -> str:
    """查询单届详情"""
    conn = _connect()
    row = conn.execute(
        "SELECT e.*, b.name_cn, b.industry_l1 "
        "FROM exhibition_edition e "
        "JOIN exhibition_brand b ON b.brand_id = e.brand_id "
        "WHERE e.edition_id = ?",
        (edition_id,)
    ).fetchone()
    conn.close()

    if not row:
        return f"错误: 未找到届次 '{edition_id}'"

    return "\n".join([
        f"### 届次详情: {row['edition_id']}",
        f"- 展会: {row['name_cn']}",
        f"- 年份: {row['year']}",
        f"- 时间: {row['date_start'] or 'N/A'} ~ {row['date_end'] or 'N/A'}",
        f"- 城市/场馆: {row['city'] or 'N/A'} / {row['venue'] or 'N/A'}",
        f"- 面积: {row['area_sqm'] or 'N/A'}m²",
        f"- 展商数: {row['exhibitors_count'] or 'N/A'}",
        f"- 观众数: {row['visitors_count'] or 'N/A'}",
        f"- 状态: {row['status'] or 'N/A'}",
        f"- 数据来源: {row['data_source'] or 'N/A'}",
    ])


def main() -> None:
    parser = argparse.ArgumentParser(
        description="DB 数据注入脚本（供 Claude Code Skill !命令调用）"
    )
    parser.add_argument(
        "query_type",
        choices=["brand-research", "industry-research", "company-history", "edition-detail"],
        help="查询类型"
    )
    parser.add_argument("identifier", help="查询标识符（brand_id/品牌名/行业名/公司名/edition_id）")
    parser.add_argument("--l2", help="industry-research 时可选 L2 子类过滤", default=None)
    args = parser.parse_args()

    if args.query_type == "brand-research":
        print(brand_research(args.identifier))
    elif args.query_type == "industry-research":
        print(industry_research(args.identifier, args.l2))
    elif args.query_type == "company-history":
        print(company_history(args.identifier))
    elif args.query_type == "edition-detail":
        print(edition_detail(args.identifier))


if __name__ == "__main__":
    main()
```

关键约束（per D-01, D-23, Pitfall 1, Pitfall 3）：
- DB_PATH 使用 `Path(__file__).resolve().parent.parent.parent / "mwlab.db"`（与 export_for_tagging.py 一致）
- exhibition_relation 为空时必须有 fallback 到同行业聚合逻辑
- LIMIT 常量（MAX_EDITIONS=10, MAX_INDUSTRY_BRANDS=50）防止输出过大
- 所有 SQL 使用参数化查询，不拼接字符串
  </action>

  <verify>
    <automated>cd "/Volumes/databoard/AI Project/D_dashboard" && python3 tools/intel/db_query.py brand-research "EXPO-0001" | grep -E "品牌基本信息|fallback|竞争关系网络" && python3 tools/intel/db_query.py industry-research "机械和设备" | grep -E "行业概览|总品牌数"</automated>
  </verify>

  <acceptance_criteria>
    - python3 tools/intel/db_query.py brand-research "EXPO-0001" 输出第一行包含 "### 品牌基本信息"
    - python3 tools/intel/db_query.py brand-research "不存在品牌XYZ" 输出包含 "错误: 未找到品牌"
    - python3 tools/intel/db_query.py industry-research "机械和设备" 输出包含 "### 行业概览: 机械和设备"
    - python3 tools/intel/db_query.py industry-research "机械和设备" 的输出中 "总品牌数:" 后面的数字大于 2500
    - python3 tools/intel/db_query.py brand-research "EXPO-0001" 输出包含 "fallback 到同行业展会" 或 "竞争关系网络"（取决于 exhibition_relation 是否为空）
    - 文件开头包含 "#!/usr/bin/env python3"
    - grep -c "sqlite3.connect" tools/intel/db_query.py 返回 1（集中在 _connect() 函数）
  </acceptance_criteria>

  <done>db_query.py 可作为命令行工具独立运行，输出格式化文本供 LLM 读取，brand-research 具备 exhibition_relation 空表 fallback 逻辑</done>
</task>

<task type="auto">
  <name>Task 2: 创建 tools/intel/qcc_client.py（企查查 API 封装）</name>
  <files>tools/intel/qcc_client.py</files>

  <read_first>
    - .planning/phases/05-intel-backend/05-RESEARCH.md — 企查查 API 详情章节（签名方式、端点、响应结构）
  </read_first>

  <action>
创建 tools/intel/qcc_client.py，实现以下功能：

```python
#!/usr/bin/env python3
"""
tools/intel/qcc_client.py — 企查查 API 封装

用法（命令行测试）:
  python3 tools/intel/qcc_client.py "上海机床厂"
  python3 tools/intel/qcc_client.py "格力电器" --page 2

配置（环境变量）:
  QCC_APP_KEY     企查查 AppKey（客户提供）
  QCC_SECRET_KEY  企查查 SecretKey（客户提供）

未配置时降级模式：返回占位符结果，不抛出异常。
"""
from __future__ import annotations

import argparse
import hashlib
import json
import os
import time
from typing import Any

import requests

QCC_BASE_URL = "https://api.qichacha.com"
_PLACEHOLDER_KEY = "PLACEHOLDER_KEY"
_PLACEHOLDER_SECRET = "PLACEHOLDER_SECRET"
_REQUEST_TIMEOUT = 10  # 秒


def _is_configured() -> bool:
    """检查 API Key 是否已配置（区别于占位符）"""
    app_key = os.environ.get("QCC_APP_KEY", _PLACEHOLDER_KEY)
    secret_key = os.environ.get("QCC_SECRET_KEY", _PLACEHOLDER_SECRET)
    return (
        app_key != _PLACEHOLDER_KEY
        and secret_key != _PLACEHOLDER_SECRET
        and len(app_key) > 0
        and len(secret_key) > 0
    )


def _make_token(app_key: str, secret_key: str) -> tuple[str, str]:
    """
    生成企查查认证 Token（每次请求前实时生成，不缓存）。
    Token = MD5(AppKey + Timespan + SecretKey).upper()
    """
    timespan = str(int(time.time()))
    raw = f"{app_key}{timespan}{secret_key}"
    token = hashlib.md5(raw.encode("utf-8")).hexdigest().upper()
    return token, timespan


def fuzzy_search(
    keyword: str,
    page_index: int = 1,
    page_size: int = 10,
) -> dict[str, Any]:
    """
    企查查模糊搜索企业。

    降级模式（API Key 未配置时）：返回
    {"Status": "PLACEHOLDER", "Message": "QCC_APP_KEY 未配置", "Result": []}

    正常响应结构（企查查返回）：
    {
      "Status": "200",
      "Result": [
        {
          "KeyNo": str,       # 企查查内部唯一 ID
          "Name": str,        # 企业名称
          "CreditCode": str,  # 统一社会信用代码
          "StartDate": str,   # 成立日期 YYYY-MM-DD
          "OperName": str,    # 法定代表人
          "Status": str,      # 企业状态
          "No": str,          # 注册号
          "Address": str      # 注册地址
        }
      ]
    }
    """
    if not _is_configured():
        return {
            "Status": "PLACEHOLDER",
            "Message": (
                "QCC_APP_KEY / QCC_SECRET_KEY 未配置。"
                "请设置环境变量后重试。"
                f"搜索关键词: {keyword}"
            ),
            "Result": [],
        }

    app_key = os.environ["QCC_APP_KEY"]
    secret_key = os.environ["QCC_SECRET_KEY"]
    token, timespan = _make_token(app_key, secret_key)  # 每次请求重新生成

    headers = {
        "Token": token,
        "Timespan": timespan,
        "Content-Type": "application/json",
    }
    params = {
        "key": app_key,
        "searchKey": keyword,
        "pageIndex": str(page_index),
        "pageSize": str(min(page_size, 20)),  # 企查查最大 pageSize=20
    }

    try:
        resp = requests.get(
            f"{QCC_BASE_URL}/FuzzySearch/GetList",
            headers=headers,
            params=params,
            timeout=_REQUEST_TIMEOUT,
        )
        resp.raise_for_status()
        data = resp.json()

        # 检查企查查业务状态码（非 HTTP 状态码）
        if data.get("Status") != "200":
            return {
                "Status": data.get("Status", "UNKNOWN"),
                "Message": data.get("Message", "企查查 API 返回非200状态"),
                "Result": [],
            }
        return data

    except requests.exceptions.Timeout:
        return {"Status": "TIMEOUT", "Message": f"请求超时（{_REQUEST_TIMEOUT}s）", "Result": []}
    except requests.exceptions.RequestException as exc:
        return {"Status": "ERROR", "Message": str(exc), "Result": []}


def format_search_results(result: dict[str, Any]) -> str:
    """
    将 fuzzy_search 返回值格式化为可读文本（供 Skill 展示）。
    """
    status = result.get("Status")
    if status == "PLACEHOLDER":
        return f"[企查查未配置] {result.get('Message', '')}"
    if status != "200":
        return f"[企查查错误] Status={status}: {result.get('Message', '')}"

    items = result.get("Result", [])
    if not items:
        return "[企查查] 未找到匹配企业"

    lines = [f"[企查查] 找到 {len(items)} 条结果:"]
    for item in items:
        lines.append(
            f"- {item.get('Name', 'N/A')} | "
            f"KeyNo={item.get('KeyNo', 'N/A')} | "
            f"代码={item.get('CreditCode', 'N/A')} | "
            f"法人={item.get('OperName', 'N/A')} | "
            f"成立={item.get('StartDate', 'N/A')} | "
            f"状态={item.get('Status', 'N/A')}"
        )
    return "\n".join(lines)


def main() -> None:
    parser = argparse.ArgumentParser(description="企查查 API 命令行测试工具")
    parser.add_argument("keyword", help="搜索关键词（企业名、人名等）")
    parser.add_argument("--page", type=int, default=1, help="页码（默认1）")
    parser.add_argument("--size", type=int, default=10, help="每页数量（默认10，最大20）")
    parser.add_argument("--json", dest="output_json", action="store_true", help="输出原始 JSON")
    args = parser.parse_args()

    result = fuzzy_search(args.keyword, page_index=args.page, page_size=args.size)

    if args.output_json:
        print(json.dumps(result, ensure_ascii=False, indent=2))
    else:
        print(format_search_results(result))


if __name__ == "__main__":
    main()
```

关键约束（per D-09, Pitfall 2, Pitfall 5）：
- `_make_token()` 每次 HTTP 调用前实时生成，不缓存 Token（避免时间戳过期）
- API Key 未配置时返回 `{"Status": "PLACEHOLDER", "Result": []}` 而非抛出异常
- page_size 强制限制 ≤ 20（企查查上限）
- 网络异常全部 catch，返回错误字典而非抛出异常
  </action>

  <verify>
    <automated>cd "/Volumes/databoard/AI Project/D_dashboard" && python3 tools/intel/qcc_client.py "上海机床" 2>&1 | grep -E "PLACEHOLDER|找到|错误"</automated>
  </verify>

  <acceptance_criteria>
    - python3 tools/intel/qcc_client.py "测试公司" 输出包含 "PLACEHOLDER" 或 "找到" 字样（不抛出异常）
    - QCC_APP_KEY 未设置时输出包含 "未配置" 字样
    - grep -c "_make_token" tools/intel/qcc_client.py 返回至少 2（定义处 + 调用处，每次请求都重新调用）
    - grep -c "raise" tools/intel/qcc_client.py 返回 0（所有异常被 catch，不向上抛出）
    - python3 -c "from tools.intel.qcc_client import fuzzy_search; r=fuzzy_search('test'); print(r['Status'])" 输出 "PLACEHOLDER"（API Key 未配置环境下）
    - 文件包含字符串 "QCC_APP_KEY / QCC_SECRET_KEY 未配置"
  </acceptance_criteria>

  <done>qcc_client.py 可作为命令行工具测试，未配置 API Key 时优雅降级（不报错），Token 每次请求实时生成避免过期</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 参数 → SQLite 查询 | 用户输入的 identifier 参数进入 SQL LIKE 查询 |
| Python → 企查查 API | API Key 以明文传输至外部服务 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Injection | db_query.py SQL 参数 | mitigate | 所有 SQL 使用参数化查询（`conn.execute(sql, params)`），禁止字符串拼接 |
| T-05-05 | Information Disclosure | QCC_APP_KEY 环境变量 | accept | API Key 通过环境变量注入，不写入代码或日志；本地工具脚本，不对外暴露 |
| T-05-06 | Tampering | 企查查 API 响应数据 | accept | 工具脚本直接使用响应数据，BD 人工核验，无自动决策风险 |
| T-05-07 | Denial of Service | DB 查询输出过大 | mitigate | MAX_EDITIONS=10, MAX_INDUSTRY_BRANDS=50 硬限制输出行数 |
</threat_model>

<verification>
执行以下命令验证 Wave 2 Plan 02 完成：

```bash
# 1. 测试 db_query.py 基本功能
cd /Volumes/databoard/AI\ Project/D_dashboard
python3 tools/intel/db_query.py brand-research "EXPO-0001"
python3 tools/intel/db_query.py industry-research "机械和设备" | head -10

# 2. 测试 qcc_client.py 占位符降级
python3 tools/intel/qcc_client.py "格力电器"
# 预期: 输出包含 "PLACEHOLDER" 或 "未配置"

# 3. 确认参数化查询（无 SQL 拼接）
grep -n "%" tools/intel/db_query.py | grep -v "LIKE\|format\|f\"" | wc -l
# 预期: 0（没有字符串格式化 SQL）
```
</verification>

<success_criteria>
- db_query.py 支持 4 种查询模式，可独立命令行执行
- brand-research 对 exhibition_relation 空表有 fallback 逻辑
- qcc_client.py API Key 未配置时优雅降级，不抛异常
- 所有 SQL 参数化，无注入风险
- 两个脚本可被 import 使用（非仅 __main__）
</success_criteria>

<output>
完成后在 .planning/phases/05-intel-backend/ 创建 05-02-SUMMARY.md，记录：
- db_query.py 支持的 4 种查询命令及参数格式
- qcc_client.py 降级模式说明
- 已知限制（exhibition_relation 为空时的 fallback 行为）
</output>
