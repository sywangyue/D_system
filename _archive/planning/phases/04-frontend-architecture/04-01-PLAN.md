---
phase: 04-frontend-architecture
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - auth_api.py
  - lib/db.ts
  - lib/auth.ts
  - .env.local
autonomous: true
requirements: [UI-POOL]
user_setup:
  - service: "mwlab.db"
    why: "Existing SQLite DB with user table — verify password hash format"
    action: "Run: python3 -c \"import sqlite3; conn=sqlite3.connect('mwlab.db'); row=conn.execute('SELECT password_hash FROM user LIMIT 1').fetchone(); print(row[0][:20] if row else 'no user'); conn.close()\""
    expected: "Starts with $2b$ (bcrypt hash). If not, run: scripts/hash-passwords.py"
must_haves:
  truths:
    - "FastAPI auth_api.py starts without import errors on port 8000"
    - "lib/db.ts provides better-sqlite3 singleton for mwlab.db reads"
    - "Python dependencies passlib[bcrypt] + pyjwt are installed"
    - "Supabase-specific code path is removable"
  artifacts:
    - path: "auth_api.py"
      provides: "JWT login/verify FastAPI endpoints"
      contains: "POST /api/auth/login"
    - path: "lib/db.ts"
      provides: "better-sqlite3 readonly singleton"
      exports: "getDb()"
    - path: "lib/auth.ts"
      provides: "Client-side auth helpers (localStorage token read)"
      exports: "getUserInfo(), clearAuth(), isAuthenticated()"
---

<objective>
建立 Phase 4 的基础设施层：FastAPI JWT 认证后端（auth_api.py）、前端 SQLite 直连层（lib/db.ts）、客户端认证工具（lib/auth.ts）。这是后续所有 API 和页面重构的依赖基础。

Purpose: 替代原 Supabase Auth（保留的 D-16 JWT 3 角色认证），建立 Python FastAPI 认证端点 + Node.js better-sqlite3 BFF 查询层
Output: auth_api.py, lib/db.ts, lib/auth.ts, 更新后的 .env.local
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-frontend-architecture/04-RESEARCH.md (参见 Architecture Patterns、Standard Stack、FastAPI auth_api.py 代码示例、Pattern 1: better-sqlite3 连接单例)
@./tag_api.py (现有 FastAPI 参考——CORS middleware 模式、数据库连接模式)
@./schema/init_db.sql (user 表结构)
@./.env.local (当前 Supabase 密钥——将被替换)
</context>

<interfaces>
从 04-RESEARCH.md 提取的关键接口：

auth_api.py 接口（每个路由必须实现）：
```python
POST /api/auth/login -> { token: str, email: str, role: str, display_name: str }
  Request: { email: str, password: str }
GET /api/auth/verify -> { valid: bool, email: str, role: str }
  Header: Authorization: Bearer <token>
```

lib/db.ts 接口：
```typescript
export function getDb(): Database.Database  // better-sqlite3 readonly singleton
```

lib/auth.ts 接口：
```typescript
export function getUserInfo(): { email: string; role: string } | null
export function clearAuth(): void
export function isAuthenticated(): boolean
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 auth_api.py——FastAPI JWT 认证端点（独立文件，不含并到 tag_api.py）</name>
  <files>auth_api.py</files>
  <read_first>
    @./tag_api.py (参考: CORS middleware 配置、sqlite3 连接模式)
    @./schema/init_db.sql (user 表: user_id, email, password_hash, role, is_active, display_name)
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md (FastAPI auth_api.py 代码示例, lines 760-854)
  </read_first>
  <action>
  创建 /Volumes/databoard/AI Project/D_dashboard/auth_api.py，实现以下端点：

  注意（W1 修复——独立 auth_api.py 的决策理由）：RESEARCH.md Q1 推荐将 auth 端点并入 tag_api.py，
  但本计划创建独立 auth_api.py。理由：
  1. 关注点分离——auth（用户认证）与 tag（品牌打标）职责不同
  2. 避免 CORS middleware 配置冲突——tag_api.py 已有 CORS 配置，合并可能导致隐式互相影响
  3. 独立部署——auth 端点可能需要独立扩缩容或更换 JWT 实现（如从 PyJWT 迁移到 Auth0）
  4. D-17 "保留 FastAPI" 的实现同时兼容两种方式，代理层（Next.js API Routes）屏蔽了后端子服务细节

  1. **文件头部**：导入 fastapi, jwt (PyJWT), passlib, pydantic, sqlite3, os, datetime, fastapi.middleware.cors
     - JWT_SECRET = os.environ.get("JWT_SECRET", "mwlab-dev-secret-2026")
     - JWT_ALGORITHM = "HS256", JWT_EXPIRY_HOURS = 24
     - pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
     - DB_PATH = Path(__file__).parent / "mwlab.db"
     - security = HTTPBearer()

  2. **CORS middleware** (参考 tag_api.py，但使用 FastAPI 内置 CORSMiddleware):
     - allow_origins=["http://localhost:3000", "http://localhost:3001"]
     - allow_credentials=True, allow_methods=["*"], allow_headers=["*"]

  3. **Pydantic models**:
     - LoginRequest(BaseModel): email: str, password: str
     - LoginResponse(BaseModel): token: str, email: str, role: str, display_name: str

  4. **POST /api/auth/login**:
     - 查 user 表: SELECT * FROM user WHERE email = ? AND is_active = 1
     - bcrypt.verify(password, user["password_hash"])
     - 失败 → 401 "邮箱或密码错误"
     - 成功 → JWT payload: { user_id, email, role, exp(utc+24h) }
     - 返回 { token, email, role, display_name }

  5. **GET /api/auth/verify**:
     - security 依赖注入获取 token
     - jwt.decode → 返回 { valid: true, email, role }
     - jwt.ExpiredSignatureError → 401 "Token 已过期"
     - jwt.InvalidTokenError → 401 "无效 Token"

  6. **GET /api/auth/users** (admin only):
     - JWT role check → role === 'admin' 否则 403
     - SELECT user_id, email, role, is_active, last_login FROM user ORDER BY email
     - 返回 [{ user_id, email, role, is_active, last_login }]

  **测试命令**：
  ```bash
  python3 -c "import sys; sys.path.insert(0, '.'); from auth_api import app; print('auth_api OK')"
  ```
  </action>
  <verify>
    <automated>
      python3 -c "import sys; sys.path.insert(0, '.'); from auth_api import app; print('auth_api imports OK')" && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def login" auth_api.py -> 存在
    - grep -q "/api/auth/login" auth_api.py -> 存在
    - grep -q "def verify_token" auth_api.py -> 存在
    - grep -q "/api/auth/verify" auth_api.py -> 存在
    - grep -q "jwt.encode" auth_api.py -> 存在
    - grep -q "CORSMiddleware" auth_api.py -> 存在
    - grep -q "mwlab.db" auth_api.py -> 存在
    - python3 -c "import sys; sys.path.insert(0, '.'); from auth_api import app; print('OK')" 无 ImportError
  </acceptance_criteria>
  <done>
    auth_api.py 文件存在，包含 login + verify + users 三个端点，可导入无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 lib/db.ts 连接单例 + lib/auth.ts 客户端工具</name>
  <files>lib/db.ts, lib/auth.ts, .env.local</files>
  <read_first>
    @.planning/phases/04-frontend-architecture/04-RESEARCH.md (Pattern 1: better-sqlite3 连接单例, lines 546-570)
    @./.env.local (当前 Supabase URL/keys——将被替换)
  </read_first>
  <action>
  创建/修改以下文件：

  **A) lib/db.ts** — better-sqlite3 readonly 连接单例
  ```typescript
  import Database from 'better-sqlite3'
  import path from 'path'

  let db: Database.Database | null = null

  export function getDb(): Database.Database {
    if (!db) {
      db = new Database(path.join(process.cwd(), 'mwlab.db'), {
        readonly: true,
        fileMustExist: true,
      })
      db.pragma('journal_mode = WAL')
      db.pragma('cache_size = -64000')
    }
    return db
  }
  ```

  **B) lib/auth.ts** — 客户端认证帮助函数（从 localStorage 读取 JWT 用户信息）
  ```typescript
  const AUTH_KEY = 'mwlab_auth'

  export interface UserInfo {
    email: string
    role: string
    display_name: string
  }

  export function saveAuth(info: UserInfo, token: string): void {
    if (typeof window === 'undefined') return
    localStorage.setItem(AUTH_KEY, JSON.stringify({ ...info, token }))
  }

  export function getUserInfo(): UserInfo | null {
    if (typeof window === 'undefined') return null
    try {
      const raw = localStorage.getItem(AUTH_KEY)
      if (!raw) return null
      const parsed = JSON.parse(raw)
      return { email: parsed.email, role: parsed.role, display_name: parsed.display_name }
    } catch { return null }
  }

  export function getToken(): string | null {
    if (typeof window === 'undefined') return null
    try {
      const raw = localStorage.getItem(AUTH_KEY)
      if (!raw) return null
      return JSON.parse(raw).token || null
    } catch { return null }
  }

  export function clearAuth(): void {
    if (typeof window === 'undefined') return
    localStorage.removeItem(AUTH_KEY)
    document.cookie = 'session=; path=/; max-age=0'
  }

  export function isAuthenticated(): boolean {
    return getUserInfo() !== null
  }
  ```

  **C) .env.local** — 添加 JWT 配置行
  保留现有内容，在文件末尾追加：
  ```
  JWT_SECRET=mwlab-dev-secret-2026
  FASTAPI_URL=http://localhost:8000
  NEXT_PUBLIC_APP_URL=http://localhost:3000
  ```
  </action>
  <verify>
    <automated>
      ls lib/db.ts && ls lib/auth.ts && grep -q "JWT_SECRET" .env.local && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - lib/db.ts 存在且包含 "better-sqlite3" import 和 "export function getDb" 导出
    - lib/auth.ts 存在且包含 "getUserInfo", "saveAuth", "clearAuth", "isAuthenticated" 导出
    - .env.local 包含 JWT_SECRET= 行
    - .env.local 包含 FASTAPI_URL= 行
    - grep -c "export function" lib/auth.ts >= 4
  </acceptance_criteria>
  <done>
    lib/db.ts（better-sqlite3 单例）、lib/auth.ts（4 个导出函数）、.env.local（JWT 配置）全部就绪
  </done>
</task>

<task type="auto">
  <name>Task 3: 安装 Python 依赖 + 验证密码格式</name>
  <files>auth_api.py</files>
  <read_first>
    @./auth_api.py (Task 1 创建——确保依赖可运行)
  </read_first>
  <action>
  1. 安装 Python 依赖：
  ```bash
  pip install passlib[bcrypt] pyjwt
  ```

  2. 检查 mwlab.db user 表密码格式：
  ```bash
  python3 -c "
  import sqlite3
  conn = sqlite3.connect('mwlab.db')
  row = conn.execute('SELECT password_hash FROM user LIMIT 1').fetchone()
  if row:
      print(f'Hash: {str(row[0])[:30]}')
      is_bcrypt = row[0] and str(row[0]).startswith('\$2')
      print(f'Is bcrypt: {is_bcrypt}')
  else:
      print('No users — create seed script')
  conn.close()
  "
  ```

  3. 如果 mwlab.db 中没有任何 user 记录，创建 /Volumes/databoard/AI Project/D_dashboard/scripts/seed_db_users.py：
  - admin@mwlab.internal / admin123 / role=admin
  - manager@mwlab.internal / manager123 / role=manager
  - readonly@mwlab.internal / readonly123 / role=readonly
  使用 passlib bcrypt 哈希密码后 INSERT INTO user。

  4. 验证 auth_api.py 可启动：
  ```bash
  uvicorn auth_api:app --port 8000 &
  sleep 2
  curl -s http://localhost:8000/api/auth/verify | python3 -m json.tool
  ```
  </action>
  <verify>
    <automated>
      python3 -c "import jwt; import passlib; print('passlib+pyjwt OK')" && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - `pip list 2>/dev/null | grep -i passlib` 输出非空
    - `pip list 2>/dev/null | grep -i pyjwt` 输出非空
    - `python3 -c "import jwt; import passlib; print('OK')"` 无 ImportError
    - 如 user 表有记录，所有 password_hash 以 $2 开头
    - 如 user 表无记录，scripts/seed_db_users.py 存在
    - uvicorn auth_api:app --port 8000 可启动
  </acceptance_criteria>
  <done>
    Python 依赖就绪、密码格式已验证/用户已 seed、auth_api.py 可启动运行
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI auth_api | 用户登录凭证跨网络传输 |
| FastAPI -> mwlab.db | 认证服务器读取 user 表密码哈希 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | POST /api/auth/login | mitigate | bcrypt 密码哈希验证（passlib），401 不泄露用户名是否存在 |
| T-04-02 | Tampering | JWT tokens | mitigate | HS256 签名验证，JWT_SECRET 通过环境变量注入 |
| T-04-03 | Information Disclosure | GET /api/auth/users | mitigate | JWT role claim 验证，仅 admin 可访问，否则 403 |
| T-04-04 | Spoofing | JWT 伪造 | mitigate | PyJWT decode 验证签名和 exp，InvalidTokenError 统一 401 |
</threat_model>

<verification>
1. `python3 -c "import jwt; import passlib; print('deps OK')"` -- 依赖安装成功
2. `uvicorn auth_api:app --port 8000 &` 可启动
3. `grep -q 'export function getDb' lib/db.ts` -- 导出正确
4. `grep -q 'export function clearAuth' lib/auth.ts` -- 导出正确
5. `.env.local` 包含 JWT_SECRET 和 FASTAPI_URL
</verification>

<success_criteria>
- auth_api.py 在 port 8000 可独立启动，login + verify + users 三个端点可用
- lib/db.ts 提供 getDb() 导出，无编译错误
- lib/auth.ts 提供 localStorage-based 认证工具函数（4 个导出）
- Python 依赖 passlib + pyjwt 已安装
- user 表密码格式已验证为 bcrypt 或已创建种子脚本
</success_criteria>

<output>
After completion, create .planning/phases/04-frontend-architecture/04-01-SUMMARY.md
</output>
